# Traps — the live footguns, and the mutation registry

Everything here has been paid for at least once. Each entry is **still live**: the underlying hazard exists in the tools, the engine, or the codebase's own conventions, and forgetting it re-arms it. Physics-model hazards live with their mechanisms in [DESIGN.md](DESIGN.md); this file is the operational layer — build, engine, tooling, and test practice.

The recurring enemy in this project is **a wrong answer that looks plausible**: nothing crashes, nothing moves, every readout agrees, and the wall stands there being wrong. Most entries below are instances of that shape.

---

## Running the tests

- **Automation runs exit 0 even when tests fail, and results never reach stdout.** Read `Saved/Logs/DestructionGame.log` and grep `Test Completed` / `LogAutomationController: Error` (commands in [CLAUDE.md](../CLAUDE.md)). Every run also reports `6382 tests available` — the engine-wide list; **the count of `Test Completed` lines is the only guard** that the filter ran what you meant.
- **Any test flagged `EAutomationTestFlags::NonNullRHI` is invisible to the documented `-nullrhi` command** — filtered out, not skipped-with-a-note. One visual test rotted through five slices this way and produced twelve errors when finally run. Run the visual set deliberately in any verification claiming completeness:

  ```
  UnrealEditor-Cmd.exe <project>\DestructionGame.uproject /Game/Maps/Lvl_Sandbox
    -game -windowed -ResX=1920 -ResY=1080 -ForceRes -RenderOffScreen
    -nosplash -NoSound -unattended -nopause -log -abslog=<unique path>
    -ExecCmds="Automation RunTests DestructionGame.Visual"
    -TestExit="Automation Test Queue Empty"
  ```

  Under `-nullrhi` a `NonNullRHI` test is not listed at all — look for its absence, not for a pass. **Omit `-game -RenderOffScreen` and the screenshot test HANGS with no timeout** rather than failing (observed: seven silent minutes, zero frames).
- **An opt-in tier is a tier that ROTS** — the same hazard as the `NonNullRHI` entry above, arrived at deliberately rather than by accident, and it is the strongest argument against the 2026-08-16 split of the oracle sweep into `OracleSweepFast` (10 tests, ~90 s, for iteration) and `OracleSweepFull` (5 tests, ~22 min, of which three walls are 94% of the cost — the ones that watch the solver at scale). The measured provocation was real — 93% of a 21.8-minute group was three tests, and most runs that week were confirming that a *comment edit* broke nothing — but the mitigation is documentation and nothing else: **`OracleSweepFull` is mandatory before any commit that touches the LP oracle, and before any commit at all if the solver changed.** A green fast tier is not verification of anything. The filter is a plain substring match (`AutomationCommandline.cpp`, `GenerateTestNamesFromCommandLine`), so the shared `OracleSweep` stem runs both — **measured at 7 / 3 / 10 `Test Completed` lines on 2026-08-16, 9 / 4 / 13 on 2026-08-18, 10 / 4 / 14 earlier on 2026-08-21 (Slice 0c added `Case21ResidualAttribution` to the fast tier), and 10 / 5 / 15 on 2026-08-21 (the block-size threshold's `DeletionResolveLatencyLadder` folded into Full)** -- the counts move whenever a slice lands, which is exactly why `Run-OracleSweep.ps1` now ASSERTS them: a test that is in a tier but in no parallel bucket runs nowhere, and a run that quietly does less looks identical to one that passed**, because filter semantics in this engine are exactly the thing the `+`-join entry below says not to assume. Neither name contains `DestructionGame`, so the default suite still cannot reach either.
- **A `+`-joined filter over two `DestructionGame.*` paths produces WRONG ANSWERS, not merely a wrong set** — measured: fabricated failures from functions that cannot return the values reported, while each filter alone and the full suite are green. `+` is safe only for joining distinct suites (`DestructionGame+Project.Functional Tests`). **Narrow with a single lengthened path**, and confirm anything surprising against the full run before believing it.
- **Functional tests (none exist yet)** register under `Project.Functional Tests.*`, not `DestructionGame.*`, discovery is an asset-registry tag written **when the map is saved in the editor** (a new C++ class alone registers nothing), and the cook gate's ini section must stay spelled `[/Script/UnrealEd.ProjectPackagingSettings]` — the legacy name is pinned by `OverrideConfigSection`, and "correcting" it to `DeveloperToolSettings` silently disables the whole section. `DirectoriesToNeverCook` takes a long package path and is applied only in by-the-book cooks. CLAUDE.md needs its two documented edits the day the first functional test lands.

## Builds

- **The unity-build anonymous-namespace landmine.** A unity blob is one translation unit, so every anonymous namespace in it is the *same* namespace, and adaptive unity keys the blob membership off `git status` — a collision appears when you commit and disappears when you touch the file. It has fired from **inside engine source** (`constexpr double F` in a test file colliding with locals in `Chaos/Utilities.h`, four C4459 errors naming the wrong culprit) and between production files (`BedJointCosine`, `GravityCmPerSecondSquared`). Rules: test files use named `*TestSupport` namespaces with `using namespace` **inside** each `RunTest` body; production file-locals carry a file prefix (`Solver…`, `Presenter…`, `GameMode…`); and verify with the blob build, because a normal green build proves nothing here:

  ```
  Build.bat DestructionGameEditor Win64 Development -Project=… -WaitMutex -DisableAdaptiveUnity -StressTestUnity
  ```

  The trigger is *adding an unrelated file*, so it can fire on anyone at any time. A `-Check` script over `Tests/` ("no anonymous namespace, no file-scope declaration") is wanted and unwritten — see CURRENT_STATE housekeeping.
- **"Target is up to date" can be a lie after a copy-restore.** UBT rebuilds by timestamp; `Copy-Item` preserves `LastWriteTime`, so restoring a mutated file can leave the correct source *older* than the object built from the mutation — the build links yesterday's arithmetic and every test reports pre-feature values while `git diff` shows the feature intact. Touch restored files and confirm the link ran. This matters here because **mutation testing is routine**, so half-restored builds are a routine hazard.
- **Editing a source file through PowerShell 5.1 CORRUPTS every non-ASCII character in it, silently.** `Get-Content -Raw` decodes a BOM-less UTF-8 file as the ANSI codepage and `Set-Content -Encoding utf8` re-encodes it *and adds a BOM*, so one round-trip turns every em-dash in the file into `â€"` and moves the first three bytes. The build still succeeds and the tests still pass — the damage is only visible in `git diff --numstat`, as hundreds of touched lines in a file you changed one constant in (measured: 1,062/344 where the honest diff was 718/0). This bit during slice 0a's mutation runs, where PowerShell was being used precisely because the edit was a one-token search-and-replace. **Use the editing tools, not the shell.** If it has already happened, the transform is reversible — read as UTF-8, `GetBytes` through codepage 1252, `GetString` as UTF-8, write with `UTF8Encoding($false)` — and `git diff --numstat` is what confirms the reversal.
- **A BOM-less UTF-8 `.ps1` is read by PowerShell 5.1 as ANSI, and one em-dash breaks the PARSE.** The sibling of the editing hazard above, from the writing side: a script authored with any non-ASCII character in it — an em-dash in a comment is enough — fails to run at all, with a cascade of parser errors pointing at *unrelated* lines several screens away (`Unexpected token 'tier'`, `Missing closing ')'`, `The string is missing the terminator`). Nothing indicates an encoding problem; it reads as a syntax error you did not make. Measured 2026-08-16 on `Run-OracleSweep.ps1`. **Keep project `.ps1` files to plain ASCII** — a BOM would also work but is invisible and gets lost on the next round-trip, whereas `LC_ALL=C grep -n '[^ -~\t]' <file>` is a check anyone can run.
- **Reverting a mutation**: `git checkout -- <path>` only if HEAD holds what you want — it destroys uncommitted work with no reflog to recover from (this happened; `RemovePiece` and friends were rebuilt from context). For uncommitted files: copy aside first, restore by copying back, **then touch**.

## Engine and content

- **`SM_Cube`'s pivot is a corner, not its centre** (local bounds (0,0,0)–(100,100,100)). Placing an actor at a box's centre puts the brick a half-size out on all three axes while `GetActorLocation()` agrees with the layout perfectly. Derive scale as `DesiredSizeCm / MeshLocalSizeCm` from `GetBoundingBox()` — never `/ 100.0` — subtract the scaled local-bounds centre, and **assert on `GetComponentsBoundingBox()`, never `GetActorLocation()`** (bounds are pivot-agnostic *and* catch a 100× scale error in the same comparison).
- **Nanite silently ignores `SetOverlayMaterial`** — the Nanite scene proxy has no overlay member; no warning, no log line. Anything the game highlights with an overlay must be a non-Nanite mesh (`SM_Cube` now is; `Content.BrickMeshCanDrawTheHighlightOverlay` asserts both the authoring flag and the runtime reading). ISM/HISM **can** draw overlays — the ISM hazard is different: an overlay material without `bUsedWithInstancedStaticMeshes` silently falls back to the default material. The highlight was covered at three levels and all three were green while the player saw nothing, because the layer underneath was never exercised; the remaining hole of the same shape is **shader compilation** (a material whose shader failed to compile has a perfect node graph and renders as the checkerboard).
- **A scenario map must be DUPLICATED, never file-copied** — a map's `PrimaryAssetId` is the inner `UWorld` object's name, not the filename; a byte copy leaves every copy claiming the original's id and **the editor refuses to open any of them**, while the `-game` loader (which never consults the asset manager) plays them all happily. Twenty-eight levels shipped broken this way. **A `-game` load is not evidence a map can be opened**: content claims must ask the asset registry. `Scripts/New-ScenarioMap.ps1` is the supported route; `Content.ScenarioMapsAreDistinctAssets` is the net. (Full story in [LEVELS.md](LEVELS.md).)
- **`NewObject<UObject>` trips an engine ensure** (abstract class), which counts as a failure of whatever test is running. Use a concrete type: `NewObject<UStaticMeshComponent>(GetTransientPackage())`.
- **`TArray::Add(Array[0])` asserts and kills the whole automation run** (aliasing its own storage across a possible reallocation). Copy to a local first; same for `Emplace`/`Insert`/`Push`.
- **`TestTrue(FString::Printf(..., SideEffect()), Condition)` is unsequenced** — MSVC evaluates the condition first, so a state-changing call folded into the message reads the flag before setting it. Make the side effect its own statement.
- **Headless Python asset work: a surprising read-back is suspect before the asset is.** `get_editor_property` returns a *plausible empty value* for a UPROPERTY that moved between engine versions — `UInputMappingContext::Mappings` is deprecated and reads back empty (live array: `default_key_mappings.mappings`); `UMaterial` `expressions` reads 0 for a full graph (moved to editor-only data); editor subsystems are `None` under `-run=pythonscript`. Cross-check from C++ before acting. Also: `unreal.Key` rejects positional args, and `save_loaded_asset` defaults to only-if-dirty while `MapKey` does not dirty — pass `False` and call `modify()`.
- **`FBrickTestWorld::Begin` names the game mode; the default is Epic's plain `AGameModeBase`.** Without that, every world test silently runs the real game mode and would spawn a 1,220-brick wall each. Do not undo it. The shared `SpawnFloor` subtracts the scaled bounds centre (it was half-off for a long time with nothing noticing); leave it.
- **A backslash at the end of a `//` comment line is a real C++ line continuation** — the next physical line is spliced into the comment. Harmless while the next line is also a comment; a code line after it silently vanishes. This is why `Convert-CommentBlocks.ps1` refuses `StructureCascadeTest.cpp` (exit 2) rather than guessing — the refusal is the warning.

## Strength basis

- **The governing-axis flips are real, measured, and not exotic** *(2026-08-14, the flip itself)*: case 9's wide jamb went tension → **Mohr-Coulomb shear** (0.985 → 0.2633, not /7), case 22's pre-cascade worst the same way (8.2241 → 2.9133, not the predicted 1.1749), case 21's worst joint likewise (3.8429 → 0.9354, not 0.549), the one-cell mortared half seat's LP bound went tension → crushing (403.75 → 1877.92, not ×7), corbel F's root went tension → compression, and the LP-side flips ran the other way too (wall-17 λ* did not move at all while wall-18's moved ×1.38; the free-end λ* height-identity died outright). Any re-derivation touching a pre-flip figure must establish the axis first — the /7 shortcut is only ever valid after the axis is proven tension both sides.
- **AND NAMING THE NEW AXIS BY EYE IS ITSELF A TRAP: the first two attributions after the flip were WRONG, and both were plausible.** `ComputeUtilisation` returns the worst of three axes and reports only the number, so which one won is invisible unless a test computes it — and for a day DESIGN §6, CURRENT_STATE, TRAPS and a test comment all recorded case 9's jamb as "the squeezed edge's COMPRESSION against the unmoved 10 MPa", a reading that is thirteen times under what production reports. What settled it was **one published formula per axis over quantities production already publishes** (`GetConnectionForce`, `GetConnectionMoment`, `GetConnectionCompositeDepthCm`) against the profile strengths written out in the test — 0.25701313 / (0.9 + 0.75 × 0.10153605) reproduced 0.26329211195559277 to all seventeen digits, and the same three lines named case 22's. That is cheap enough that **an axis claim in prose with no formula beside it should be treated as unverified**. Related sub-trap: a kern-clamped joint's flexural tension is EXACTLY zero (bending and normal stress cancel bit for bit), so "the tension fell below the compression" can be a wildly wrong description of a joint whose tension is not a small number but a zero.
- **A lower bond RESTORES LESS, so lowering a strength makes a break MORE likely, not less** *(2026-08-14, the interim guard's basis discriminator)*: the spec for that test was recorded as "the 10-step bare arm, 1.085 at 0.70 and ~0.93 at 0.6", scaling the 0.70 ratio by 0.6/0.70 instead of 0.70/0.6. Measured, the 10-step arm reads 1.2662 at 0.6 — it fires at BOTH bases and discriminates nothing; NINE steps is the rung whose verdict turns on the basis. The general form: a demand-over-capacity ratio moves INVERSELY with the capacity, so a discriminating window written for "ratio inside (1.0, 7/6)" is really "inside (6/7, 1.0)" and the verdicts are the other way round. Work the direction, do not scale the number.
- **The §8 rulings' characteristic-vs-mean discount is SPENT — never apply it twice.** Until 2026-08-13 the profiles carried characteristic strengths (f_xk1 = 0.10, f_vk0 = 0.20) and every §8 ruling that read a model or LP number against a mean-basis judgement divided by ~6 (case 8's "still 18.0 after /3 and /6", case 9's "~0.15× mean", case 19's "~0.21× mean", the sweep header's "multiply by ~6"). The mean-strength re-anchor moves that factor INTO the data (tension 0.70, cohesion 0.90, μ 0.75, cap 2.0 — Gooch et al. 2023/2025 + JCSS, derivation record in the re-anchor slice). From the flip onward a model reading IS a mean-basis reading: applying /6 (or ×6) to it on top re-spends a budget that no longer exists, and it is the single most likely way for the re-anchor to silently corrupt a future ruling. Any prose still quoting the /6 route is describing the characteristic era; the git history of the §8 entries is the record. Corollary: mean ≠ characteristic scaling is NOT uniform — tension moved ×7, cohesion ×4.5, compression ×1 — so the cohesion/tension governing axis can FLIP at the re-anchor and no anchor may be re-pinned by scaling alone; re-measure.

## Fail-closed conventions in the arithmetic

- **NaN is laundered by `FMath::Min`/`Max`** (`(A < B) ? A : B` — every NaN comparison is false), so a NaN can become a plausible capacity. Guards are spelled **`!(x > 0.0)`**, never `x <= 0.0`, so NaN lands *inside* the guard; degenerate inputs return `TNumericLimits<double>::Max()`, never zero. The NaN guards and the shear cap are **coupled** — the cap would launder a NaN into a believable number if the compression-axis `IsFinite` guard were dropped. The break comparison is written `!(x <= 1.0)`; don't "simplify" it back.
- **Fail-closed for a mass is NaN, never zero** — `AddPiece` deliberately accepts zero ("a massless piece is meaningful"), so a zero from a degenerate box would be laundered into a real piece that weighs nothing and never breaks anything. NaN is refused by the existing guard; no second guard exists anywhere.
- **A value that is safe as an ANSWER is unsafe as a COMMAND — fail-closed inverts polarity at every seam where a diagnostic becomes an action.** `EPieceSupport::Falling` is the zero enumerator so an absent answer promises least (right for a readout) — and one layer up, `Falling` triggers irreversible release (before the guard existed, an unsolved structure released *entire*, foundation included, permanently). The house pattern: anything irreversible first asks whether there is an answer at all (`FStructure::HasSupportAnswer`, per handle — empty array and short array are separate ways to be outside the extent). Look for this trap wherever a support state or utilisation crosses out of the world-free layer.
- **The degenerate-normal caller obligation** (`ClassifyForce` + `ComputeUtilisation` composed naively read a joint with no interface plane as healthy) is stated in DESIGN §2 — it applies to any new direct caller.
- **`AddConnection`'s normal guard has a known top-end hole** (components ~1e200 → `Normalize` returns true having produced the zero vector, classified as a head joint). Unreachable from real content; fixing it is a **lockstep change** with the fuzz oracle's deliberate transcription of the same normalisation — one ulp of drift is five spurious cascade-fuzz failures. Both sites carry the instruction.
- **An overlap is an interval intersection, `min(highs) − max(lows)` — never `reach − distance`.** The two agree for equal-sized and partially-overlapping boxes and diverge silently the moment one span *contains* the other, with the area and the rectangle wrong **together**, so a rectangle-times-extents consistency check passes on the wrong pair. This shipped in `MakeInterface` (fixed 2026-08-08, held by `Layout.Interface`'s contained-pier rows) and a copy still lives, latent, in `LayoutTest.cpp`'s `ContactsOf` oracle — see CURRENT_STATE, Layout producer.

## Test practice

- **A green-on-arrival test is indistinguishable from one that asserts nothing until production is mutated.** Every fuzz property and every green-on-arrival row is proven to bite by a recorded mutation before it is trusted. Chained reds (refusal rows written against a stub) prove nothing until the happy path exists — mutate the door guard instead.
- **An invariant asserted over fixtures that all share a hidden property is not an invariant.** When an invariant has never failed, check whether it *can*.
- **Fixed simulated seconds, never a settle-poll**; group world tests by world configuration; the integration entry rule is in DESIGN §4.
- **Reviews**: transcribe-and-fuzz found what reading did not; scale review depth to topological code with silent failure modes, not to tables of constants; never re-verify findings already closed.

### The mutation registry — reuse these, never invent from scratch

Run against otherwise-unmutated production; expected signatures below. **(b) hangs rather than fails — run under a timeout.** (g)/(h) are the only mutations that watch an over-eager break; every other property inherits production's break set as its premise, so do not use them to demonstrate anything else.

| # | Mutation | Signature |
|---|---|---|
| — | `Structure.cpp`: drop the falling-support filter from the split | `Structure.Fuzz` 4,604 failures (seed 20260754) |
| — | strand on un-orderability instead of `LoadReturnsToPiece` | `Structure.Fuzz` 4,025 (seed 20260742); `PieceSupportReason` 6 failures, the two downward-stranding rows are the only tests naming the fault |
| (a) | `HasGiven` filter moved out of the tier decision | CascadeFuzz 13,411; the cheap one, and proves the two fuzzes are not duplicates (`Structure.Fuzz` stays green) |
| (b) | delete the already-given skip in the break sweep | **HANG** — no result at all |
| (c) | cascade stops after the first breaking pass | 4,294 |
| (d) | count the final non-breaking pass | 17,031 |
| (e) | delete the `HasGiven` skip from `SolveLoads` entirely | 13,506 |
| (f) | never write the break-pass stamp | 14,651 |
| (g) | joints give at `!(u <= 0.1)` | 2,280 — **BROKE UNDER CAPACITY and nothing else** |
| (h) | scale force ×1e6 from pass 2 onward | 1,537 — later-pass sweep only; pass 1 bit-correct |
| (i)/(j) | test-side: rebuild `SurvivingGraphOf` at `Pass ∓ 1` | (i) all 706 later-pass breaks fail; (j) floors fire — the only check that the rebuild targets the right pass |
| (k) | `UtilisationUnder` returns 0 when given | exactly the 4 given-joint rows |
| (m) | recompute + bump one ulp | 11 failures — one ulp is the whole margin (a *faithful* recompute is invisible on unit normals; the identity guards drift, not duplication) |
| (n)/(o) | `AdoptLayout`: reversed actor array / boxes off-by-one | 10 failures each, actor-identity row / box row respectively; counts survive both |
| — | release-everything (delete the `IsReleased` skip in the push walk) | 5 tests, 85 assertions; 18 are the redistribution control — **the only thing stopping "release everything" passing the collapse tests** |
| (m1) | `ShowPieceMenu`: delete the leading dismiss | 13 failures, rows stack to 9 |
| (m3) | present only on hit (`if (Rows.Num() > 0)`) | exactly the 2 miss rows |
| (m6) | clamp instead of refuse in `ChoosePieceMenuRow` | 48 failures — every out-of-range choice commits row 0 |
| (m7) | solve per piece instead of once-last in `RunPieceActions` | 6 failures, **all solve counts, no outcome moves** — invisible without `NumSolves` |
| (m8) | menu folds with `\|\|` (union) | 6 failures, all the `{live, RELEASED}` rows |
| (q) | force `Hit.Ref.PieceIndex = 0` on any hit | 6 failures in `World.Inspect` — the wall-right-refs-wrong defect only a per-brick trace sees |
| (r) | game mode builds but skips `SolveAndPush` | 10 failures — an unsolved wall and free fall are the same answer |
| — | commit door: `Ref.PieceIndex` instead of `ResolvePiece` | 43 failures in `CommitRefFailsClosed`; menu door variant: 6 in `MenuOffersWhatCanRun` — the two structure-id rows are the only watch on resolve-not-trust |
| — | `CommitPieceAction`: no-op the `Orphan->Destroy()` | exactly 1 assertion — nothing else can see an orphaned collider |
| — | one-cell thrust gate, withhold everywhere (spell `if (PieceIndex > INT32_MIN) { return false; }` at the top of `HasArchingAbutment` — a bare return trips C4702, re-confirmed 2026-08-14) | **RE-MEASURED AT THE FLIP (2026-08-14)**, filter `DestructionGame.Core.Structure` (51 tests): **5 failing tests / 41 error lines against a 1-test / 1-line baseline (CorbelStepsBeforeTensionWins, the standing red)** — the bite moved to the arched-identity family exactly as predicted: `AnArchNeedsMasonryOverIt`, `AnArchThrustsAndTheSpringingMustCarryIt`, `AHoleWiderThanOneBrickIsSpanned`, `AMissingBrickIsBridgedNotCantilevered`. The characteristic-era signature (lime one-cell row ~1.29) is dead, and **`AOneCellArchMustEarnItsThrust` does NOT fire at all** — the anti-over-withhold outcome bite is gone at the mean basis, which is why the earned-seat arched-reading replacement pin (CURRENT_STATE) is owed rather than optional |
| — | one-cell thrust gate, grant everywhere (restore the pre-gate `HasArchingAbutment` / force it true) | same test, dry-stone row: springings read 0.00473 instead of over-capacity, both bricks Supported instead of Falling — failing lines `StructureOneCellThrustTest.cpp:600/683/703`. Also `Acceptance.Beam.Catalogue` 5→8 assertion failures: all six `DropsToday`/`PassesToday` pins fire (expected 3 got 0 / expected 1 got 0), the STANDS violations flip to pass, case 1 gains "broken beam takes itself down" |
| — | `ReseatSpannedGroups` early-return (spell `!HasCompleteGeometry() \|\| Pieces.Num() > INT32_MIN` — bare return trips C4702) | coarse (removes a routing mechanism). **State the counting method with any figure** — the recorded numbers used different methods: 36 (2026-08-11), 38 (2026-08-12, error-line count), and 2026-08-13 under filter `DestructionGame.Acceptance.Wall`: **51 error lines / 5 failing tests vs a 14-line / 4-test baseline = 37 mutation-caused lines**. **THE COUNTS ARE CHARACTERISTIC-ERA and stale since the 2026-08-14 flip** (the baseline moved — case 21 is now an inverted red with different assertions, several readings changed axis); re-measure before quoting, per this row's own counting-method rule. The sharp half is expected to survive (case 8 dropping exactly {c4/5, c4/6} is strength-independent re-seating) but is likewise unmeasured post-flip |
| — | case-10 pin flip (`DropsToday` 12 → 13) | exactly 4 Catalogue errors, the extra one naming the pin — the recorded proof the inverted-red pins bite (the failing STANDS assertion does not absorb them) |
| — | survivor regions widened to swallow one fallen brick each (10 → `{0,4,-1.0,9.5}`, 19 → `{0,9,4.25,13.0}`) | exactly 2 — one per row, naming the swallowed brick ({c4/9}, {c1/4.5}); proves the regions bite and are non-vacuous (46 and 74 named survivors) |
| — | case-21 survivor region widened (`{0,3,-1.0,1.75}` → `{0,4,…}`) | **VACUOUS SINCE THE 2026-08-14 FLIP** — production drops nothing on case 21, so the survivor region cannot fire; the regression watch on the inverted red is the `DropsToday = 0` pin (bite-prover: the case-10 pin-flip pattern, re-recorded below at the flip). The characteristic-era signature (exactly 1, naming {c4/1, c4/0}) is the record of the collapse era |
| — | case-21 `DropsToday` pin flip (0 → 1, the case-10 pattern re-run at the mean basis) | exactly 1 extra Catalogue error naming the pin — the recorded proof the inverted red's zero-pins bite post-flip (measured 2026-08-14; counting method: error lines added to the Catalogue baseline) |
| — | `ThrustUu` scaled to zero (production) | **HALF-STALE SINCE THE 2026-08-14 FLIP**: the case-21 arm is dead (case 21 stands with passes 0 unmutated, so the mutation is invisible there); case 22's arm — untouched at 254 — is the surviving bite and is unmeasured in isolation post-flip. Re-measure when the one-cell gate is next touched |
| — | flush odd course weight: two half bats are 20.5 cm of brick, not 21.5 — an odd course weighs **214/215** of an even one | bit any hand derivation of wall weights (a closed-form λ bound came out 0.27% low and the LP "violated" it); check the course parity before multiplying bricks × weight |
| (M1) | LP oracle: delete the moment-balance rows | 37 failures across 6 tests (re-measured 2026-08-12; the fast sweeps and the twin row now watch it too) |
| (M2) | LP oracle: delete the friction rows | 8 (re-measured 2026-08-12; beam/corbel-B windows joined the original two sliding rows) |
| (M3) | LP oracle: unlimited tension | 13 (re-measured 2026-08-12) — mortared λ* ~3.3× high; dry rows stay green *correctly* (associative friction at c = 0 already implies no-tension) |
| (M4) | LP oracle: conversion open-coded as 100 | fast 36 / slow sweep 8 (re-measured 2026-08-12, superseding the reconstructed ~15 doubt) — every strength-governed λ* exactly 100× low |
| (M5) | LP oracle: delete the λ-cap row | exactly 1 — the unbreakable row (phase-2 unbounded → fail closed); unchanged under the sparse solver |
| (M6) | LP oracle: gut input validation | 63 — 21 poison rows × 3 assertions; catalogue untouched; unchanged under the sparse solver |
| (M7) | LP oracle: bridge ignores the latch | exactly 1 — the after-removal jamming row; unchanged under the sparse solver |
| (S2) | ~~`BreakOverturnedBodies` early-return~~ **RE-AIM to `BreakByEquilibrium` early-return** (`if (Pass > INT32_MIN) { return false; }` — a bare `return false;` trips C4702). The guard was renamed/replaced by the equilibrium gate 2026-08-27, but the leaning stacks fell identically, so the same mutation still bites | exactly the Sweep.LeaningStack test, 4 assertions incl. both production drop-count pins (29/39 → 0) — re-verify the counts under the gate before trusting them |
| (S3) | oracle verification tolerance 1e-6 → 1e-3 | **zero failures** — when the dense solver existed its envelope refusals were decisive at 1e-3 too; under the sparse solver every answer clears 1e-6 |
| (S3b) | ~~verification off (tolerance 1e30)~~ | **RETIRED 2026-08-12** with the canary's promotion — its "exactly 1" signature (the canary catching an uncertified λ* 257.24) can no longer occur; the verification gate's bite is now proven by S6-family mutations below |
| (S4) | test-side: flip the one-cell disagreement classification | exactly 1 — the pinned-relation row |
| (S6) | sparse solver: delete the iterative-refinement pass at refactorisation | 2 — the two λ* = 0 fixtures refuse via "the optimal basis failed verification" |
| (S5+S6) | pivot-out first-past-tolerance AND refinement deleted | 3 — all three λ* = 0 fixtures refuse via verification; the gate's bite-prover (S5 alone measures 0 — refinement absorbs it) |
| (S8) | test-side: perturb one half-area twin's area ×(1+1e-9) | exactly 1 — the cross-row SAME-NUMBER equality; the old 1e-6 closed-form window passes this mutation silently, which is why the cross-row check exists. **RE-MEASURED 2026-08-14 after the flip loosened the equality to ≤ 2 ulp** (the two pivot paths land 1 ulp apart at the mean data, 8.6877701307335329 vs …311, where the characteristic data landed them bit-identical): still exactly 1 — the 2-ulp check is ~5×10⁵ tighter than this mutation's 1e-9 shift |
| (P1) | pricing entering rule → Bland's (first negative reduced cost — the cheap-scan-bad-choice anti-pattern a naive candidate list falls into) | PricingCost's two COST assertions only (wall pivots 1,942 → 85,550 against the pre-partial-pricing Dantzig baseline — today's is 2,016; scans 24,588,835; 4.716 s → 576 s) while λ* stays bit-identical and all 20 hand answers + 10 sweep fixtures hold — simultaneously the strongest optimum-invariance evidence and the recorded warning: rank the queue by most-negative (or weight), never first-past-the-post |
| — | **M1–M4 counts predate PricingCost** — its correctness assertions (the stack closed form, the wall crush attainment) fire under several M-family mutations, so e.g. M4's "fast 37" is at least 39 now; re-measure before quoting an M-count as exact |
| (P2) | pricer refill loop → single window (empty window ⇒ "optimal" — the queue-is-the-answer bug; spell as `while (Scanned == 0)`, the body's `break` makes an `if` fail to compile) | **8 failures / 4 tests**: λ* collapses to ~1e-23 on the closed-form and window rows — while BOTH cost budgets pass more comfortably than ever (0 pivots, 343,653 scans). The recorded lesson: a cost assertion cannot see early stopping; only derived answers can |
| (M4″) | M4 against the classified sweep (2026-08-12) | slow sweep **26** (19 λ windows + 7 relation flips: corbel C/D, wall-09/10/12/19/20 fall below 1.0 — reconstructs exactly from λ*/100); fast **37** (16 sweep + 21 oracle test; the registry's earlier 36 was one low — correction, not caused by the slice) |
| (N1) | test-side: flip a slow row's pinned relation | exactly 1 — the pinned-relation line |
| (N2) | test-side: perturb the free-end 7×20 fixture's density ×(1+1e-9) | exactly 1 — the SAME-NUMBER λ identity (relative 1e-9); both λ windows and the reading ratio pass silently — the S8 lesson generalised |
| (N3) | test-side: the 7×20 row builds 10 courses (the pair quietly becomes one fixture) | 4 — relation, drop pin, reading ratio (reads 1.0), crossing; the λ identity PASSES, which is why a same-number pin always needs a stay-apart pin beside it |
| (N4) | production utilisation ×(1 + 1e-6 × NumPieces()) — any change making a reading depend on masonry beside the opening | **exactly 5, within the sweep file's two Opening tests** (a production mutation — run suite-wide it would also fire tight production pins elsewhere, e.g. wall-09's 1e-9 knife-edge): the three jamb-blindness ratio pins, the depth reading pin, and the matched-span reading identity — while every λ window, ladder ratio, block count and drop count stays green; the production-invariance pins' bite-prover. It provably does NOT fire the probe test's three invariance ratios (identical piece counts cancel the factor) — those have no recorded bite-prover; see CURRENT_STATE |
| (N5) | test-side: probe cohesion selector neutered (`CentreZCm < 0.0 * …`) | 4 — joint-count pin (36 wanted, 0 rewritten), relation, λ window, ratio; λ* returns **bit-identically** to the control's 5.5110095421575718 — the proof the override is the only difference between probe and control |
| (N6) | test-side: matched-span rung flip (17,s=2 secretly built as case 21) | 4 — λ window, drop count (43→45), block count (100→83), and the matched-span ratio reading *exactly the 1.00 the span hypothesis predicts*; production's matched-span identity pin PASSES (the two fixtures read identically on purpose) — the LP-side pins carry this finding, the production one only corroborates |
| (N7) | test-side: tension factor neutered (×1.0 instead of ×0.5/×2) | 4 — both λ windows, both ratios, while the 43-joint count pin PASSES (it still rewrote 43 joints, with the same value) — **a count pin catches a selector that matched nothing and cannot catch a knob that turns nothing**; ratios must sit beside counts |
| (N8) | test-side: the PROMOTED abutment j=4 rung built secretly as j=3 (the N6 rung-flip pattern on the rung the 2026-08-13 solver repair unlocked) | **4 in `OpeningMechanismLadders`** — λ window (7.379 against [9.35854, 9.35892]), drop count (49 against 53), block count (95 against 107) and the new 2× ratio pin (1.339 against [1.69805, 1.69833]). Production's j=4/j=2 blindness ratio PASSES, exactly as under N6: production reads the two jambs identically on purpose, so the LP-side pins carry this finding |
| (N9) | test-side: `PhaseTwoMustNotRefuseABoundedProblem`'s 107-block rung built secretly as its own 5-cell-jamb neighbour | **3** — block pin (119 against 107), joint pin (223 against 193) and the BRACKET (λ* 11.115 is not strictly below its own high neighbour, 11.115). The bracket half of that test is green-on-arrival — its MUST-ANSWER half was red on arrival and needs no mutation — and this is what proves the bracket bites. It does NOT touch the 99-block rung's certified window; N10 is that pin's prover |
| (N10) | test-side: the same test's 99-block rung built secretly as its 10-cell neighbour | **4** — block pin (104 against 99), joint pin (226 against 216), the new two-certified-readings window (29.567 against [37.9317143, 37.9332316]) and the bracket. Recorded because a window pinned on the fixture a test exists to solve needs its own prover, and N9 flips the other rung |
| ~~(G1)~~ | **RETIRED 2026-08-27** — the mutation and its target test (`TheOverturningGuardRestoresAtTheProfilesMeanBond`, in the deleted `OverturningGuardBasisTest.cpp`) are both gone with the guard. Nothing to re-aim: the equilibrium gate has no bond-bracket to flip a rung across | — |
| ~~(G2)~~ | **RETIRED 2026-08-27** — `SolverInterimOverturningMeanBondMPa` is deleted (Slice 2), so there is no constant to revert and no discriminator test to fire. The finding it recorded (the guard's 16.7% capacity change was invisible to every other fixture) is preserved in git history; the LP gate replaces the guard and fells the leaning stacks identically | — |
| (W1) | test-side: case 22's `PreCascadeWorstToday` flipped 2.9133264370386338 → 2.9 (the case-10 pin-flip pattern on the new margin pin) | **exactly 1 extra `Acceptance.Wall.Catalogue` error** naming the margin — the "must be past 1.0" row beside it correctly stays green, which is the point of splitting the two |
| (W2) | test-side: the stack-bond red's hanging-brick count off by one (`Heights - CutCourse` instead of `Heights - (CutCourse + 1)`) | **exactly 2 extra errors, one per height**, inside an already-red test — the recorded proof that a characterisation pin inside a deliberate red is not free-riding on the red |
| (W3) | test-side: the span pair's case-9 axis identity written against the COMPRESSION axis (`2·σ_n/f_c` — literally what DESIGN §6 and CURRENT_STATE said before 2026-08-14) | **exactly 1 error, and `SpanIsReadInTheJointNotInTheOutcome` goes green → FAIL**: 0.020307210426829269 against production's 0.26329211195559277. The recorded proof that naming the axis is a real assertion and not a restatement of the reading |
| (W4) | test-side: the span pair's WIDE half built as case 7 (the N6 rung-flip pattern) | **6 in one test** — joint identity, magnitude pin, axis identity, the thrust row (0 uu both sides), the reaction row (equal) and the reaction-ratio pin (reads 1.0 against 1.9046). Every assertion added when the span-law floor was retired bites |
| (S9) | `RigidBlockOracle.cpp`: the ratio test's `PivotFloor` reverted to the absolute `PivotTol` (i.e. the 2026-08-13 fix backed out, `RelativePivotTol` left unused) | **2 assertions / 1 test** — `PhaseTwoMustNotRefuseABoundedProblem`'s two MUST-ANSWER rows, reproducing the pre-fix red EXACTLY: 836 pivots on the 99-block rung, 671 on the 107-block one, both "phase-2 simplex failed", all four neighbours untouched. The fix's own bite-prover, and the cheapest way to re-confirm the defect is what it is said to be |
| (S9b) | `RigidBlockOracle.cpp`: `RelativePivotTol` reverted 1e-9 → 1e-11 (slice 0a's fix backed out; the S9 pattern re-run against the constant that moved) | **2 assertions / 1 test** — `PhaseTwoMustNotRefuseTheCoveredOpeningFamily`'s two MUST-ANSWER rows, reproducing the 2026-08-15 red exactly (17,316 pivots on the 128-block rung, 6,244 on the 200-block one, both "phase-2 simplex failed: the basis went singular"), while `PhaseTwoMustNotRefuseABoundedProblem` stays GREEN at 1e-11 — which is the evidence that the new value EXTENDS the 2026-08-13 fix rather than replacing the fixture evidence for it |
| (N11) | test-side: the covered-opening test's 8-cell (128-block) rung built secretly as its 9-cell neighbour (the N10 rung-flip pattern on the first new refuser) | **4** — block pin (137 against 128), joint pin (328 against 306), the certified window (127.726 against [155.6164, 155.6476]) and the BRACKET (127.726 is not strictly above its own 9-cell neighbour, 127.726). The window is pinned on a fixture the test exists to make answerable, so it needs its own prover |
| (N12) | test-side: the same test's 16-cell (200-block) rung built secretly as its 14-cell neighbour | **3** — block pin (182 against 200), joint pin (438 against 482) and the certified window (55.975 against [43.1284, 43.1371]). Three and not four because that rung deliberately carries no bracket — its neighbours cost 166 s and 255 s, and the bracket's argument is bought once by the 8-cell trio |
| (X1) | test-side: slice 0b's sandwich ladder deletes from course 5 instead of course 6 (the N6 rung-flip pattern on the regional measurement) | **7 assertions / 1 test** in `OracleSweepFast.RigidBlock.RegionalSandwich` — the global pivot pin (873 against 965), the r=4 FREE-boundary verdict (feasible against infeasible, because a radius-4 region round a course-5 brick reaches the foundation), the r=5 block count (147 against 142), the closing radius (4 against 5), the closing region's block count, and both closing pivot pins. The "the sandwich costs more than the global solve" row correctly stays GREEN (1,149 > 873), which is the evidence that row states a relation rather than echoing the numbers |
| (X2) | test-side: slice 0b's gate fixture built as 8x11 instead of 8x10 (the same pattern on the cost measurement) | **5 assertions** in `OracleSweepFull.RigidBlock.FeasibilityReformulationCost` — block count (92 against 84), joint count (228 against 207), the λ\* window (1128.1710079112304 against [1128.4217, 1128.467]), the LIVE pivot pin (3,804 against 2,606) and the DEAD pivot pin. **Baseline note, and re-measure before quoting**: taken while that test carried the early-exit seam as a deliberate red, so it was recorded as "5 EXTRA over a 1-error baseline". The seam went green in the same slice, so **the baseline is 0 today and a re-run reproduces 5 total, not 6** — the W2 pattern (a characterisation pin living inside a deliberate red) no longer applies to it |
| (X3) | production: `RigidBlockOracle.cpp`'s `Result.PhaseOnePivots = Iterations` replaced by the constant `0` (slice 0b's early-exit seam, the "one field to a constant" pattern) | **1 assertion / 1 test** — `FeasibilityReformulationCost`'s early-exit row, reading "the solver must report how many pivots phase 1 spent (got 0) and at which pivot its infeasibility sum first reached tolerance (got 487)". Nothing else in either group reads the two fields (grep-verified), so the signature is scoped to that test by construction |
| (X4) | production: the phase-1 pivot loop's feasibility watch disabled (`bWatchArtificialFeasibility` never consulted), so `PivotsToFirstFeasible` stays INDEX_NONE | **1 assertion / 1 test** — the same row, "(got 490) … (got -1)". Half of the slice's original red signature, which is why BOTH fields get their own mutation: either one alone leaves an assertion that reads plausible. **BOTH SIGNATURES ARE SUPERSEDED AND WILL NOW FIRE WIDER — re-measure before quoting.** They were taken while the early-exit assertion checked population and ordering only; since 2026-08-15 every row pins both fields by VALUE and the pooled totals are pinned too, so each mutation reaches the eight per-row pins and the pooled pins as well as the one row named here. The gap those two entries recorded — "a mutation reporting a wrong-but-in-range pivot index passes" — is what **X5** closes |
| (X5) | production: the phase-1 feasibility watch records UNCONDITIONALLY at the first pivot (`if (true)` in place of the infeasibility-sum test in `RunRevisedSimplex`), so every fixture reports `PivotsToFirstFeasible = 1` | **10 assertions / 1 test** in `FeasibilityReformulationCost` — the eight per-row first-feasible value pins (21→1, −1→1, 487→1, 1021→1, 449→1, 799→1, 866→1, 5381→1), the pooled skipped-pivot pin (58 → **9,237**) and the seam re-solve's own 487 pin. **The eight ORDERING assertions beside them correctly stay GREEN** (1 lies inside [0, phase-1] on every fixture), and every phase-1 pin, every λ\*, every pivot count and both live-pose 0/0 pins stay green: this is the exact "wrong but in range" mutation X3/X4 could not catch, and under it the file's headline conclusion inverts from **0.63% to 99.91%**. The recorded proof that the early-exit refutation is defended by an assertion rather than by a printed line |
| (X6) | test-side: `SpikeExtractRegion` keeps the out-of-region blocks instead of dropping them (weight and restraint both retained — the crudest form of §12 D2''s repair direction) | **14 assertions / 1 test** in `RegionalSandwich` — r=2/r=4 block counts (149 against 44 and 114) and their FREE-boundary verdicts (feasible against infeasible), the r=5 block count, the closing radius (2 against 5) with its region size and both pivot pins, the agreement count (**10 of ten** against 5), and **all four of PART D's pins**: the band's size (30 against 6), the free-boundary FEASIBLE row, the grounded-boundary row, and the band's λ\* (**0.44049 against [18.48088, 18.48163]** — the band has become the whole stack). PART D's third row, "the WHOLE stack must be INFEASIBLE", correctly stays green because it does not go through the extractor. The prover for the row that invalidated PROMOTION_DESIGN §5.3, per §9.5's rule that such a row may not rest on an argued bite. **RE-MEASURED 2026-08-15 against the repaired extractor and the count here is unchanged at 14**, plus **12 more in `RepairedRegionalSandwich`** (five strip block counts, the closing region's size and both its pivot pins, the cost-ratio row, the under-half-the-wall row, the tall wall's region and the stack's closing band). PART D's two NEW repaired rows correctly do NOT fire — keeping every block makes the repaired band the whole stack, which is infeasible, which is what those rows demand. **RE-MEASURED AGAIN 2026-08-16 against `SubUnityWallCertificate`: 26 more there**, and the ones that matter are **all five IDENTITY rows** (the certified region's block count, joint count, both pivot counts and its free-boundary verdict) plus the certified strip's own λ\* window (621.957 → 0.44049, the region having become the whole structure). That test's mechanism claim is the identity, and this is its prover — X11 leaves every identity row green |
| (X7) | production: `SolveRigidBlock` refuses with `EOracleRefusal::PhaseOneFailure` on any 44-block problem with no grounded block — i.e. exactly the sandwich ladder's r=2 FREE-boundary region, the arm that refused 128- and 200-block members one commit before | **exactly 1 assertion / 1 test** — `RegionalSandwich`'s "r=2 FREE-boundary region: the solve must ANSWER … It said: phase-1 simplex failed". **Everything else stays GREEN, and that is the finding**: the r=2 pessimistic verdict pin (expects infeasible, a refusal reads infeasible), the conservation lemma (`!bPessimistic`), the closing radius and the 5-of-10 count are all computed from a predicate that cannot tell a refusal from an infeasibility, so before the 2026-08-15 guard this mutation fired **zero** assertions and the test would have reported the refutation from a solve that never happened |
| (X8) | test-side: the repaired sandwich's surcharge applied at the INTERFACE CONTACT's X instead of the charged component's own centre of gravity (`Applied.AtXCm = Joint.CentreXCm`) — the plausible-looking way to write repair (1), and the one that keeps the force while losing the moment | **7 assertions / 2 tests** — 4 in `RepairedRegionalSandwich` and 3 in `RegionalSandwich`'s re-aimed PART D, **and it RE-CREATES THE FALSE CERTIFICATE**: the stack's band 0..5 certifies feasible=1 against a stack that reads infeasible, the closing band collapses from 0..29/30 blocks to 0..5/7, and the false-certificate counter reads 1. The sharpest single mutation in the slice: it says the load-bearing half of repair (1) is WHERE the omitted weight acts (the stack's omitted courses weigh on a line at X = 175 cm while their contact with the band sits at X = 55), not that it acts at all. A surcharge that keeps only the resultant force is no repair. **Re-measured 2026-08-15 at 7 (was 6)** when review's LINE-OF-ACTION pin landed: "independently derived here as 175 cm … and it acted at 55 cm". That pin exists because the verdict rows are an EXTREME-case defence only — a wrong-but-nearer X, say the region block's own centroid, might flip no verdict and pass in silence |
| (X9) | test-side: the repaired sandwich's carried set computed as EMPTY (`Rule == Carried` skips every component; repair (1) removed with its plumbing left in place) | **13 assertions / 2 tests** — 8 in `RepairedRegionalSandwich` (three false-certificate rows on bands 0..5/0..10/0..15, the charged-block pin 23 → 0, the interface-joint pin 1 → 0, the closing band 29 → 5 and 30 → 7 blocks, and the false-certificate count 0 → **3**) and 5 in PART D. The counterpart to X8: X9 proves the surcharge is read at all, X8 proves it is read in the right place |
| (X10) | test-side: the repaired sandwich's `Carried` rule made to charge EVERY component whether or not it reaches ground (the rule SELECTOR inverted, not the rule's arithmetic) | **7 assertions / 1 test** in `RepairedRegionalSandwich` — five of the six wall-arm `Charged == 0` pins (0 → 108, 84, 60, 36, 12 as the strip widens and the omitted set shrinks; the w = 5 rung correctly stays 0 because nothing is omitted) plus BOTH pessimistic pivot pins (262 → 514, and the tall arm's 474 → 761, the extra surcharge rows costing pivots). **No verdict moves**, which re-states from a second direction the slice's P8 miss: the wall arm has so much margin it cannot discriminate surcharge rules, so the `Charged` counts and the pivot counts are the ONLY things that can notice one changing. Written because review found those six pins had no prover — X6/X8/X9 all leave them green |
| (X11) | test-side: the sub-1.0 wall's chimney built with NO LEAN (`SpikeChimneyLeanCm` 10.0 → 0.0), so the same thirty blocks and thirty joints stand as a vertical column and the structure STANDS (λ\* 0.44049 → **275.414**) | **14 assertions / 1 test** in `OracleSweepFast.RigidBlock.SubUnityWallCertificate` — the composite-is-infeasible row, its λ\* window, its global pivot pin (1,308 → 1,114), the w = 4 and w = 5 rungs' block / charged / free-boundary pins (six), **the FALSE-CERTIFICATE COUNT (4 → 0)**, the charged-but-certifying row, and all three contrast pins. **The w = 0..3 rungs and every IDENTITY row correctly stay GREEN** — the certified region is the same 41 blocks either way, which is what the identity exists to say — so this mutation is scoped to the fixture's *falling-ness* and X6 above is what proves the identity. It is also what found a silent-skip hole: with nothing certifying falsely, the identity and the "how false" λ\* were skipped without a word, and a loud `AddError` on "no strip closed" now exists because of it. Counting method: error lines added to a zero baseline, that test alone. **Note (2026-08-18)**: it does NOT touch the bypassed-joint hand-statics pins added that day — those are arithmetic on fixture constants and are blind to the lean, which is why X13 exists |
| (X12) | production: `RigidBlockOracle.cpp`'s WARM-START PRIMAL-INFEASIBILITY REPAIR skipped — the sign-flip loop in `SeedWarmStartBasis` gated off (`&& Form.NumRows < INT32_MIN`), so a warm basis's genuinely negative basic values reach `Refactorise`'s clamp and are laundered into a feasible-looking point, which is precisely the hazard the seam's header names | **THREE OF THE FOUR ROWS IN `OracleSweepFast.RigidBlock.WarmStartAfterADeletion` STOP ANSWERING**: one brick out of course 3 and the whole top course refuse with *"the optimal basis failed verification: against row 15"* / *"row 1"*, and the 12x12 refuses with *"phase-1 simplex failed"* (the ratio test's no-positive-entry arm, on a phase 1 that cannot be unbounded). **COUNT THE BEHAVIOUR, NOT THE LINES** — the mutated run had FEWER error lines than the control (6 against 9) because a row that stops answering returns early and skips the assertions under it. *(That control was 9 because the warm pins were still UNMEASURED sentinels; since they were pinned on 2026-08-18 the control is **0**, so a re-run reads its own number and "fewer than the control" no longer holds. Count the three rows that stop answering, which is what the row means.)* **THE PREDICTED ROW IS NOT THE ONE THAT FIRES**: the slice predicted the ground cut (the λ = 0 falls arm) would flip to stands, and measured it still answers and still reads falls — **5 of its 2,446 hints failed to survive seeding, against 28–462 on the three standing rows**. *(Said as "5 flips" until 2026-08-18, which overstates it: `hints − accepted` counts a SIGN FLIP and a SINGULARITY REPAIR alike and no counter distinguishes them, so the measured quantity is hints changed, not flips. Distinguishing them needs a second counter on `FOracleResult` and nothing needs it yet.)* What the mutation therefore proves is sharper than what it was written for: **the λ bit-identity assertions are defended by the post-solve VERIFICATION GATE, not by the repair.** With the repair gone the solver never returns a wrong λ; it refuses. The repair is load-bearing for ANSWERING. wall-01 is unchanged by it (it refuses at pivot 64 with or without, and reaches the fail-closed cold retry either way) |
| (X13) | test-side: the sub-1.0 wall's chimney built ONE COURSE SHORT (`SpikeChimneyCourses` 30 → 29) — the N6 rung-flip pattern on the fixture's SIZE rather than on its falling-ness, which is what X11 already covers | **9 assertions / 1 test** in `OracleSweepFast.RigidBlock.SubUnityWallCertificate` — the composite block pin (179 → 178), the composite joint pin (415 → 414), the composite λ\* window (0.44049 → **0.47322**, the shorter chain being less eccentric), the composite global pivot pin (1,308 → 1,161), the w = 3/4/5 `Charged` pins (30/28/26 → 29/27/25), the CONTRAST strip's charged pin (27 → 26) and **the bypassed-joint margin window** (1.5438 → 1.6588, W and the lever both moving). Written because review found the composite SIZE pins had no prover at all. **What correctly stays GREEN is the finding**: the plain wall's 149/385 pins and its FEASIBLE control (no chimney to shorten), the FALSE-CERTIFICATE COUNT of 4, every IDENTITY row and the certified strip's own λ\* — the strip is the same 41 blocks either way, which is the identity's whole point. So the plain-wall control still has no prover, and it cannot have one from this direction; say so rather than implying X13 covers it. Counting method: error lines added to a zero baseline, that test alone, excluding the `Result={Fail}` line |
| (X14) | production: `RigidBlockOracle.cpp`'s `Form.InitialBasis = Seed;` DELETED from `SeedWarmStartBasis` — the seeded basis is computed, validated, repaired, sign-flipped and then **never installed**, while `WarmStartColumnsAccepted` is still counted from `Seed` one statement later. The "a field reports work that did not happen" pattern | **16 assertions / 2 tests** — 11 in `WarmStartAfterADeletion` (PART A's two idempotence pins, 0 → **48** pivots and 0 → 47 in phase 1; PART C's live-seam warm pivots 458 → 186; and all eight of PART B's warm pivot / phase-1 pins) and 5 in `WarmStartAtWallScale` (`Accepted == 0` → **12,459**, the wasted-work pin 64 → **1,702**, the `WarmAccepted` pin, and both warm pivot pins). **EVERY `WarmAccepted` PIN ON THE FOUR FAST ROWS STAYS GREEN — 2600 / 2032 / 2441 / 4560, and PART A's "every column is taken" with them.** That is the defect this mutation exists to expose stated as a measurement: the acceptance count is derived from `Seed` *independently* of the assignment, so on its own it will happily report a rich warm start that the solver never took. What defends the file is PART A's `pivots == 0` and the warm pivot pins, and nothing else. Note it is not a clean cold-start equivalent — the appended negated columns are still added to the matrix, so the mutated warm counts (350 / 354 / 91 / 1,164) sit near but not on the cold baselines |
| — | `RigidBlockOracle.cpp`: the no-entry seam repair (refactorise-retry + set-aside) REMOVED, floor left in | **zero failures** — and that is why the repair is not in the tree. Recorded as the measurement that kept it out: with the pivot floor in place both refusers answer with bit-identical λ* and identical pivot counts whether the seam is repaired or left to refuse, so no failing test drove it. The arm is unreached, not sound (CURRENT_STATE) |
| (R1) | production: `SolveMinViolationReadout`'s per-group minimax REDUCTION reverted from a fixed point to a single pass (`Candidates = StillAtLevel; break;`) — a flank contact sitting incidentally at t\* stays falsely pinned critical because rebalancing it to its partner contact is net-zero in one min-Σ | reds **`Readout.DegenerateIntraGroupTradeIsPermutationStable`** (the 2-member `Degen` pair): flanks read 103333/92666 instead of the even 88000/108000, per-joint **violation drift 3.067e4** across permutations, worst base joint flips 1↔2 with column order; central stays correct (N 294000, viol 104000 = t\*). The fixed-point loop's own bite-prover (commit 18911a7). **Does NOT bite the 3-flank `TriDegen`** — that topology collapses in a single reduction, which is why R2 exists. `OracleSweepFull` untouched (readout is flag-gated off the maximise-λ path) |
| (R2) | production: `SolveMinViolationReadout`'s per-group LEVEL RECURSION stopped after the top level, the reducible flank family read back through a bare `min Σ s_k` instead of being evened by its own level | reds **`Readout.ThreeWayReducibleTradeIsPermutationStable`** (the 3-member `TriDegen` trio, distinct caps): **23 assertions** — the mid flank absorbs the ENTIRE 116000 subtotal while low/high starve to their caps (violations 0/116000/0 vs the even 38666.7 each), **violation drift 1.160e5** (= the whole subtotal sloshing) across 8 seeds, worst joint flips 1↔2; central stays correct. Guards the level-recursion even-split on a ≥3-member reducible family — new ground R1/Degen (a 2-member pair) do not cover |

**A mutation whose early `return` precedes code trips C4702 and the BUILD FAILS while the old DLL keeps running** — the run then reproduces the *previous* mutation's signature verbatim. Verify `Result: Succeeded` on every mutation build before believing its run; this has now bitten twice (the M6-stale incident, and once during the sparse rewrite). **The multi-agent variant bit on 2026-08-12**: with two agents in one tree, a `Target is up to date` build can mean *the other agent's* build already relinked YOUR half-edited source — a measurement taken then is plausible and wrong. Before believing any measured number, check the build log for `Compile [x64] <your file>` in a build you ran after your last edit.

Degenerate-fixture note: a row meaning to hit the harness bounds bug must name the **ungrounded** piece first (`{1, INDEX_NONE}`) — `||` short-circuits on a grounded piece 0 and the second handle is never evaluated.

**Running-bond opening families alternate BY COURSE PARITY — and the dominant effect is the reveal SPAN, not the toothing count** — an opening cut at courses s..s+2 is a *different fixture* at odd and even s: 52 vs 53 bricks cut, and, decisively, the cover bears on a clear reveal a full cell different (383.50 vs 406.00 cm on the case-21 family — span² alone predicts 0.892 of the measured 0.873 step on λ*, and production's ratio sits 0.93% from span²). A ladder swept over courses is two interleaved fixtures; read ratios at fixed parity, and don't attribute the step to "toothing" — the matched-span 2×2 (measured 2026-08-13, `OpeningMechanismLadders`) settled the split: with spans matched across opposite parities the step collapses to 0.9897, so the reveal span carries roughly **eleven twelfths** of the 13%, and the 0.081 residue is an *upper bound* on the cover's bond parity (the matched pair also differs by a course of height and a cell of width). Pure span at fixed parity measures λ* ∝ L^−2.39; the parity step independently reads −2.38 — agreement to two figures.

**A λ* window and a ratio pin cannot catch a rung secretly building the wrong wall** — the 2026-08-13 rung-flip mutation showed both passing while s=3 was secretly s=1 (λ 1e-6 apart, ratio exactly the 1.0 the pin wants). A **block-count pin per rung** is the only thing that caught it; window + ratio + size pins work as a set, never alone.

**"Unable to build while Live Coding is active"**: an editor open on the *sibling* project (`DestructionGame 5.8`) holds a Live Coding mutex keyed on the shared engine exe, blocking builds here too. `-NoHotReloadFromIDE` on the build is the safe workaround (the two projects link different DLL paths) — a companion to the close-the-editor rule.

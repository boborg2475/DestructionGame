# Current State

**Living document.** This is the running TODO list for the project, not a historical record.

- **Finishing something? Remove it from this file** as part of that work. A completed item left here is worse than no list.
- **Deferring something? Add it here** before moving on, with enough context to act on it later.
- Check this file at the start of a work session and again before calling anything done.
- Items are deliberately unnumbered where possible — they churn constantly.

**This file assumes you already know the design.** [INDEX.md](INDEX.md) says which document owns what and where to write a new fact; [DESIGN.md](DESIGN.md) is the authority on the model, the constants, the anchors, the evolution path and the dated rulings; [TRAPS.md](TRAPS.md) holds the live footguns and the mutation registry; [PROMOTION_DESIGN.md](PROMOTION_DESIGN.md) owns evolution step 4; [SHED_PATH.md](SHED_PATH.md) sequences the north-star brick-shed /goal (the layer above step 4 — the phased route through wood, tension, member failure and 3D to a playtest-verified shed, with the standing rulings); [LEVELS.md](LEVELS.md) indexes the thirty-one playable levels. Settled reasoning lives there and in git -- not here.

Last updated: **2026-08-16**. **What landed recently lives in its own home, not in this file** -- rulings in DESIGN §8, step-4 measurements in PROMOTION_DESIGN, footguns and mutation signatures in TRAPS, and the numbers themselves in the test headers that pin them. This file is the list of what is NOT done; if you want the history, read git.

**Where step 4 stands, because it is the active work:** slices 0a and 0b are done and the repaired regional sandwich is measured (PROMOTION_DESIGN §5.3, §12 D2/D2'/D2"). Regions CAN be small -- 27.5% of a wall at 0.322x a global solve -- so the size question that gated the step is answered. Whether a closed sandwich is TRUSTWORTHY was the open half, and **it was answered on 2026-08-16 and the answer is NO** -- the sub-1.0 wall fixture exists (`OracleSweepFast.RigidBlock.SubUnityWallCertificate`), it issues four false certificates, and the pessimistic side is a heuristic rather than a bound (PROMOTION_DESIGN §5.3's box, §11 R2, §12 D2'''). **Warm starts (refuted), Slice 0c (case 21's residual is a non-brittle friction+compression mechanism, PROMOTION_DESIGN §4.4) and Slice 0d (first-crack rows wired behind `bFirstCrackRows`, measured across the sweep, §4.3) are all done and closed as of 2026-08-21.** The measurement preconditions on the promotion are now satisfied. **The block-size threshold is now MEASURED (2026-08-21, `OracleSweepFull.RigidBlock.DeletionResolveLatencyLadder`; result in DESIGN §8): promotable band ~84–104 blocks / ~360–550 pivots, 3.5×+ below wall-01 and scenario scale, so scope-by-size is viable.** A finding it produced feeds §5.6: the fail-closed deterministic budget should be a BLOCK cap, not one global pivot number (wall-clock grows ×174 vs pivots ×11.9 across the ladder). **Slices 1 and 2 are DONE (2026-08-27).** Slice 1: the oracle relocated to `Core/RigidBlock/` as all-config production code + the LU/eta factorisation fuzz. Slice 2: the equilibrium gate (`FStructure::BreakByEquilibrium`) replaced the interim overturning guard — it asks the LP feasibility question below an 84-block cap (fail-closed to the router above it), fells the two-load-path body the guard's N≥2 blind spot missed, and `BreakOverturnedBodies` + `SolverInterimOverturningMeanBondMPa` + `OverturningGuardBasisTest` are deleted. The leaning-stack family did NOT move (the LP fells them identically to the guard — the guard was well-calibrated on its home turf). **Slice 3a is DONE (2026-08-27, oracle-side only): the phase-1 dual is extracted as the collapse mechanism** (`RigidBlockOracle::ExtractMechanism` at the infeasible arm — one BTRAN, no counted quantity moved, `OracleSweepFull` bit-identical), Farkas-verified fail-closed (`yb>0`, `yA_j≤tol` on structural columns, non-empty set, else `Refuse(VerificationFailure)`), canonicalized into a stable named set (relative threshold `MechanismRelativeTol = 1e-6`), and the bridge fills `PieceOfBlock`/`ConnectionOfJoint`. Driven by `DestructionGame.Oracle.RigidBlock.Mechanism.*` (correctness vs an independent kinematic oracle, permuted-column determinism, Farkas fail-closed). **The determinism gate genuinely bit**: a 24-course dry leaning stack added to the permutation sweep showed the raw plastic multipliers are NON-unique under degeneracy (they named 8 opening joints on one column order, 13–16 on another) while the block velocity triples are unique (drift ≤1.1e-15) — so the joint set is derived from the STABLE block KINEMATICS (relative velocity at a contact), never from the multipliers. **Slice 3b + Slice 4 are DONE (2026-08-27) — the LP is now the production break authority below the block cap, and the core of step 4 is complete.** Below the cap (raised 84 → **200**, ruling D8) the strength sweep is demoted to the overlay estimator and the canonical mechanism is the sole break authority; `GetPieceSupport` is LP-authoritative (a piece the LP carries reads Supported, overriding the router's flood). This CLOSED both in-flight reds (two-load-path falls, dry stack stands) and re-pinned the whole conservative fallout to the LP's more-correct verdicts (rows 10/19/20 STAND, the beams and one-cell seat stand under plastic, the 132-block push wall holds all but its 4 over-capacity corners). Review verified no fail-open, the solver bit-identical, and RENDERED the below-cap collapses as believable standing masonry. **FIRST-CRACK PROMOTED TO AUTHORITY (2026-08-28, D1): `Problem.bFirstCrackRows = true` in `BreakByEquilibrium` below the cap** — bonded joints now crack at first crack (3× stricter than plastic). Fallout was exactly 4 tests: `Beam.Catalogue` row 1 and `TheMemberMaterialDecidesTheOutcome` went GREEN (the C24 timber glue line cracks and falls, the S275 steel holds — the material decides the outcome by the glue-line route), and `MomentBreaks` (the 17-brick hang now FALLS, first-crack λ\* 0.990 — re-inverting the Slice-4 plastic re-pin) + `BeamPair` row 1 (production drops 3) re-pinned to the falling behaviour. Every DRY (f_t=0) fixture stayed BIT-IDENTICAL (the keyed-on-data proof); nothing over-fell; `OracleSweepFull` 5/5. Suite 187 = 183 green + 4 standing design-limit reds. **What remains: the pre-RELEASE R7 Shipping-FP confirmation (owed, de-risked, §Slice-3a items); the D8 latency debt if a mid-size (84–200 block) production structure is ever built; row 21 stays a ruled-Collapse inverted red; a below-cap render of the now-falling collapse (a visual gap the harness can't reach — low risk); and the later evolution steps** — tension support (step 5), member failure (step 6 — the beam-Midspan mechanism red + the giant-corbel family), and 3D. The region repair the sub-1.0 finding implies is a design question below, not a queued slice.

**Slice 3a deferred/owed items (logged 2026-08-27):** (a) **R7 Shipping-FP — STRONGLY DE-RISKED BY ARGUMENT, empirical run owed as confirmation (orchestrator assessment 2026-08-27, open to veto).** Two facts downgrade this from a hard blocker to a recommended confirmation: (i) `DestructionGame.Build.cs` / the Target.cs carry NO fast-math or FP override, so the oracle compiles under UE's default **`/fp:precise`** in every config; on the MSVC x64 toolchain that forbids result-changing FP reordering and FMA contraction, so Development and Shipping (which differ only in /O level and LTO) produce **bit-identical** double arithmetic → the identical pivot path → the identical canonical mechanism. (ii) The Farkas verification runs in **all** configs including Shipping, so even if some FP difference DID arise (e.g. under a clang toolchain that contracts FMA), Shipping can never name the WRONG bricks — it would at worst produce a different-but-still-Farkas-valid set or fail-closed refuse to the router. So R7's residual stake is *test-validity consistency* (does Shipping collapse a wall the same way the tests validate), NOT correctness. **Recommendation: the empirical Shipping run (a small non-test entry point — a console command or commandlet, gated behind a command-line flag, that solves the mechanism fixtures and logs a hash of the named set — built Shipping, compared to Development) should be the FIRST task before 3b wires any wall, to confirm (i) on the actual toolchain; but it is no longer a correctness gate given (ii).** Do not skip it silently — run it or have the user waive it. **UPDATE 2026-08-27: the cheap empirical route is BLOCKED** — the Test configuration (Shipping-grade FP that keeps `WITH_DEV_AUTOMATION_TESTS`) cannot be built with this binary engine distribution ("Targets cannot be built in the Test configuration with this engine distribution"), and Shipping drops the automation tests. So the empirical confirmation needs a bespoke flag-gated Shipping entry point after all. Given (i) the `/fp:precise` argument and (ii) the Farkas-in-all-configs correctness guarantee, this is downgraded to a **pre-RELEASE** confirmation, not a pre-3b blocker; 3b proceeds. Owed before shipping. (b) **The block-velocity canonicalization has NO lexicographic minimal-support tie-break** — every fixture owned has UNIQUE block velocities (permutation drift ≤1.1e-15), so the moving-BLOCK set is stable without one; the D7-named tie-break was left unbuilt because no fixture exhibits genuinely alternate block mechanisms (infeasibility breaks the symmetry that would create them), and building it speculatively has no failing test. If such a fixture is ever found (a structure whose collapse is certifiable by two different moving-block sets), the moving-set canonicalization would need the index-ordered minimal-support tie-break; the joint set is already robust (derived from kinematics). (c) **SATISFIED 2026-08-27 — the determinism-at-scale gate PASSED** (`Tests/OracleMechanismMultiModeDeterminismTest.cpp`). On a genuinely infeasible 124-block dry wide-opening wall (multi-mode: 96 moving / 228 opening), 6 seeded permutations named the BIT-IDENTICAL set on every ordering that answered, while the raw-dual VirtualUz drift across them was **3.2e-3** — the Farkas ray is genuinely non-unique yet the canonical named set holds. **Correction to the review framing:** the 170–358 Bland entries are a *phase-2* anti-cycling event on *standing* walls; the mechanism extracts on the *phase-1 infeasible* arm, which does not reach that streak, so raw-dual drift (not Bland count) is the right non-uniqueness witness there — and a *mortared* opening wall cannot be marginalized by bond (it stands by compression thrust, the case-21 mechanism), so the reachable multi-mode infeasible fixture is the dry collapse. **Robustness caveat (accepted, not a blocker):** on some column orderings the solver fail-closes (`PhaseOneFailure`) — it never names the WRONG bricks, it declines, and the router absorbs a refusal; so the verdict's *availability* is not fully permutation-robust though its *correctness* is. (d) **SATISFIED — τ = 1e-6 is not load-bearing on a knife edge:** named set identical at τ ∈ {1e-5, 1e-6, 1e-7}; separation ≥2 orders (2.9 block / 2.7 joint on the dry wall). (e) `ConnectionOfJoint` value assertion still lands when 3b consumes the joint provenance. **THE ONE REMAINING GATE BEFORE 3b is (a), the R7 Shipping-FP RUN.**

**Slice 2 deferred/open items (logged 2026-08-27):** (a) the gate's **≥2-ground-borne-bodies decline branch is untested** — fail-closed and consistent with D5/D6 coarseness, but no fixture drives two independent past-tipping bodies; add one, or accept the fail-closed decline. (b) **Latency near the cap is an open perf risk**: the gate runs one feasibility LP per cascade pass plus a second (remainder) solve whenever it finds a body — up to ~2×(passes) solves per player action at ≤84 blocks (~0.065 s each), which over several passes can exceed the D2 ≤100 ms budget; no test guards per-action wall-clock (§5.6). Slice 3's feasibility+dual reuse may absorb this; measure it there.

**Slice 3b deferred/open items (logged 2026-08-27, RELEASE-RULE WIRING LANDED):** the mechanism is now the sole break authority below the cap (`FStructure::BreakByEquilibrium`): the strength sweep is demoted to `BreakByCapacitySweep` and runs only when the gate declines; on Stands/Falls the gate rewrites the support arrays from the LP (`ApplyLimitAnalysisSupport`) so `GetPieceSupport` is LP-authoritative below the cap; Slice 2's whole-body flood + single-clean-body restriction + ≥2-decline + remainder second-solve are DELETED (the per-piece dual subsumes them). Driving reds greened: the dry stack stands, the support-authority seam passes, the two-load-path body still falls, and wall rows 10/19 STAND (0 fell). Open items: **(a) THE BLOCK CAP DEFAULT WAS RAISED 84 → 200** — the medium wall-catalogue fixtures (rows 10/19/20 at 125/125/174 blocks; wall-row 9 at 174) had to be IN scope for the LP to stand or fell them, and there is NO cap value that greens rows 10/19 without also pulling in the beams (~3), the 17-brick hang (~18), the one-cell dry seat, and the 132-block dry ragged wall — they all sit below 174. This **exceeds the measured ~84–104-block per-action latency band** (D6-c / §5.6): a structure of a couple hundred blocks now solves the LP synchronously. No production structure sits in 84–200 today (the default wall is ~1200 blocks → still declines to the router), so the cost is test-time for now, but the latency item (Slice-2 (b) above) is now LARGER and the 200 value wants an owner's ratification and a per-action wall-clock guard. **(b) THE 3b FALLOUT IS RE-PINNED — SLICE 4 DONE (2026-08-27).** Every moved verdict was re-measured predictions-first against §12 D8-fallout and re-pinned to the LP-correct behaviour, all confirmed a genuine admissible equilibrium (rows 10/19/20 stand, the 17-brick hang stands with a plastic-limit margin ~2.78×, the three beams and the one-cell dry seat stand, the 132-block dry ragged wall holds all but its 4 over-capacity corner corbels). `MomentBreaksTheJointItOverloads`, `Sweep.{BeamPair,OneCellDisagreement}`, `EveryLevelsCaptionTellsTheTruth`, `World.Push.AWallOverCapacityDoesNotWaitForAClick`, the beam catalogue `DropsToday`/`PassesToday`, the wall rows 10/19/20 + captions + known-red list, and the `OracleSweepFull.WallsAndLadders` production drop/strand counts for wall-10/19/20 are all green; the λ\* pins stayed bit-identical (OracleSweepFull green). Suite back to 6 standing reds. *The remaining historical note below records what moved.*  Below the cap the LP now stands many structures the router's per-joint sweep collapsed (the whole Class-A gate moved): beam rows drop 0 (were 3), row 20 drops 0 (was 9), rows 10/19 strand 0 (were 3/6) and their `DropsToday`/`StrandsToday` anchors + the `EveryLevelsCaptionTellsTheTruth` tripwire + the level captions are stale paperwork, `Oracle.RigidBlock.Sweep.{BeamPair,OneCellDisagreement}` and `Core.Structure.MomentBreaksTheJointItOverloads` and `World.Push.AWallOverCapacityDoesNotWaitForAClick` all moved (production now agrees with the oracle / the LP holds what the router dropped), and the opt-in `OracleSweepFull.RigidBlock.WallsAndLadders` fails ONLY on its production drop/strand pins for wall-10/19/20 (its oracle λ\*/pivot readings are bit-identical — nothing leaked into the solver; the other four OracleSweepFull tests are Success). These were left EXACTLY as they fell per the slice's "do not re-pin the fallout" instruction — the Slice-4 checkpoint re-measures and rules on each, then deletes the stale anchors and updates the captions. See the dev-expert 3b report for the complete name-by-name old-vs-new list. **(c)** the `≥2-decline` untested branch (Slice-2 (a)) and the remainder second-solve are both GONE, so those two items are closed by deletion.

## Where the suite stands

**197 tests, 193 green, 4 red = 4 standing deliberate reds, 0 in-flight** (2026-08-29, after first-crack promotion + Slice 6a's 5-test min-violation readout suite [`Readout.{ViolationMatchesTheHandOracle,IsPermutationDeterministic,PrimalIsAGenuineEquilibrium,DegenerateIntraGroupTradeIsPermutationStable,ThreeWayReducibleTradeIsPermutationStable}`] + Slice 6b's 4 wiring/consumption tests [`Acceptance.StrainReadout.{PerJointReadoutIsLPSourcedBelowTheCap,ReadoutUsesFirstCrackForBondedBendingBelowTheCap,InspectPieceShowsLPUtilisationBelowTheCap,NoStaleReadoutAfterGateDeclines}`] + Slice 6d's assembly-split test [`Oracle.RigidBlock.DeadLiveSplit.LiveSurchargeHingesTheHeader`] all landed additive — OracleSweepFull 5/5 bit-identical, no verdict moved. **Slice 6b COMPLETE** (the strain overlay shows the LP-primal first-crack utilisation below the cap); **Slice 6d assembly split done**, its wall-15/16 bridge-posing follow-on open (line ~232).). The **4 standing** reds are the design-limit anchors: `Acceptance.Beam.MidspanCarriesTheMembersBendingMoment` (|M|=0 readout, step 6 — the last beam mechanism red, kept as step 6's driver), `Acceptance.Wall.Catalogue` (row 21, the ruled-Collapse inverted red), `Acceptance.Wall.StackBondColumnShearIsHeightIndependent`, `Core.Structure.CorbelStepsBeforeTensionWins` (step 5). They go green only at their named evolution step; never "fix" one by weakening its assertion. Both former in-flight reds closed at Slice 3b/4:
- ~~`Acceptance.Overturning.ABodyOnTwoLoadPathsPastTippingMustFall`~~ — **GREEN as of Slice 2**: the equilibrium gate (`FStructure::BreakByEquilibrium`) now falls the two-load-path body the interim guard's N≥2 blind spot missed.
- ~~`Acceptance.MustNotFall.APureCompressionDryStackStandsByGeometry`~~ — **GREEN as of Slice 3b**: below the cap the per-piece LP mechanism is the sole break authority and it suppresses the joint sweep's spurious kern break, so the geometrically-stable dry stack stands (the release-rule change §3.7 promised). No longer in-flight.

The mean-strength re-anchor (red 2026-08-13, green 2026-08-14) is COMPLETE in the default suite, and so is the interim guard's basis swap: its discriminator went green on 2026-08-14 when `SolverInterimOverturningMeanBondMPa` started reading the profile, and **exactly one verdict moved with it** (verified by running the swap out and back — TRAPS mutation (G2)). The 6 standing reds and what each anchors:

| Red | What it anchors |
|---|---|
| `Acceptance.Wall.Catalogue` — **1 row: 21** (was 4: 10, 19, 20, 21) | **Rows 10, 19 and 20 GREENED at Slice 3b/4** — below the 200-block cap the equilibrium LP is the break authority and stands all three (λ\* 111.5 / 48.0 / 218.42), so production now agrees with their ruled STANDS; their `DropsToday`/`StrandsToday` pins were deleted with the re-ruling and row 20 was re-ruled LocalLoss→Stands. **Row 21 remains the one inverted red**: a ruled COLLAPSE the model (and the LP, at 17.24) now STANDS — its composite re-sectioning reads the cover ~3.1× under the hand σ; `DropsToday = 0`, `StrandsToday = 0` pinned, the caption admits the disagreement. Goes green only at the composite/f_x2 rework or when the ruling is accepted. |
| `Acceptance.Wall.StackBondColumnShearIsHeightIndependent` | The model routes a hanging column's whole load to its foot. **Re-pinned 2026-08-14 at 12 and 20 courses** — the old 10/16 pair had gone VACUOUS at the mean re-anchor (the head-joint reading is bare cohesion and moved ×2/9 with f_v0 while the bed joint beside the hole did not, so at 10 courses the worst joint was that BED joint, 0.0090226494 against the head's 0.0088962, and the printed "factor of 2.465" compared two different joints on two different axes). Measured crossing: the bed competitor governs to 10 courses, the head joint from 11 (margins 1.23× at 11, 1.28× at 12, 1.30× at 20), and the test now asserts a ≥ 1.15 head-over-bed margin so it cannot go vacuous again. Correct per-pair figure 0.0022240556; production reads exactly (courses − 6) × it — 0.013344333333333335 at 12 and 0.031136777777777784 at 20 — and that identity is PINNED as a passing characterisation. Needs the "load sheds as it rises" design pass before code (see below). |
| `Core.Structure.CorbelStepsBeforeTensionWins` | A **finding**, not a wrong expectation: the counterweight buys a corbel nothing — C and D cross at **124 steps identically** since the mean re-anchor (the crossover moved from the 36-step tension crossing to the ~124-step CRUSHING crossing against the unmoved 10 MPa; cap 150) — because masonry behind a joint never reaches it. Goes green at evolution step 5 (tension support). |
| `Acceptance.Beam.MidspanCarriesTheMembersBendingMoment` — 1 row (was 3) | **The two beam OUTCOME rows greened at the first-crack promotion (2026-08-28)** — `Beam.Catalogue` (row 1 falls) and `TheMemberMaterialDecidesTheOutcome` (timber falls, steel stands). What remains is this MECHANISM red: midspan reads \|M\| = 0 under **2.32×** mean C24 bending capacity, because the true member-bending moment needs member failure (evolution step 6). It is deliberately kept as step 6's driving red — first-crack delivered the outcomes by the glue-line route, but the member itself parting is step 6. |

## What to do next — ranked

### Decide before building

- **What a selection MEANS when a cascade releases a picked brick** — a product decision with both readings written out (see "piece menu presenter" below). **This is reachable today, not latent**: the 2026-08-09 guard review found the cascade already on the world wire (`FStructureBinding::SolveAndBreak` at `Core/StructureBinding.cpp:177`, called by both commit doors in `Core/PieceActions.cpp` and the spawn settle in `World/DestructionStructureSubsystem.cpp` — in-tree since 2026-08-06). Decide now.

### Then build, in this order

1. **Close the cascade-wire residue.** The wire itself is **already built and in-tree** (since 2026-08-06): `FStructureBinding::SolveAndBreak` exists, both commit doors and the spawn settle call it, and `ApplyResults` releases Stranded and Falling alike — the old entry here claiming it was "removed, not stubbed" was stale, survived the consolidation, and was caught by the 2026-08-09 guard review. What its spec still owes, verified against today's tree: (a) the presenter's "broken (went with a removed piece)" wording at `Core/PieceMenu.cpp:1196` **is now a live lie** — a joint the cascade breaks under load gets the removed-piece wording; its failing test was supposed to land with the wire and never did (see piece menu presenter below); (b) confirm whether the strength-driven *world* integration test exists — the leaning stack is now a fixture that can genuinely overload a joint, which was the recorded blocker.
2. **The 2026-08-08 review queue**, in its approved order — next section.
3. **The per-brick joint-force breakout: a human presses Play.** Built end to end and green; the suite proves the model and nothing about the panel (every run is `-nullrhi`, and this project has twice shipped a green suite over something invisible). The check list when someone does: panel legibility (engine-default text, no scroll — a large selection runs off screen), magenta-vs-cyan strength at playing distance, hover routing on the opening frame, whether an entry click is swallowed, the odd reading order (Delete above the readout — accepted for the no-moving-buttons safety property, wants human eyes).
4. **Scenario system — the switching half.** Restart-current, a tiny lightweight default so the game opens fast (today's default is the 30 × 40 wall, ~190 ms of begin-play), and in-game scenario change (the in-world menu of DESIGN §9). Rows are data; a scenario needing a class is the drift to stop.

## The 2026-08-08 review queue — approved by the user, items 1–4 built

In sequence (rationale in DESIGN §7, which records each landing: item 1 rewrote case 12 to a one-cell pier ruled STANDS; item 2 landed the leaning-stack family and the interim overturning guard `FStructure::BreakOverturnedBodies`; item 3 closed the one-cell arching gate by capacity-check-and-withhold; item 4 built the rigid-block LP oracle and its fixture sweep, whose measurement summary is DESIGN §7 step 3). What remains:

1. **All four rulings landed 2026-08-12; wanted-case 2b delivered whole 2026-08-13; the case-21 ruling landed 2026-08-13** (Collapse stands, LP scope limit recorded — DESIGN §8). What remains from those rounds: the open user call on whether §8's cover-pair note moves on the LP direction finding.
2. **Promote equilibrium to cascade authority** → tension support → member failure → arbitrary force (DESIGN §7 path). Deleting `BreakOverturnedBodies` and its constant is part of this step. DESIGN §7 step 3's measurement summary records what the oracle already settled (corbel rulings upheld; counterweight priced 22.6×; composite depth cross-validated 0.03% on 13/14).

   **DESIGNED AND RULED 2026-08-14 — [PROMOTION_DESIGN.md](PROMOTION_DESIGN.md) is now the authority for step 4** and supersedes the one-line sketch above. All five decisions approved as recommended (DESIGN §8 carries the ruling). **The next work item in the whole project is its Slice 0a**, and the sequence is:
   - **0a — close the eight-course-cover family's phase-2 refusal. DONE 2026-08-15** (both reds green; default suite still 167 + 6; the whole slow group re-run with **no window re-pinned anywhere**). What landed, in the order the design mandates: (1) the `WhyNot` split — `EOracleRefusal` (7 enumerators), `RefusalText` and `FOracleResult::Refusal`, every refusal site setting reason and sentence from one statement, the three phase-2 phrases keeping "phase-2 simplex failed" as their prefix so the two dead relation branches that match that literal still match; (2) the defect itself — the ratio test's `RelativePivotTol` **1e-11 → 1e-9**. Both refusers answer (λ\* 155.63200561101226 at 128 blocks / 21,394 pivots / 75 s, and 43.132916253688222 at 200 / 34,825 / 230 s), both landing on the readings the two diagnostic variants had already agreed on, and the 7- and 9-cell neighbours came back **bit-identical** (λ\* and pivot count both), which is the evidence the change moved only the paths that were broken. Reasoning against the alternatives is at the constant: `Refactorise` builds the LU from the ORIGINAL columns, so a singular LU means the ratio test chose a numerically dependent BASIS, and since det(B) is the product of U's diagonal no elimination ordering can repair that — Markowitz is a speed lever here, not a fix. What 0a leaves behind is in the correction block below.
   - **0b — the latency spike. MEASURED 2026-08-15 (`Tests/OracleFeasibilitySpikeTest.cpp`, two rows in the opt-in group). R1 DOES NOT FIRE; R2 FIRES.** The feasibility reformulation (`bGravityIsLive = false`) works and pays: eight fixtures re-posed, the boolean cross-validated against λ\* ≥ 1 on every one, and **the 84-block gate fixture answers in 0.065 s against a ~0.050 s target — a 1.3× shortfall where the design's arithmetic said 12×**, with warm starts (the criterion's own named escape) unbuilt. wall-01's 375 blocks fall 280.5 s → 26.0 s. **But the regional sandwich is refuted, and not merely as under-delivering**: (a) summing a free-boundary region's vertical rows gives `0 = -W_region`, so the pessimistic side is INFEASIBLE for every region containing no grounded block — measured at every radius, no exception; (b) the sandwich's closing radius on a 12-course wall with a mid-height brick out is **5, where the region is 142 of 149 blocks**, and its two solves cost **1,625 pivots against the 965 of just solving the whole wall — 1.68× MORE than the work it exists to avoid**; (c) over ten deletions at radius 4, **five of ten certify** — exactly the five whose region reaches the foundation; (d) on the 30-course leaning stack the optimistic side certifies the collapse only at radius 14, where the region is **all thirty blocks**; and (e) **a closed sandwich can be a FALSE CERTIFICATE** — the stack's bottom band is feasible with a free boundary AND with a grounded one, so the design's rule certifies "the full structure is feasible" about a stack whose λ\* is 0.4405. Dropping the material outside a region removes its WEIGHT along with its restraint, so §5.3's "the found force system extends" is false. **What this leaves open, and it is the next design question**: regional decomposition is still REQUIRED (scenario scale is ~3.3× wall-01), so the pessimistic side needs a form that keeps the omitted material's weight as a dead surcharge while removing its restraint, and region growth is ground-anchored by construction rather than bounded by a bonded distance. Residues: the seconds ratio beats the pivot ratio by up to 5× and nobody knows why (a phase-1 pivot is cheaper than a phase-2 pivot — worth one attribution slice before anything is budgeted on pivots); the dead-pose pivot count runs 2.2× the equality-row count at 84 blocks and ~5× at 375, so **the lever weakens with scale**; and on the two small stacks the dead pose costs 5.5× MORE pivots than the λ\* pose, so the design's 10–100× estimate is refuted in both directions. **THE EARLY EXIT IS NOW MEASURED, AND §5.2's LARGEST REMAINING CLAIM IS REFUTED (2026-08-15).** `FOracleResult::PhaseOnePivots` and `PivotsToFirstFeasible` are set during the solve (observability only — the solver still runs phase 1 to optimality, so every λ\*, pivot count and scan count in the suite came back bit-identical), and the eight fixtures answer §5.2's open question: **0.63% of phase-1 pivots happen after the problem first reads feasible** — 58 pivots out of 9,245 pooled, and **3.85% is the largest on any single fixture** (wall-18). Per fixture, dead pose, phase-1 pivots / first-feasible pivot: stack-5 21/21 (0%), stack-30 163/never (infeasible — no such pivot exists, reported INDEX_NONE), 8×10 gate 490/487 (0.61%), corbel D 1021/1021 (0%), wall-18 467/449 (3.85%), wall-15 802/799 (0.37%), wall-06 875/866 (1.03%), wall-01 5406/5381 (0.46%). **Why the design's arithmetic was wrong, and it is structural rather than a bad fixture list**: phase 1's OBJECTIVE *is* the infeasibility sum, so "the sum reaches tolerance" and "phase 1 is optimal" coincide by construction — there is no optimality-proof tail to skip, only the handful of degenerate pivots after the last artificial reaches zero. §3.2's "proving feasibility can stop the moment the sum reaches zero" is true and worth nothing: the saving it names is already spent. All eight gravity-live poses report 0/0, confirming from the outside what the code comment claimed — a live pose is feasible before a single pivot and phase 1 never runs. **Residues**: (a) an early exit would also skip the pricer's closing FULL optimality sweep, which costs scans rather than pivots and is invisible to these two fields — unmeasured, but BOUNDED ABOVE by pricing's own measured 0.5% share of wall-01's runtime, so it cannot be hiding the lever; ~~(b) the seam's assertion pins population and ordering only~~ — **CLOSED 2026-08-15: every one of those integers is now an exact per-row pin, plus the pooled 58-of-9,245 as its own assertion, so "refuted at 0.63%" is a contract and not a printed line.** Its prover is TRAPS **X5** (the watch made to record at pivot 1: 10 assertions fire, the eight ORDERING rows stay green, and the headline inverts to 99.91% — exactly the wrong-but-in-range mutation X3/X4 could not catch). A second supporting fact recorded at the same time: the phase-1 gate is already LOOSE (≈13 absolute at wall-01 scale with the 1e6 cap RHS), so a stricter gate can only move first-feasibility LATER — 0.63% is an upper bound on this lever, not a mid-point.

     **What the same review found undefended and is now closed** (all inside `OracleFeasibilitySpikeTest.cpp`; the group is 9/9 with **nothing re-pinned**, every λ\* and pivot count bit-identical): (i) **a refusal is not an infeasibility** — `SpikeIsFeasible` reads `bAnswered && λ ≥ 1` and `Refuse` returns `bAnswered = false, λ = 0`, so a REFUSED region solve read as "the free-boundary side is infeasible", which is the shape of R2's own finding. All 61 solves in that test now assert `bAnswered` with `WhyNot` in the message and every region line prints it. Prover **X7**: force `PhaseOneFailure` on the r=2 free-boundary region and **exactly one** assertion fires — the new guard — while the r=2 verdict pin, the conservation lemma, the closing radius and the 5-of-10 count all stay green, which is the measured evidence that before the guard the mutation fired **zero**. (ii) **Part B never checked its five certificates against the global verdict** though Part A did; it now solves the whole cut wall per deletion (one 149-block dead solve each, taking the test 3.1 s → 7.2 s) and all five hold. (iii) **Part D is quantitative**: the falsely-certified band's own λ\* is **18.481256459924058** against the whole stack's 0.44048 — the certificate is wrong by **41.96×**, not a knife edge, so no tightening of the extractor rescues the lever (review's independent (N−1)^−2.29 fit predicted ~18.7 and lands 1.2% off). Prover **X6** (the extractor keeps the out-of-region blocks): 14 assertions, four of them Part D's.

     **FORWARD HAZARD — FULFILLED 2026-08-15, AND HALF OF IT MISFIRED. Read the correction first.** The physics half came true exactly: the repaired extractor does make the bottom band infeasible. The *pin* half did NOT — the repair landed as a **new** function (`SpikeExtractRepairedRegion`) beside an untouched `SpikeExtractRegion`, so Part D routed around the hazard rather than tripping it, and stayed green. The lesson to carry, because it generalises: **a pin can only notice a change that goes through the code it calls**, and a repair implemented beside the thing it repairs is invisible to every pin on the original. Part D is now a split — the unrepaired band's false certificate and its λ\* = 18.481256459924058 stay asserted as the historical evidence, with the repaired band's `charged=24, appliedW=64012.767, feasible=0` beside it. Original text follows. Part D's green **encoded the unrepaired extractor**. The user-ruled repair (§12 D2′: the pessimistic side keeps the omitted material's weight as a dead surcharge) makes the bottom band INFEASIBLE, so `PART D: the bottom band … must be FEASIBLE` goes **red by construction** the day the repair lands. That is the pin working — it is the only thing in the suite that would notice the repair changed a region's answer — and it must be **re-aimed deliberately** onto "the *repaired* band agrees with the whole stack", never patched or deleted to restore green. Said in the test header too.
   - **0c — attribute case 21's residual λ\* = 4.768. DONE 2026-08-21** (`OracleSweepFast.RigidBlock.Case21ResidualAttribution`; full result PROMOTION_DESIGN §4.4). Landed as seven probe rows: friction/crushing/both, the chain-vs-bearing split, and a fifth (μ=0 + bond zeroed) added on review. The residual is a non-brittle **friction+compression** mechanism (crushing inert); the 9.935 that appears when friction is removed is a **distinct bond-tension** mechanism (measured by the fifth probe collapsing it to 3.747, not a cap being lifted); cohesion's contribution is **local to the bearing courses**, not run to ground. Confirms §4.2, strengthens §4.4's governance rule. **Owed bite-prover** (recommended by review, not yet run): an X-series mutation making the oracle ignore per-joint `FrictionCoefficient` — probe 1 would read the control's 4.768 instead of 9.935, failing the AgreeStands window and Finding 3. Log the signature in TRAPS when run. Crushing mutations are NOT good bite-provers here (probe 2's same-number identity holds under a crush-cap drop — that asymmetry is itself worth the TRAPS note).
   - **0d — the first-crack rows measured across the whole sweep. DONE 2026-08-21 (implemented, measured-and-pinned, reviewed, committed).** The rows are wired behind `FOracleProblem::bFirstCrackRows`, gated on the flag AND per-joint on f_t > 0 (`RigidBlockOracle.cpp`). Review (Opus 5) independently re-derived the two LP rows from section statics and confirmed signs/coefficients/indices/area/`Conv` all correct (no 100× error); two one-line fixes applied before commit (case-21 window widened to ~±2e-5 to match its siblings; the uncapped-RHS deferral logged below). The bite test `DestructionGame.Oracle.RigidBlock.FirstCrack.BitesBondedBendingSparesDryJoints` (default) pins the three beam movers (exactly control/3, N≈0 pure bending) and the one-cell dry bit-identity. **Sweep-wide flag-on pins landed, MEASURED not scaled:** `Sweep.CorbelFamily` (default) carries corbel A (mover, off 20.3329 → on 5.66388, ratio 0.2786 — **below** the per-joint 1/3 floor by MULTI-JOINT REDISTRIBUTION across the 4-joint arm, NOT per-joint net tension: a single joint's ratio is (1+k)/(1+3k) ∈ (1/3,1] and cannot go below 1/3; review corrected the earlier "cuts deeper because net tension" gloss) and corbel B (compression control, off 333.403 → on 306.885, ratio **0.9205** — an ~8% move, so category-B invariance is APPROXIMATE, pinned as a characterisation not asserted unchanged); `OracleSweepFast.OpeningStrengthProbes` carries case 21 (off 17.2389 → on 15.1169, ratio 0.8769, STANDS, does NOT flip — the §4 confirmation; bond bending is an even more minor term than the plan's 6–14 guess). Each flag-on row pins `OverriddenJoints` = the bonded-joint count (4 / 26 / 133) so "a row for every bonded joint and none for a dry one" is a contract. **Mover bite PROVEN (M1: gate off → all three mover windows return to their flag-off values, ratios read 1.0; 6 of my pins fire plus the beam bite test's 6).** Disagreements recorded in the predictions record (`scratchpad/first_crack_predictions.md`, "MEASURED 2026-08-21" section). **STILL OPEN, three items plus a ruling:** (a) **the keyed-on-data BITE-PROVER does not fire on the one-cell dry pair** — M2 (key on "always"/material instead of f_t > 0) leaves it BIT-IDENTICAL (λ* 9592.68, pivots 11==11), because a dry joint's first-crack row is `-(n1+n2)+3|n1-n2| <= 0`, exactly the kern condition e ≤ h/3, and a *standing* dry fixture's optimum already sits in the kern (that is HOW the oracle stands it tension-free), so the added rows are slack. So the crux dry pin is TRUE but its bite is UNPROVEN on the fixtures we own. A biting witness needs a dry fixture standing on an eccentric optimum (h/3 < e ≤ h) — the "dry stack reads as falling" configuration the plan cites — which is not an oracle sweep fixture; build one and pin M2 against it (measure, never scale). Until then the dry bit-identity is a characterisation, not a proven-biting pin. (b) **The exhaustive slow-wall (wall-15/16/17/18/19) flag-on re-pin is DEFERRED** as disproportionate for the value — the movers + compression control are representatively pinned in the default/fast tiers already. When taken, it goes in `OracleSweepFull.WallsAndLadders`, and it must be MEASURED per window, never scaled (wall-18 is the tension mover, wall-17 intact is the candidate for a genuinely-unchanged compression control — but corbel B shows even compression fixtures move ~8%, so measure it, do not assume 1.0). (c) **The first-crack row is NOT gated on f_t < UncappedStrengthMPa** the way the tension row is (`RigidBlockOracle.cpp`, at the `!(FtMPa > 0.0)` guard). Benign — an uncapped-yet-bonded joint would get a row with RHS ~1e13·A, trivially slack, and no sweep fixture drives it — but a conscious omission, so logged: if a fixture ever carries an uncapped-yet-bonded joint, add the same `< Uncapped` guard the tension row uses. ~~**THE RULING OWED, gating a LATER slice (not 0d):**~~ **RULED AND DONE 2026-08-28 — first-crack promoted to authority.** Turning the flag ON in production flipped beam catalogue row 1 (C24 heavy glue line) to Falls (λ\*(on) = 0.88258 < 1.0), the correct verdict by a slightly wrong route (the glue line cracking; the member-failure route is step 6). The promotion landed cleanly (see the "Where step 4 stands" entry): 4 tests moved, dry fixtures bit-identical, nothing over-fell. The remaining first-crack residues (a)/(b)/(c) above are still owed but unaffected by the promotion.
   - **1/2 — relocation + the equilibrium gate. DONE 2026-08-27.** Relocation landed the oracle in `Core/RigidBlock/` + the LU/eta fuzz; the gate (`BreakByEquilibrium`) fells the two-load-path body, and `BreakOverturnedBodies`, `SolverInterimOverturningMeanBondMPa` and `OverturningGuardBasisTest` are deleted. The replacement did NOT need re-measuring — the leaning-stack family was measured stationary (the LP agrees with the guard on its home cases).
   - **3 — the mechanism becomes the break set** (feasibility formulation, phase-1 dual extraction, Farkas verification, release-on-mechanism). Red: rows 10 and 19. Closes them, `Beam.Catalogue`, and half of `Beam.Midspan`; makes row 20 answerable per-brick for the first time; `StrandsToday` goes to zero and the absent cycle-division rule stops mattering.
   - **4 — the diff-against-anchors checkpoint** (both mechanisms live, no deletions) against §7's three anchor classes: **A** verdicts/regions/counts exact, **B** discriminations at ±2% with each naming its axis, **C** readings re-measured once and pinned at measured bits. The rule "the diff reproduces the anchors" CANNOT mean the same numbers — most §6 utilisation anchors *are* the heuristics being deleted.
   - **5 — delete the heuristics. SUPERSEDED BY D8 (2026-08-27) — closed as a finding, not a deletion.** The slice assumed the LP would be the full cascade authority; D8's scope-by-size kept the ~1,220-block flagship + giant corbels on the ROUTER, so `ReseatSpannedGroups` / composite depth / the one-cell thrust gate / the arching cap are NOT dead — they are the router's break authority ABOVE the cap (and every acceptance fixture is below it, so the suite cannot see a deletion's effect on a large structure). Below the cap they no longer decide verdicts but still feed the READOUT, which Slice 6 re-bases. No clean deletion is possible independently; the intent revives only if the LP takes the flagship. Full analysis + regime map in PROMOTION_DESIGN §6 Slice 5.
   - **6 — residues**: the readout's source (6a) and its production wiring/overlay (6b) — **DONE**; the dead/live split capability (6d assembly split) — **DONE**, its wall-15/16 outcome demo deferred to step-7/shed (line ~232). **6c — re-aim the fuzzes to the mechanism — DEFERRED to Phase E (3D), 2026-08-29.** The re-aimed property is `CascadeFuzz`'s "no joint broke that was NOT in the extracted mechanism" (`supported-implies-routed` stops being an invariant below the cap). Scoping (2026-08-29): the cascade fuzz (`FStructureCascadeFuzzTest`, `StructureFuzzTest.cpp`) drives the ROUTER's per-joint sweep (`SolveAndBreak`) on structures with NO geometry — pieces built via 2-arg `AddPiece(MassKg,bGrounded)` (centroid zero, `bHasCentreOfMass=false`) and joints with only Normal+Area, so the bridge (which needs `CentreOfMassCm`, `InterfaceCentreCm`, `InterfaceHalfExtentCm`) would pose a degenerate origin-collapsed LP. So the property needs a PLACED-block generator with geometric coherence — the real work; the mechanism API it targets (`ExtractMechanism`, `PieceOfBlock`/`ConnectionOfJoint`, `FOracleMechanism.JointOpensOrSlides`) already exists (3a) and the geometry vocabulary exists in `OracleMechanismExtractionTest.cpp`'s GroundedSeat/FreeBlock/BedJoint helpers and the wall/layout builders. **Why deferred to Phase E and not blocking:** the property is a green-on-arrival regression net (the LP already breaks exactly the mechanism by design), and the mechanism extraction is 2D today but gets generalized to 3D in Phase E — a 2D mechanism-property fuzz built now would need re-aiming for 3D, so it is built once, with the 3D mechanism, as its natural home. The existing router-cascade fuzz stays valid for the above-cap router in the meantime.
     - **6a — the min-violation LP that sources the strain readout. DONE 2026-08-28 (oracle-side only, no production wiring).** `RigidBlockOracle::SolveMinViolationReadout`, routed from `SolveRigidBlock` on `FOracleProblem::bMinViolationReadout` and NOWHERE else, so the maximise-λ path is untouched (OracleSweepFull 5/5 bit-identical — every λ*/pivot/scan pin held). It is a separate assembly (no lambda column, no cap row): gravity fixed as a dead constant at λ=1, per-block equilibrium EQUALITY rows kept HARD, every strength inequality relaxed `a_k·x - s_k ≤ b_k` with a non-negative violation `s_k`, and it fills `FOracleResult::Readout` (per joint: N=n1+n2 compression-positive, M=HalfLen·(n1−n2), total slack, worst-row utilisation demand/capacity). **The canonicalization is PER-GROUP LEXICOGRAPHIC MINIMAX.** A bare `min Σ s_k` is constant over an indeterminate free family and lands on a permutation-dependent vertex (the readout wobbled). A SINGLE global minimax `min Σ s_k + w·t` (`s_k ≤ t`) was the first fix but is WRONG on any structure with more than one independent overload group: the global `t` only evens the globally-maximal group, leaving lower groups a free family — permutation-unstable AND physically wrong per-joint (a two-group fixture piled group B's centre joint to 63333 instead of its true even 25333). The correct objective evens EACH independent group at ITS OWN level, via a bounded loop of LP solves: (1) a LEVEL solve `min t` s.t. free `s_k − t ≤ 0` → the current worst level `t*`; (2) a REDUCTION solve `min Σ_candidates s_k` s.t. free `s_k ≤ t*` (constant) — this is the crux: it pushes off `t*` every candidate that CAN come down, so the slacks still reading `t*` afterwards are the CRITICAL ones (irreducible — stuck at `t*` in every optimum), distinguished from a lower group's slack that merely reached `t*` incidentally on a pivot path; (3) pin the critical slacks at the scalar `t*` (even by construction → permutation-invariant) and recurse on the rest. Terminates because `t*` is achievable so ≥1 slack is critical per level (fails closed `VerificationFailure` if it ever makes no progress). **Measured: single-group PIERS reads 190000 evenly = 63333.3 per joint; the two-group fixture evens group A at 63333.3 AND group B at 25333.3 per joint — both identical across 8 seeded block+joint permutations.** The criticality binding tolerance is derived-not-tuned: `BindingRelativeTol = 1e-6` applied as `1e-6·max(1,|t*|)` sits three orders above solve noise (~1e-9 of scale) and orders below the inter-group gap (~38000 uu), so any tolerance between those extremes selects the same critical set; it never decides a pinned VALUE (always the scalar `t*`). Cost: 2 solves per distinct overload level + 1 final (STAND 3, OVER 5, PIERS 5, TWOGROUPS 7) — bounded, and the readout is cached (solve-on-settle), not per-frame. Driven by `DestructionGame.Oracle.RigidBlock.Readout.{ViolationMatchesTheHandOracle,IsPermutationDeterministic,PrimalIsAGenuineEquilibrium}` (default suite; 190 tests, 186 green + the 4 standing design-limit reds). **Reviewed, no blocking findings** (two-solve criticality proven sound — false-reducible impossible by construction, false-critical absent on all owned + general parallel-support/independent-group cases; OracleSweepFull 5/5 bit-identical). **Red-first confirmed by execution:** the single-global-`t` predecessor piled group B's centre joint to 63333 (vs the even 25333.3) with the flanks at 6650/6016.67, and that split DRIFTED 633.3 under permutation against a 0.19 tolerance — the exact bite the re-based `IsPermutationDeterministic` (TwoGroups) now gates. **The review-flagged degenerate corner was REALIZED, found a real bug, and is FIXED (2026-08-28).** The hardening probe (`Readout.DegenerateIntraGroupTradeIsPermutationStable`, fixture `Degen`) built the case the reviewer could not: a rigid cap on three grounded point piers — a central pier (distinct column → forced critical) beside two flanking piers at the SAME x (equilibrium fixes only their slack SUM) with MIXED caps (30000 vs 50000, both over cap → constant pair subtotal). It went RED for the right reason (flanks read 73333.3/42666.7 not the even 58000/58000, split SWAPPING under permutation, drift 3.067e4 vs 0.294 tol). The mechanism was one level finer than "piers": with point contacts (`HalfLen=0`) each joint splits into two contacts sharing one joint force, and a flank contact sitting *incidentally* at `t*` got FALSELY pinned critical because rebalancing it to its partner contact is net-zero in the single `min Σ` reduction → an arbitrary column-order vertex. **The fix iterates the reduction to a FIXED POINT:** re-run `min Σ` over only the slacks still reading `t*`; a member reducible by rebalancing to a now-unpenalised partner (outside the shrunk cost set) strictly cuts the objective and drops clear of `t*`, while a member stuck in every optimum stays — the at-`t*` set is monotone non-increasing so it stabilises (≤1 LP per truly-critical member), pins only the scalar `t*`, is LP-only/deterministic/permutation-invariant, and preserves the fail-closed no-progress guard. `Degen` now reads the even split (58000/58000) permutation-stable across 8 seeds; the un-iterated single reduction IS the gate-bites mutation. Suite now **191 = 187 green + the 4 standing reds**; `OracleSweepFull` 5/5 bit-identical. **This closes the residual — the readout is sound for 6b to consume.** (Perf note for a later slice: the fixed-point loop adds a bounded handful of extra LP sub-solves only at a level that has a reducible sub-family co-level with a critical member; symmetric fixtures stabilise on the first check. Solve-on-settle, so not collapsed into fewer solves — no failing test justifies that optimisation yet.) **6b IN PROGRESS (2026-08-28): core wiring DONE, three follow-on reds remain.** The solve-on-settle cache is wired: `FStructure::CacheMinViolationReadout(const FOracleProblem&)` poses a SEPARATE copy of the gate's assembly with `bMinViolationReadout = true` and solves it, called from `BreakByEquilibrium` on BOTH answered below-cap branches AFTER the break authority has run (after `ApplyLimitAnalysisSupport` on Stands, after the mechanism sever loop on Falls); per-joint `FOracleJointReadout` is keyed to production connections via `Problem.ConnectionOfJoint` into `TArray<FConnectionReadout> ConnectionReadoutCache`, exposed by `FStructure::GetConnectionReadout(int32)` (`{bPresent,NormalUu,MomentUuCm,ViolationUu,Utilisation}`). The cache is `Reset()` at the top of `SolveAndBreak` so a decline-to-router above the cap cannot serve a stale below-cap reading; a readout the solver can't produce leaves the entry absent (fail-closed to router), not zero-filled. ADDITIVE — the readout is a separate cached solve after the verdict; `OracleSweepFull` 5/5 bit-identical, suite 193 = 189 green + 4 standing reds, NO break verdict moved. Driven by `Acceptance.StrainReadout.PerJointReadoutIsLPSourcedBelowTheCap` (a determinate tension hang: the LP carries it in tension reading util 0.3724 while the router strands it at ~0, so a readout matching the hand oracle can only be LP-sourced; M=0 so first-crack is a no-op here and does not entangle this red). **Reviewed?** pending. **The three follow-on reds that finish 6b:** (i) **first-crack rows in the readout LP — DONE (2026-08-28).** `SolveMinViolationReadout` now assembles the two uncracked peak-fibre branches for each bonded joint (`f_t>0`, guarded `!(f_t>0)` so NaN fails closed) via `AddStrengthRow` when `bFirstCrackRows` is set — each with its own violation var, joining the per-group lexicographic-minimax canonicalization uniformly (no special-casing) — and `CacheMinViolationReadout` sets `bFirstCrackRows = true` to match the gate. Driven by `Acceptance.StrainReadout.ReadoutUsesFirstCrackForBondedBendingBelowTheCap` (a verdict-free bonded corbel, e=3h: the readout reads the first-crack util 0.59584 not the ~3–4× looser plastic 0.14896 — ratio exactly 4). Both parts independently load-bearing (reverting the flag reds the cached path back to plastic while the oracle-side pose still reads first-crack). DRY (f_t=0) fixtures bit-identical (zero rows added to a dry joint), the M=0 hang readout unchanged (first-crack ≡ plastic with no gradient), `OracleSweepFull` 5/5 bit-identical (maximise-λ path untouched — the new rows are gated behind `bMinViolationReadout`). Suite 194 = 190 green + the 4 standing reds. First-crack row NOT gated on `f_t < UncappedStrengthMPa`, mirroring the maximise-λ path's existing deferral (0d residue c). **Reviewed, no blocking findings** (coefficients/RHS/units verified against the maximise-λ path and §4.3 — full joint face `f_t·A`, conversion once, no 100× trap; NaN fail-closed; dry bit-identical by construction; canonicalization determinism covered by the 6a proof since first-crack rows are uniform slack rows). **Two non-blocking follow-ons the review raised:** (1) **a bonded-INDETERMINATE readout determinism guard is DEFERRED HARDENING** — Degen/TriDegen are dry and the corbel is determinate, so no test drives first-crack rows through an indeterminate bonded overload group (a small redundant bonded wall below the cap would); the 6a class-closure proof covers it and any wobble there is a below-cap cosmetic overlay wobble not a verdict, so it is not blocking, but it wants a guard when convenient. (2) **`Core.Structure.ACorbelResistsWithItsWholeDepth` is a pre-existing SLOW test (>10 min)** — its corbel cases build ~2,200–3,100-piece walls far above the cap, which decline to the router (the readout code is inert there), so the cost is the above-cap router's heuristics on a multi-thousand-piece wall (the DESIGN-flagged full-building perf risk), NOT this change; it reproduces at baseline fa9ede8. It makes a full-suite run take many minutes and blocked the reviewer's independent green reproduction (dev's run completed at 194 = 190 + 4). Wants a perf bound / a way to keep it out of the fast loop — relevant to the shed's above-cap latency (SHED_PATH flags). (ii) **the exact under-cap utilisation definition** — pin that below the cap it is the LP-primal demand/capacity, not `FConnection::UtilisationUnder`, on a fixture where the two measurably disagree (every fixture so far has M≈0). (iii) **the overlay-presenter consumption + the cache-staleness fix — DONE (2026-08-28). 6b IS COMPLETE.** `InspectPiece` (`Core/PieceInspection.cpp`) sources `FJointInspection.Utilisation` from `GetConnectionReadout(Index)` below the cap when `bPresent`, else falls back to the router's `GetConnectionUtilisation` (above the cap / absent) — so the strain overlay shows the LP-primal first-crack utilisation below the cap (the LP is the authority there) and `UtilisationUnder` above it. **First time the min-violation readout is PLAYER-VISIBLE.** This settles follow-on (ii): the under-cap util below the cap IS the LP-primal (first-crack) demand/capacity, not `UtilisationUnder`. Driven by `Acceptance.StrainReadout.InspectPieceShowsLPUtilisationBelowTheCap` (the tension hang: InspectPiece flips from the router's stranded ~0 to the LP's 0.3724). **Stale-read hole CLOSED:** a per-pass `ConnectionReadoutCache.Reset()` at the TOP of `BreakByEquilibrium` (belt-and-suspenders with the per-call `Reset()` at `SolveAndBreak` top) makes the "no stale reading" invariant hold UNCONDITIONALLY — only an answering pass (Stands / certified Falls) refills the cache, so every decline return leaves it absent → router fallback. Guarded by `Acceptance.StrainReadout.NoStaleReadoutAfterGateDeclines` (green, and BITES — removing both resets lets the pre-decline 0.3724 survive → red). The review's within-call answered-then-declined scenario was PROVEN UNREACHABLE through the public API for owned fixtures (over-cap/no-geometry declines are whole-structure-constant across passes; bridge acceptance is monotonic — severing only skips joints, never starts refusing an accepted remainder; the only remaining trigger is a numerical mid-cascade decline no owned fixture hits below the cap, `MaxPivots` being compile-time constant) — so RED B pins the reachable cross-call case while the per-pass reset defends the unreachable within-call window unconditionally. Additive: diff is the read path + one cache-reset line, no break-decision line touched, so OracleSweepFull bit-identity holds by construction (not re-run). Suite 196 = 192 green + the 4 standing reds. **Reviewed, no code-blocking findings; VISUAL VALIDATION RULED NOT REQUIRED** (the slice changes only the data source of a range-safe overlay scalar — no rendering logic, cascade, or verdict moved; the LP value shares the router's 0→1→>1 domain the HUD already handles, and RED A pins the exact drawn value; linkage confirmed `PieceMenu.cpp:1138` → HUD). **Three non-blocking notes:** (a) FIX 2's per-pass reset is not INDEPENDENTLY red-pinned — RED B (a cross-call cap-flip) is cleared by either reset alone, and the within-call window it truly defends is unreachable through the public API; kept as a cheap defensive line that makes the invariant hold by construction rather than by a reachability argument a later slice could invalidate (`MaxPivots` compile-time constant + no owned fixture reaching a mid-cascade numerical decline). (b) **Cross-source cosmetic:** below the cap a joint row now shows router-sourced `ForceUu` (~0 on the stranded hang) beside the LP-sourced ~37% utilisation — deliberate scope (`ForceUu`/`MomentUuCm` stay router-sourced), but wants an owner's call later on whether force/moment should follow util to the LP. (c) `CacheMinViolationReadout` is last-write-wins if two oracle joints ever map to one connection (fine for 1:1 bed joints; note if point-contact `HalfLen=0` joints ever share a connection). (Perf: the readout is computed on every answered below-cap pass, last wins — acceptable, cached solve-on-settle; no failing test justifies restricting it to the final pass yet.)  **The original deferred items, still live where not superseded above:** (b) **The under-capacity utilisation is left crude on purpose:** it is the worst per-row `demand/capacity` off the primal, which is correct on the governing axis (STAND reads a tiny compression ratio ≤1, and `util == 1 + violation/cap` holds for over joints as the tests assert) but 6b should decide the exact under-capacity definition the overlay wants (e.g. matching `FConnection::UtilisationUnder`'s biaxial fibre stresses / composite relief — the min-violation primal here has M≈0 on every fixture so bending is untested). (c) **First-crack rows are NOT assembled in the min-violation LP** (`bFirstCrackRows` is ignored on this path); no fixture sets it, but if the shipped readout should crack bonded joints at first crack, 6b adds those rows each with its own violation var. (d) the fail-closed `FailClosedUtilisation`/zero-capacity-axis branch in the readout is DEAD on the owned fixtures (dry friction rows carry ≤0 demand) — it protects the "violation>0 iff util>1" invariant but has no biting fixture; a joint sliding with cohesion eroded below zero would drive it. Its NaN polarity was corrected in the 6a fix (`!(Demand <= floor) ? FailClosed : 0.0`) so a NaN demand lands OVER not the comfortable 0.0. **`FOracleResult::FinalBasis` is no longer populated on the min-violation path** (the readout takes no warm start and nothing consumes it) — noted in case a later slice wants it recorded.

Also queued from the same session: **a deliberately-red hanging acceptance test** (a piece screwed to the underside of a grounded slab — reads Falling today, should stand while EN 1995 withdrawal capacity holds; a second over-capacity row must fall). Makes the currently-dead fastener withdrawal data load-bearing; anchors step 5.

**The mean-strength re-anchor — COMPLETE 2026-08-14** (decided 2026-08-08, red phase 2026-08-13, green phase 2026-08-14; derivation record revision 2, user-approved fallout plan). The profiles carry the means with citations; **the guard question is CLOSED (2026-08-14)** — `SolverInterimOverturningMeanBondMPa` now READS `DestructionProfiles::GeneralPurposeMortar.TensileStrengthMPa` (0.70) instead of its old guard-local 0.6 literal. Review had held the swap back because it moves every overturning verdict 16.7% and nothing in the suite discriminated the two values, which under the non-waivable TDD rule made it production logic needing its own red; that red was written and the swap landed behind it — `Core.Structure.TheOverturningGuardRestoresAtTheProfilesMeanBond` (the spec this block used to carry had the direction inverted and named the wrong rung — see the interim-guard block below for the measurement). Measured fallout: **exactly one verdict moved, the discriminator's own** (suite 166 + 7 reds → 167 + 6; mutation (G2) in TRAPS re-runs the swap out and back and confirms it suite-wide). Not constexpr any more, and that is safe: `GeneralPurposeMortar` is an aggregate initialised entirely from literals, so it is constant-initialised and strongly happens-before this dynamic initialiser. Green-phase findings a future reader needs (all re-measured, none scaled): case 9's wide-jamb reading went **SHEAR**-governed at the flip (0.26329211195559277 — Mohr-Coulomb sliding under the arch thrust; the compression is 0.0203 and the tension is exactly zero, and the "squeezed edge" attribution recorded here until 2026-08-14 was wrong), the one-cell mortared half seat's LP answer went CRUSH-governed (1877.92298545, reproduced by the new `JammingAloneCrushLambda` closed form), the half-area-twins λ* equality is 1 ulp apart at the mean data (pinned ≤ 2 ulp; was bit-exact at the characteristic data), the 15/16 sweep ratio fell 31.56× → 4.51× (loaded side compression-governed, floor re-derived 10 → 3), and corbel F's 100-step worst axis is now compression ~0.654. The slow group was re-measured and re-pinned in the same pass and is **all green** (5/5 at the time, ~13 min a run; second run certified every window — the group is 7/7 and ~16 min since Slice 0a added two tests). Prediction disagreements the measurement found, so nobody re-derives from the plan instead of the data: the derivation doc's "Layout spanning brick falls" is wrong (pure compression, invariant); case 21's predicted ~0.549 worst measured **0.9354** (axis flip); the case-21 λ* scaled ×3.13, not ×4.5; the zeroed-cohesion probe reads 4.77 and STANDS (the AgreeFalls arm died); the abutment ladder's sensitivity collapsed to ~1.05×. Still open from the green phase, each its own entry below: the lost-discrimination replacements and the `MortarRaggedWorstAsBuilt` independent hand derivation (now 0.0065014926, still owed).

**Lost discrimination — replacements OWED (specified in-file where each died; may be built in or after the green phase, never silently dropped):**

- **Too-wide-to-arch (mortared)**: `StructureThrustTest`'s 20-cell fall arm and `StructureCoverTest`'s one-course fall arm both stand at mean strengths (springing capacity ~4.5×). Specified replacement: a 20-cell cut under ONE course of cover in the 30×40 wall — hand estimate demand ~1.26 MPa vs capacity ~0.92, ~1.4× over — laid and measured in green.
- **Free-end height ladder**: capacity crossing moves ~59 → ~412 courses; ladder keeps trend + agreement anchor only. Replacement: a deeper multi-course free-end cut (re-design, measure; never re-tune).
- **Corbel crossover pair E35/E36**: the 36-step tension crossover is dead; the worst-axis crossover is now ~124 steps in COMPRESSION (closed form F(k)·w/(105.0625e4·10); `CorbelStepsBeforeTensionWins` re-derived, bisection cap raised to 150). Replacement pair of scenario levels either side of the measured crossover owed; `ScenarioHoldTest`'s condemned floor is SUSPENDED (`StandingCases >= 2` interim) until the condemned exemplar lands — restore `CondemnedCases >= 1` then.
- ~~**Corbel F**~~ — LANDED: the 150-step re-spec (fails by CRUSHING, compression strictly worst, 1.457 ≥ 1.2 margin) went green at the flip, and the ~6,800-piece fixture cascades in **~3.7 s wall** — no fallback needed.
- **One-cell anti-over-withhold**: lime's wholesale-withhold outcome signature is dead, **measured 2026-08-14** — the withhold-everywhere mutation fires 5 tests / 41 lines in `Core.Structure` (the arched-identity family; TRAPS registry has the full signature) and `AOneCellArchMustEarnItsThrust` does not fire AT ALL, so that test is currently blind to over-withholding. Specified replacement stands: pin the earned seats' readings to the arched identity (`ArchedUtilisation`, the same 1e-12 kern-edge form `StructureArchingTest` uses for cement) inside `AOneCellArchMustEarnItsThrust`; prove by the same mutation and update its TRAPS row again.
- **Composite depth's mortared falling arm** *(found in the green phase — the red phase's fallout inventory missed it)*: `ACorbelResistsWithItsWholeDepth`'s two 45-step MustComeDown rows stand at the mean basis (0.1786 and 0.1948 against the mean 0.70; the tension ladder cannot reach 1.0 below the ~124-step crushing crossover), so both are now `Unasserted` with the loss recorded in-file. The DRY row still falls and `AHundredStepCorbelMustComeDown` (150 steps, crushing) carries the giant-corbel guarantee; a mortared depth-bound falling row, if slice 5 wants one, must be a re-design at the crushing crossover, never a re-tune. Deciding the 45-step rows' honest verdict (MustStand is a ruling nobody has made) goes with that re-design.
- **Case 9's 0.985 knife-edge flap watch** is vacuous — the wide jamb's reading sits ~3.8× clear of 1.0 and is not the tension the knife edge was about. **CORRECTED 2026-08-14 by decomposing the joint**: it is not "the squeezed edge's COMPRESSION" either, as this file, DESIGN §6, TRAPS and the test's own comment all said — that axis reads **0.020307210426829269**, thirteen times under production. The reading is **Mohr-Coulomb SHEAR**, the jamb sliding under 270,024 uu of arch thrust: 0.25701313 MPa against a capacity of 0.9 + 0.75 × 0.10153605 = 0.97615204 MPa is 0.26329211195559277 to all seventeen digits. Its flexural tension is EXACTLY zero (the moment is kern-clamped, so bending and normal stress cancel bit for bit). Both axes are now asserted as one-formula identities in `SpanIsReadInTheJointNotInTheOutcome`.
- **Case 21's post-flip axis is UNVERIFIED and its recorded attribution is suspect** *(2026-08-14)*. Everywhere — DESIGN §8, the catalogue row, the caption test — records the 0.93542327561664174 as "the squeezed-edge COMPRESSION". That exact attribution was made for case 9's jamb and for case 22's worst joint and **both proved wrong on decomposition**, reading Mohr-Coulomb SHEAR with compression an order of magnitude under; case 21's worst joint is the same jamb-bed shape as both. The verdict and the inverted-red pins do not depend on it, but the *explanation* of the disagreement does. **All eight sites now hedge it rather than assert it** (2026-08-14 review close: the caption, the sweep's wall-21 and parity-rung headers, WALL_CASES.html, LEVELS.md joined the four that already did) — so nothing in the tree claims an axis the arithmetic has not produced. **Specified slice to settle it**: `FWallResult` already carries the pre-cascade reading; add the worst joint's `GetConnectionForce` decomposition beside it (three doubles, no extra solve — case 22's axis wants the same seam and is logged separately), then assert the axis as a one-formula identity exactly as case 9's now is (`0.25701313 / (0.9 + 0.75 × 0.10153605) = 0.26329211195559277`) rather than describing it. Bite-prover: write the identity against the *wrong* axis and confirm it fails by the order of magnitude the case-9 mutation (W3) measured.
- **The span pair's span-law floor is RETIRED — it passed for a reason unrelated to span** *(2026-08-14; this is the replacement-or-record rule being exercised)*. `ReadingRatio >= SpanRatio` divided a shear utilisation (÷0.976) by a tension utilisation (÷0.70), so its 3.66 → 6.84 move at the re-anchor was the DIVISOR changing, not the span. Worked three ways and the span law holds on none: worst-axis stresses 0.0269441 MPa of tension against 0.2570131 MPa of shear (different quantities); same-axis both walls (each ratio has a zero in it — case 9's tension is clamped to zero, case 7's jamb carries no shear at all); and the honest strength-free reaction, the normal force through the joint, **56,010.69 → 106,676.31 uu = 1.9046× for 2.6928× of span — SUBLINEAR**, so w·L/2 is not satisfied. What replaced it, and is stronger because no strength appears in it: **the wide opening puts 270,024 uu of horizontal THRUST through its jamb where the narrow one puts exactly none** (a mechanism arriving, not a bigger number), plus a strict-increase row on the reaction and the 1.9046 ratio pinned as a passing characterisation of the weak span term. **Owed**: the sublinearity is a finding, not a red — if step 4's equilibrium promotion gives the reaction an honest span term, that pin fires and the ≥ linear arm can be restored as a real assertion.

**Interim guard fixtures — RETIRED WITH THE GUARD (2026-08-27).** This whole block was about `Tests/OverturningGuardBasisTest.cpp`'s basis discriminator and the guard's (0.60854, 0.75975) MPa effective-bond bracket. Slice 2 deleted `BreakOverturnedBodies`, `SolverInterimOverturningMeanBondMPa` and the discriminator test, so none of it exists to pin any more — the equilibrium gate answers overturning by LP feasibility, not a bond bracket. Kept in git history if the derivation is ever wanted. (The firing-boundary pair it mentioned as "unwritten" is moot — there is no guard to fire.)

### New acceptance cases wanted (priority order, from the review)

| P | Case | Why |
|---|---|---|
| 2 | Compression-vs-shear capacity ratio (one joint, two directions, ~50×) | DESIGN §4 calls it key validation; nothing anywhere does it. Belongs beside `ConnectionStrengthTest` |
| 3 | L-corner / return (brick out at a corner vs a free end; assert the *relation*) | the solver has never seen two orthogonal joint families |
| 4 | Lintel over case 7's opening (with/without) | the pair case 7 needs (it fails the published arching gate and is the standing half of three pairs); the model's first spanning member |
| 5 | Dry-stone acceptance rows (intact; one out) | the only configuration where Mohr-Coulomb coupling is first-order; the set has none |
| 6 | Two-wythe wall (same cut, one vs two wythes) | the composite `t` term has only ever been 10.25 cm |
| 7 | Eccentric surcharge over an opening | no test applies a discrete external load |
| 8 | Progressive removal to a predicted count (12×12 wall; stands at N−1, falls at N) | DESIGN §4's headline integration test exists only on a 6-piece toy |
| 9 | Removal order independence ({A,B} == {B,A} settled state, bit for bit) | cheap, Core-level, uncovered |
| 2 | **Connected-wall locality** (cut ONE brick from the middle of a single large bonded running-bond wall; assert the far end keeps its exact support state) | The stronger form of the C1 "a change far from the cut stays put" promise. `Acceptance.Locality.AChangeFarFromTheCutStaysPut` (added 2026-08-27) covers the DISCONNECTED-regions case — it catches a radius-based-spread bug across a gap (proven to bite) — but not over-propagation ALONG real joints inside one bonded structure, a strictly larger bug class. From the independent product-acceptance review (its C1/C4). |

Note: rows 8 (progressive removal to a predicted count) and 9 (removal-order independence) were independently re-derived by the 2026-08-27 product-acceptance review (its B2 and a determinism case) — corroboration they are worth doing.

Standing doubts on existing cases (physical evidence would settle, not more derivation): case 17 has zero discriminating power on *outcome* intact — narrowed 2026-08-12 (it now carries λ* 918.05 and a 1.41× LP margin against case 18) but the repurpose idea stands (the *wide* stack-bond removal — utilisation grows with width while running bond arches); case 18's height-independence property belongs on a cheap 3-column Core fixture, parameterised over n; case 20's true count is more than 2, fewer than the model's 9-today (untouched by the LP measurement — λ* has no local vocabulary). Case 3's verdict is right but reached via a 2.1 m composite section, which is not the real mechanism.

## Deliberately left alone — known, unfixed, with pickup context

None of these block the list above. Each needs its own red test first; most are branching logic that nothing drives yet.

### Load solver

- **A cohesionless joint with a real tensile bond survives the withheld one-cell relief, and that entangles gap 5 behind gap 4.** Measured while designing `AOneCellArchMustEarnItsThrust` (its header records the whole finding): `CohesionlessBond` — dry stone's μ = 0.7 with a real 0.08 MPa tensile bond — is refused the relief correctly at 1.4921 of sliding capacity, and then does **not** fail, because composite vertical action re-sections the moment and it reads **0.807**. So for a zero-cohesion joint the honest verdict needs DESIGN §7 **gap 5** as well — the deep beam's horizontal shear flow is never applied as a demand on the bed joints that must carry it — and a row asserting the outcome today could only be satisfied by closing both. That is why the acceptance table has no such row; write the gap-5 demand first, then the row.
- **The one-cell thrust check credits the whole moment vector's magnitude, torsion included.** `(1 − k)·|M|` at the call site in `SolveLoads` takes `FVector::Size()`, where only the in-plane part is what a horizontal pair can supply; a component about the seat's own normal is torsion, which DESIGN §5.3 does not model and which gravity on an axis-aligned rectangle never produces. The difference can only ever ask for *more* thrust, so it fails closed — but the day a joint carries a torsional moment, this judges it slightly harshly. Distinguishing fixture needed before either spelling can be preferred.
- **The one-cell thrust gate's degenerate and asymmetric cases are covered only by construction, not by a row.** The single `!(demand <= capacity)` is what lands a NaN capacity, a zero/NaN arm and a degenerate area inside the refusal — no separate branch exists for them, deliberately — and the per-seat evaluation is what would let an asymmetric pair (one springing affording the push, the other not) resolve one side at a time. Both behaviours are believed right and neither has a test naming it; the fixture for the second is a one-cell hole whose two half-seated bricks stand under unequal cover. One genuine fail-open in the family, found in review: the hand-written truncation at the capacity site keeps a NaN *capacity* (refuses, correct) but silently drops a NaN `MaxShearStrengthMPa` *ceiling* (compares false → capacity untruncated). Unreachable today — profiles are static and fixture-asserted — fold its row in with these.
- ~~**The interim overturning guard credits GENERAL PURPOSE MORTAR's bond to every joint it evaluates**~~ — **MOOT 2026-08-27: the guard is deleted (Slice 2).** The per-joint-blindness concern (the guard restoring a dry-stone or fastened body with a bond it does not have) dies with it — the equilibrium gate reads each joint's own strength through the bridge, so a body is only ever held by the strengths it actually carries. Nothing owed here.
- **The guard's `Supported` gate and NaN polarity are untested in either direction** — the gate's protection is across passes, not within one (a body whose only ground path this same pass's sweep severed still reads Supported, so its bridge bearing can pick up a same-pass stamp; comment at the site in `Structure.cpp` states this honestly). **Specified pin from the guard review**: grounded base → deliberately over-capacity joint → platform → 12-course 10 cm/course stack; pass 1's sweep severs the weak joint; assert whether the stack's bottom bridge carries a pass-1 stamp — either answer is defensible, the value is pinning the simultaneity convention. NaN polarity (guard fires nothing on garbage arithmetic — deliberate, breaker-conservative) likewise has no test either side.
- **Dry stone has no rocking model, and the leaning stack proved it in the wrong direction** — dry stone's tensile strength is an exact zero, so any joint outside the kern reads `Max()` and a 5-course 2 cm/course dry stack that plainly stands in reality reads as falling (standing-reads-as-falling, the opposite polarity to every other gap here). **The red test now EXISTS (2026-08-27): `Acceptance.MustNotFall.APureCompressionDryStackStandsByGeometry`** (two DryStone bricks, upper offset 8 cm, CoM 2.75 cm inside the base bearing edge — genuinely stable, neither tips nor slides at μ = 0.7 — which production fells because e = 4.0 > kern 2.25 with f_t = 0 gives net edge tension against a zero limit; base keeps the earth, nothing stranded, the upper brick's own bed joint gives). It is an **in-flight red** (see "Where the suite stands"), red for the right reason, and **closes at the step-4 equilibrium gate** where rigid-block statics stands a geometrically-stable stack, or at a step-5 no-tension rocking model. Do not weaken it to green. Originally noticed during the leaning-stack design (`LeaningStackAcceptanceTest.cpp` header).
- **The leaning-stack fixture family has no playable levels** — the wall and corbel catalogues are one level per fixture (`LEVELS.md`); the four leaning-stack rows are the first acceptance fixtures without scenario rows. A `FScenario` producer for an offset stack is a small slice when wanted.
- **`FConnection` is copyable and the latch is per-copy** — `for (FConnection C : Connections)` latches temporaries and reports a structure that never breaks. Closed at the one site that latches; nothing prevents the next. Revisit if a second site latches.
- **`StructureFuzzSupport::UtilisationUnder` is a deliberate hand transcription** — redirect it at production as its own change (never inside a solver refactor), re-running mutations (g)/(h) before and after ([TRAPS.md](TRAPS.md) registry).
- **`AddConnection` happily joins a removed piece** (a tombstone is a valid index) and such a joint is never severed — fail-open, reachable the day anything adds bracing to a damaged wall. One `GraphValidation` row + a guard reusing `IsPieceRemoved`.
- **A pre-latched `FConnection` can be added through the front door** (`ApplyForce` then `AddConnection`): given, unstamped, invisible to the solver, counted by `NumConnections`. Matters for save/load; reject or accept-and-stamp, red test first.
- **`CheckCascadeCase` has a spec-vs-structure index desync one level up** — a fixture row `AddPiece` rejects would shift every per-piece assertion onto the wrong piece. Needs a decision on what such a row asserts.
- **Cycle-division rule absent** (a true voussoir arch still strands; DESIGN §5.1). Deliberately later; make the *reason* observable meanwhile. Now observable per acceptance row: cases 10 and 19 strand 3/6 **live** pieces — part of those two collapse verdicts is unroutability, not masonry — pinned by `FWallCase::StrandsToday` in `WallAcceptanceTest.cpp` (the other eighteen rows claim zero; old case 12's 11-strand pin went with its 2026-08-09 rewrite). Those pins go to zero when this rule lands.
- **Neither fuzz covers piece removal — the reason to wait has expired.** Extend `Structure.CascadeFuzz`: remove 0–2 seeded pieces before cascading, floor the removing-case count, assert (a joint touching a removed piece has given and carries no stamp) and the converse, plus stamps strictly increasing across a cascade–remove–cascade sequence. Oracle drops the node outright (including grounded-ness) where production severs — the divergence is the value.
- **`CascadeFuzz` doesn't check a joint broke in the *earliest* deserved pass** — sweep every joint in the pass-N graph and require over-capacity ⇒ stamped exactly N. Its own mutation needed.
- **`CascadeFuzz` mostly probes the obliterated regime** (median break 9.9× capacity; only ~20% below 2×). Biasing the mass table toward the marginal band is the cheapest improvement; it moves every reported distribution figure, so do it deliberately.
- **Neither fuzz asserts stranded-set equality both ways** — `GetPieceSupport` now exposes it; promote `STRANDING` from diagnostic to property, mutation to prove it bites (un-orderability mutation is recorded live).
- **The accumulation ordering contract is named in a comment and asserted nowhere.** Specified test: three bed joints with areas `{1.0, 1e-16, 1e-16}`, exact `==` against the ascending-order sum (the tolerance *is* the assertion); second row mixing PieceA/PieceB ends; prove by descending-order mutation.
- **The knot check is O(pieces × (pieces + edges)) per fixpoint pass** — Tarjan SCC + one reusable visited buffer is the known fix. **Re-measure the cascade by hand first**: the 1.2 s bottom-course delete predates the adjacency index (which took `SolveLoads` 33.6 → 6.4 ms) and nobody has repeated it, so it is unknown whether the user-visible Delete lag still exists. Nothing in the suite exercises `SolveAndBreak` at scenario scale.
- **Pass-bound observations unasserted**: inner fixpoint provably ≤ 2 passes (no seam to observe it); outer cascade measured ≤ 4 (generator property — pin only as a generous ceiling if ever).
- **`AddConnection` normal top-end hole** (~1e200 components) — lockstep change with the fuzz transcription; see TRAPS.
- ~~`ConnectionStrength.ShearCap`'s literal compressions~~ — CLOSED in the 2026-08-13 re-anchor red phase: the compressions are now derived from the bite point (`(cap − c)/μ` + margins) with fixture preconditions pinning the mean-basis profile values and the shear-governs condition.
- **`UtilisationTolerance`'s 1e-12 absolute floor** must stay below the smallest non-zero expectation (`Unbreakable`'s 1.96e-11) or that row can't fail.
- **An unsupported island reads zero on its internal joints** — "carries nothing" is ambiguous between given / grounded-both-sides / falling; a strain readout will someday need more than the force.
- **`StructureCascadeTest.cpp` hard-sets `BrickMassKg = 2.72`** — used only for grounded pads; two-line tidy when the file is next open.

### Rigid-block LP oracle (`Core/RigidBlock/RigidBlockOracle.*`, with the `FStructure` bridge in `Core/RigidBlock/RigidBlockBridge.*`)

- **THE OPT-IN SWEEP IS TWO TIERS SINCE 2026-08-16, AND THE TIERING IS ITSELF A ROT HAZARD** (TRAPS carries the hazard beside the `NonNullRHI` one). `OracleSlowSweep` is gone as a name; `OracleSweepFast` (**10 tests**, ~90 s) and `OracleSweepFull` (**5 tests**, ~22 min) share the `OracleSweep` stem, so one filter runs both — **measured, not assumed: 7 / 3 / 10 on 2026-08-16, 9 / 4 / 13 on 2026-08-18, and 10 / 5 / 15 on 2026-08-21 once the 0c/0d and deletion-ladder slices landed. `Run-OracleSweep.ps1` now ASSERTS those counts**, because its first version defined the full tier *as* a hard-coded bucket list, so a new solver-scale test joined no bucket and ran NOWHERE — in every mode including serial, under the command CLAUDE.md calls mandatory. A run that quietly does less is indistinguishable from one that passed**, and neither name contains `DestructionGame`. **`OracleSweepFull` is mandatory before any commit that touches the LP oracle, and before any commit at all if the solver changed**; the fast tier is for iteration and verifies nothing about the solver at scale. Per-test seconds, measured 2026-08-16 from the automation log's timestamps, so the next person can re-target the optimisation rather than re-measure it:

  | tier | test | secs (2026-08-16) | secs (2026-08-15) |
  |---|---|---:|---:|
  | FULL | `WallsAndLadders` | 478 | 455 |
  | FULL | `PhaseTwoMustNotRefuseTheCoveredOpeningFamily` | 455 | 432 |
  | FULL | `FeasibilityReformulationCost` | **313** | 344 (before the live-pose trim) |
  | FULL | `WarmStartAtWallScale` (new 2026-08-18; wall-01's three solves, one of which is the abandoned warm attempt plus its cold retry) | 61 | — |
  | fast | `OpeningMechanismLadders` | 22.4 | 22 |
  | fast | `PhaseTwoMustNotRefuseABoundedProblem` | 19.6 | 19 |
  | fast | `FreeEndHeightLadder` | 16.1 | 16 |
  | fast | `RepairedRegionalSandwich` | 9.6 | 9 |
  | fast | `SubUnityWallCertificate` (new 2026-08-16; 4.2 s of its 8.0 is the composite's one gravity-live solve, which buys the "how false" ratio) | 8.0–10.3 | — |
  | fast | `RegionalSandwich` | 7.2 | 7 |
  | fast | `WarmStartAfterADeletion` (new 2026-08-18; four rows at three solves each, the seam fixture and the one gravity-live row) | 5.2 | — |
  | fast | `OpeningStrengthProbes` | 4.1 | 4 |
  | fast | `RefusalNamesItsReason` | 1.0 | 1 |

  The two big rows read ~5% higher on the second day on the same fixtures — this machine varies by more under load than several fast rows cost, so quote the range rather than the last number.

  **What the same slice deleted**: `FeasibilityReformulationCost` re-solved corbel D, wall-06, wall-15 and wall-18 in the λ\* (gravity-live) pose, and all four λ\* are pinned in `WallsAndLadders`, which runs in the SAME tier — same producer, same cut, same bridge, same solver, so the same solve. Those four rows now carry `bSolveLivePose = false` (~33 s). **Wall-01 keeps its live solve deliberately** — the sweep excludes the 30-course walls on arithmetic, so that test holds the only pinned wall-01 λ\* in the project and its 10.88× headline; the 84-block gate fixture and both leaning stacks keep theirs too (the gate arithmetic, and the 30-course stack is the infeasible arm of the cross-validation). **Every DEAD solve was kept**, which is why the pooled early-exit pins (9,245 / 58), the eight-point block spread behind "the lever weakens with scale", the gate arithmetic and every verdict came back bit-identical. **The cost, recorded rather than glossed: the four rows' LIVE pivot pins (9,490 / 3,885 / 6,076 / 14,209) are now pinned NOWHERE** — `WallsAndLadders` pins λ\* and not pivots — and wall-06's 16.2× (the largest speedup in the set) becomes a cross-run reading. Cheap re-homing if wanted: `WallsAndLadders` already solves those four, so pinning their pivot counts there is a data change on rows that already run.

  **Safe to split across processes** (checked for `Scripts/Run-OracleSweep.ps1`): there is no `static` storage anywhere in `RigidBlockOracle.*` or in either sweep file, every test builds its own fixtures, and the solver carries no state between calls — the cost test's own EARLY EXIT SEAM row asserts that a second solve of an identical problem reports identical fields. No test depends on running beside another or on ordering. Give each process its own `-abslog`, and count `Test Completed` per log.

  **Queued and deliberately NOT taken. The reason they were held has EXPIRED (2026-08-18): warm starts were measured and refuted (PROMOTION_DESIGN §5.4 — worse with size, slower in wall-clock, unusable at wall scale), so nothing is coming to make these unnecessary. Re-judge them on their own merits, which means weighing ~150–250 s and ~141 s of saving against the discrimination each costs.** The two: (a) **use the feasibility pose on sweep rows where only the verdict matters** — worth ~150–250 s, but about half the expensive rows are the discriminating pairs where the λ\* *number* is the finding, so it is a per-row judgement and not a sweep-wide switch; (b) **trim the covered-opening test's two bracket neighbours** — ~141 s, but the bracket is what proves the 8-cell rung is not secretly its neighbour (TRAPS N11), so the trim costs a bite-prover.

- **The solver performance roadmap** (user-directed 2026-08-12; preliminary sketch, each lever its own measured slice — never assume, measure):
  - *Oracle-side:* **partial/candidate-list pricing LANDED 2026-08-12** (rotating 384-column window + 12-deep queue ranked by static steepest-edge d_j/‖A_j‖; every queued candidate re-priced against current duals; full-sweep optimality proof; Bland fallback stands the pricer aside entirely). Measured: wall-01 answers for the first time — **λ* = 272.20, 58,605 pivots, ~4.2 min** (was cut off unanswered at 45+ min); the 8×10 fixture 4.7 s → 0.61 s with λ* bit-identical. **Pricing is finished as a cost driver (0.5% of wall-01's runtime)**; the measured profile is now BTRAN 44% / refactorisation 33% / FTRAN 18% per-iteration linear algebra × the pivot path. Known trade recorded: cheap scans lengthen the pivot path on some mid-size rows (wall-09 50→125 s while corbel D 80→9 s; net a wash) — **Devex/dynamic weights** are the roadmapped fix and were deliberately not bolted on untested. Still queued: **warm starts across sweep rows** (~10× pivot collapse expected); **presolve**; **Markowitz/fill-reducing LU ordering** (now material — refactorisation is a third of runtime); **the artificial pivot-out pass's scans** (now the dominant scan cost, ~64% on the 8×10 — its own test first); **RefactoriseEvery = 64** is untested as a tuning point and is 33% of runtime; the `FPartialPricer` Bland branch is no longer undriven (see the phase-2 entry below — the opening ladders enter it 170–358 times a solve), but no fixture is chosen to exercise its behaviour deliberately.
  - *Production-side, for evolution step 4's latency budget:* **regional decomposition** — ~~solve only the neighbourhood of a change; a deleted brick provably cannot affect masonry beyond a bonded distance~~ **that premise is REFUTED twice over**: thrust is not local (case 21's mechanism runs the full jamb chain to ground), and Slice 0b measured the two-sided sandwich meant to replace it certifying at 95–100% of the structure, costing 1.68× a global solve, with a pessimistic side that is not a bound (a false certificate, wrong by ~40×). Still the likeliest answer to 1,220-piece scenarios, but only in its repaired form — omitted weight kept as a dead surcharge, regions ground-anchored by construction — **measured 2026-08-15: certifies at 27.5% of a wall for 0.322× a global solve, so the lever is real; and measured again 2026-08-16, THE PESSIMISTIC SIDE IS NOT A BOUND** -- the gating sub-1.0 fixture (a wall carrying a leaning chimney) certifies falsely at four of six strip widths, so a closed sandwich is a heuristic and §5.6's fail-closed fallback is mandatory on every region solve (PROMOTION_DESIGN §5.3's box, §12 D2‴; numbers pinned in `OracleSweepFast.RigidBlock.SubUnityWallCertificate`). **What is now owed, and it is bigger than the surcharge was**: holes (2) and (3) are one repair -- the region must keep the interface joint AND the charged component's internal joints in the problem, because the fixture shows a strip certifying while charging the failing material IN FULL (a surcharge carries a force and a moment and no STRENGTH); hole (1) needs the carried-set rule to stop reading "has a ground path" as "is not carried"; and the area-proportional split across multiple interfaces is still arbitrary. **A cheaper alternative is now the front-runner and needs no trustworthiness at all**: D2″(iii)'s probe-once-else-solve-globally.

- **THE ABANDONED WARM ATTEMPT'S REFUSAL REASON IS DISCARDED AT THE SEAM, AND A DESIGN ATTRIBUTION RESTS ON IT** *(deferred 2026-08-18, from the warm-start review; needs a `RigidBlockOracle.h/.cpp` change so it is its own slice)*. `SolveRigidBlock`'s fallback wrapper runs the warm attempt, and on a refusal throws away `Warm.WhyNot` and `Warm.Refusal` entirely before returning the cold retry's result. So `WarmStartColumnsAccepted == 0` is all a caller ever sees, and that zero is **overloaded across three distinct events** — the seeding refused the hint, every hint was repaired away, or the attempt refused and the wrapper forced zero — which is exactly the discrimination the field's own header says it exists to make. What is measured but unpinnable through the seam: **wall-01's mapped basis refuses with `NumericalFailure` at pivot 64**, a sentence PROMOTION_DESIGN §5.4 now carries and only hand instrumentation has ever seen. **Half of it is closed already** — `WarmStartAfterADeletion`/`WarmStartAtWallScale` declare `bWarmAbandoned` per row and pin the wasted work as `WarmPivots − ColdPivots == 64`, which distinguishes "abandoned" from "accepted nothing" from the outside and asserts the retry was a genuine fresh cold solve. What is owed is the *reason*: carry the discarded attempt's `EOracleRefusal` on `FOracleResult` (an `AbandonedWarmStart` field, or the existing `Refusal` set with `bAnswered` true), then assert it on the wall-01 row. Red for the slice: that row asserting the enumerator, which today has no way to be anything but `None`. Not taken now because it is production code and would leave the opt-in tiers red.
- **`PivotsToFirstFeasible` is not carried across the warm-start fallback retry** *(2026-08-18, same review; harmless today)*. `SolveRigidBlock` adds the abandoned attempt's `SimplexIterations`, `PricingColumnScans`, `BlandDegenerateEntries` and `PhaseOnePivots` onto the cold retry's, but leaves `PivotsToFirstFeasible` as the retry's own value — so a warm row that met slice 0b's early-exit pins would under-report by the wasted pivots. No fixture reads that field on a warm path today (`FeasibilityReformulationCost` is all cold), which is why it is a note and not a defect. Fix it in the same slice as the entry above; the two are three lines apart.
- **A DELETION-CAUSED sub-1.0 fixture is still wanted** *(2026-08-16, deferred deliberately)*. The chimney wall is infeasible BEFORE its deletion, which is sound as a refutation (the pessimistic side's claim is unconditional) but weaker as evidence about how a region lever would fail in production, where the LP is asked after a player's action. The stronger fixture is the one PROMOTION_DESIGN names first: a wide opening near the λ\* = 1 crossing (case 21's family, ~40--45 cells) with the deletion in a JAMB -- the strip through the jamb is ground-anchored and stands while the cover it no longer supports fails across the span, outside the strip. Not built here because it is 500+ blocks, tens of seconds per dead solve and knife-edge to bracket, which is minutes in the slow tier for a second instance of a refutation already in hand. Build it if the region lever is ever revived. And **dual-simplex re-solves** (after a deletion the old solution stays dual-feasible, so a re-solve is a handful of pivots — this plus warm starts is the standard incremental-LP playbook for "re-solve on every player action").
  - *Deliberately avoided:* **threading the scans** — parallel floating-point sums are order-dependent and bit-reproducibility is a load-bearing contract; the speedup would be spent buying determinism back.

- **The case-21 family's MEAN-BASIS mechanism split is open** *(measured at the 2026-08-14 flip; feeds the step-4 brittleness precondition)*: at the mean strengths the LP's λ* moved ×3.128 (17.2389), NOT the ×4.5 a pure jamb-cohesion pricing predicts; the abutment sensitivity collapsed (j=3/j=2 1.339 → 1.038, j=4/j=2 1.698 → 1.051); the zeroed-cohesion probe keeps 27.7% of λ* and the modified wall now STANDS (4.768 — the AgreeFalls arm is dead; bond tension 0.70 and friction carry it); the span dominance survives (parity step 0.8875, matched-span identity 1.00088, span exponent ~2.4). So the 2026-08-13 attribution (jamb bed cohesive shear ≈ the whole capacity) is a characteristic-era fact, and the mean-basis capacity mix — dominant-but-not-whole cohesion plus something abutment-blind — is unattributed. The chain-vs-bearing split probe (below) should be designed against the MEAN data when picked up.
- **Two LP cross-fixture identities died at the flip and are open re-derivations**: the free-end 7×10/7×20 λ* height-identity (256.820018 both → 629.199/297.997, ratio 2.1114 — the mean-basis binding constraint scales with stacked load; what binds is unestablished) and the wall-13/14 projection-pair cross-validation (LP ratio 2.7777 matched production's 2.7786 to 0.03% at the characteristic data; at the mean data the LP reads 1.856 while production keeps 2.7786 — the two rungs' binding constraints diverged). Both are pinned at their measured values; naming the constraints belongs with the step-4 design.
- **The PhaseTwo 99er's certified window is single-reading since the flip** (89.898933555823774 ±2e-5; the characteristic-era two-pivot-path pair predates the re-anchor) — re-pair it the next time a pivot-path change gives a second certified reading.
- ~~**Case 22's pre-cascade worst is un-instrumented post-flip**~~ — **CLOSED 2026-08-14 and the knife edge was an artefact.** `SolveWallCase` now solves the cut structure once before handing it to the cascade, every catalogue row PRINTS the reading, and case 22 PINS it (`FWallCase::PreCascadeWorstToday`, plus a separate "must be past 1.0" row so a Collapse reached from under capacity is a loud failure). Measured **2.9133264370386338** at c3/36.5-c4/36, not the predicted 8.2241/7 = 1.1749 — **wrong by 2.5×, and for exactly the reason the prediction flagged**: the axis flipped. Decomposed from the joint's own pre-cascade force, it is **Mohr-Coulomb SHEAR** (0.35540386 MPa of compression across, 3.3985494 MPa along, capacity 0.9 + 0.75 × 0.35540386 = 1.1665529, quotient exact to seventeen digits); flexural tension is EXACTLY zero (kern-clamped) and compression reads 0.0710808. So the "one modest routing change flips this verdict" alarm was the /7 talking, and the true margin is 2.91×. Collapse unchanged: 3 passes, 316 down. **Owed, small**: asserting that axis needs the pre-cascade FORCE carried on `FWallResult` alongside the reading — three doubles, no extra solve — the way `SpanIsReadInTheJointNotInTheOutcome` now asserts the pair's.
- **RULED 2026-08-13 — case 21 keeps its Collapse verdict; the LP's dissent is booked as a rigid-plastic scope limit** (user; the full ruling entry with the measurement record is DESIGN §8). The LP's λ\* = 5.511 stand is bought entirely by the jamb bed joints' cohesive shear (zeroed → 0.785, `AgreeFalls` on the *modified* wall), mobilised in full as rigid-plastic analysis must while real bond is brittle. Nothing in the suite changes shape: the dissent stays pinned `OracleStandsProductionFalls`. **The consequence lands on the promotion design** — a brittleness answer (first-crack/ductility discount, cohesion-mobilisation rule, or explicit scope carve-out) is now a step-4 precondition before an LP verdict overrides a cohesion-bound catalogue row. Open threads that stay measurements, not blockers: the thrust-line shape, the ≈1.5-vs-5.5 hand-price gap, and the chain-vs-bearing split below.
- **Specified follow-up — the chain-vs-bearing split of the cohesion probe**: the probe zeroes all 36 jamb bed joints at once, including the two bearing courses at the top of the jamb, so it prices the chain as a whole and cannot say whether the bearing or the run to the ground is the tighter link. One more row of the same shape (zero only the bearing-course bed joints, or only the below-bearing run); the probe test's header records this as owed.
- **Specified test (from the 2026-08-13 pricing review) — assert each rung's clear reveal from the built geometry**: extend `BuildOpeningLadderWall` (or a support helper reading the built `FStructure`) to compute the clear reveal on the *top opening course* — `min(centre.X − halfLength)` right of the opening minus `max(centre.X + halfLength)` left of it — against the oracle `(OpeningCells + 1)·22.5 − 21.5`, minus 22.5 when that course is odd. Per rung: 406.00 (18@s=2), **383.50 as an equality pin on 18@s=1 AND 17@s=2** (the matched-span experiment's whole premise, which today lives only in prose — prose that carried an off-by-half until review), 361.00 (17@s=1). **Extended 2026-08-13 to the phase-2 test's span ladder**, whose header states the same quantity and whose three rungs sit at s=3 (top opening course 5, ODD, so a half cell under the even-course formula): 158.50 (8 cells), 181.00 (9), 203.50 (10) — the first draft of that header had all three a full cell too wide, which is the second time this arithmetic has been got wrong in prose and the reason it wants a test. Bite-prover: widen the cut's ±0.25-cell margin to ±0.75, or flip the parity term's sign in the oracle.
- **Specified test (same review) — the cohesion selector's set derived twice**: the Z-below-cover selector and an independent X-outside-the-reveal-band selector must choose the *identical joint index set* (not merely the same count) on every rung of the family — two derivations of one set cannot agree with a wrong selector the way a hand-derived count can. No extra solve. Mutation: shift the X window one cell.
- **Recorded gap — the three probe production-invariance ratios have no bite-prover**: all four probe rows build the identical fixture and `AdjustProblem` cannot reach `FStructure` by type, so those ratios can only fail on production nondeterminism (mutation N4 provably does *not* fire them — identical piece counts cancel the scale factor). Defensible as a guard against a future structure-side override; the mutation that would drive them is unrecorded. Likewise the (17,s=1) rung's pins are covered only by symmetry with the recorded rung-flip (N6) — the mirror flip was not run.
- **Not reproducible without re-instrumenting**: the deleted active-set probe (which contacts sit at tension/friction/crushing bounds at the optimum — 108/149/0 of 266 on wall-21). Re-taking it means re-instrumenting the LP extraction; worth building as a real `FOracleResult` field if the thrust-line shape ever needs settling.
- **THE CASE-22 REFUSAL RECORD WAS STALE, AND SLICE 0a's DIAGNOSIS REPLACES IT (2026-08-15).** Everything that named 218 / 290 / 371 blocks as the refusing fixtures — this file, PROMOTION_DESIGN §6/§10/§11 R4, and `WallAcceptanceTest.cpp`'s case-22 block — was measured on **2026-08-13, at the characteristic strengths**, one day before the mean re-anchor changed every number in the LP's data. Re-measured at today's profiles (whole family: one course below, a three-course opening, eight courses of cover, two-cell jambs, widths swept):

  | opening cells | blocks / joints | λ\* | pivots | secs |
  |---|---|---|---|---|
  | 2 | 74 / 174 | 537.60206692224722 | 6,578 | 4.9 |
  | 4 | 92 / 218 | 384.91922849080379 | 10,455 | 15.7 |
  | 5 | 101 / 240 | 298.0111013112051 | 13,997 | 26.1 |
  | 6 | 110 / 262 | 236.97383758407059 | 16,353 | 39.5 |
  | 7 | 119 / 284 | 190.53602223331399 | 17,983 | 52.3 |
  | **8** | **128 / 306** | **REFUSED** | 17,316 | 40.1 |
  | 9 | 137 / 328 | 127.7264657209236 | 22,753 | 89.7 |
  | 10 | 146 / 350 | 105.64778136561756 | 24,635 | 106.9 |
  | 12 | 164 / 394 | 75.120451403407529 | 29,261 | 157.5 |
  | 14 | 182 / 438 | 55.975345056998457 | 30,848 | 166.0 |
  | **16** | **200 / 482** | **REFUSED** | 6,244 | 18.3 |
  | 18 | 218 / 526 | 33.651690962702006 | 37,705 | 254.9 |
  | 26 | 290 / 702 | 15.622461777098057 | 60,721 | 555.1 |
  | 35 (**acceptance case 22**) | 371 / 900 | 8.4149459982219277 | 88,810 | 1196.9 |

  So **all three named walls answered, case 22 included**, and what refused were two smaller members at **128 and 200 blocks** — a third of the size the blocker was thought to start at, and deeper inside any region the §5.3 sandwich would use. **BOTH ARE CLOSED as of 2026-08-15** (the `RelativePivotTol` move; the two REFUSED rows above now read λ\* = 155.63200561101226 at 21,394 pivots and 43.132916253688222 at 34,825). **What was actually re-measured after the fix is five of the fourteen widths** — 7, 8, 9, 16 and 35 — all unmoved, with the 7- and 9-cell neighbours and case 22 bit-identical in λ\* *and* pivot count; the other nine widths (2, 4, 5, 6, 10, 12, 14, 18, 26) were not re-solved after a constant that widened the ratio-test floor 100×, and saying "every other row is unmoved" overstated it. The honest claim: **the two known holes are closed, five widths are confirmed unmoved, and nothing guards against a new hole at an unswept width** — a hole is a property of a pivot path, not of a width, which is exactly what the ±1-cell neighbours answering demonstrates. The table stays because it is the family's measured curve and the new test's bracket rests on it.

  - **THE ARM WAS `NumericalFailure`, and BOTH recorded suspects are refuted.** Instrumented then reverted: phase 2 returns NumericalFailure from the periodic refactorisation inside the pivot loop, `Factorise` finding an LU pivot under `SingularPivotTol` in the last three columns of the basis — 128 blocks: position 4018/4021, col 6119, pivot 4.3758616521949456e-12; 200 blocks: position 6322/6325, col 9639, pivot 4.928959752317684e-12. Bit-reproducible. **The spurious-unbounded arm fired on no solve in the whole sweep** (it stays unreached-rather-than-sound, and this family is not the fixture that licenses reinstating its repair). **NB-7 is not it either**: the artificial pivot-out pass took no pivot under 1e-6 absolute or 1e-7 of its own column's largest on either wall — worth tightening on its own merits, still not what refuses these.
  - **TWO ONE-CONSTANT EXPERIMENTS BOTH REACHED THE ANSWER**, which is what made this a defect rather than fail-closed correctness, and **the first of them is the fix that landed**. `RelativePivotTol` 1e-11 → 1e-9: 155.63200561101226 and 43.132916253688222. `RefactoriseEvery` 64 → 16: 155.63200342392039 and 43.132560867194137. The two agree to 1.4e-8 and 8.2e-6 on visibly different pivot paths (20,606 vs 21,394 pivots), both clear the 1e-6 verification gate, and the 128-block reading lands on the family's own L^-2.4 interpolation (~156). **Why the pivot floor and not the cadence, and not Markowitz**: `Refactorise` builds the LU from the ORIGINAL sparse columns and never from the eta file, so an LU that goes singular is a statement that the BASIS COLUMN SET the ratio test chose is numerically dependent — and det(B) is the product of U's diagonal, so no elimination ordering can make a dependent set's pivots large. That refutes the third reading (the fixed left-to-right factorisation order) as a *cause* — fill-reducing ordering stays on the roadmap as a speed lever with its own slice. The cadence lever would have paid everywhere (refactorisation is already a third of runtime) for a defect that lives in one place.
  - **THE HAZARD IS UNCHANGED AND STILL UNWATCHED**: case 22 answers after **88,810 pivots against a `MaxPivots` of 100,000** — 89% of the termination cap. Re-measured after the fix (2026-08-15, throwaway harness, 1,198.6 s): **bit-identical**, λ\* = 8.4149459982219277 and the same 88,810 pivots, so no pivot on that wall's path fell in the newly-refused band. It remains one pivot-path change away from a *different* refusal and nothing in the suite watches it; a watching row costs 20 minutes a run, which is why there isn't one.
  - **BITE-PROVERS RECORDED 2026-08-15** (signatures in TRAPS): the N10 rung-flip on each new refuser (8-cell built as 9-cell → **4**: block, joint, certified window, bracket; 16-cell built as 14-cell → **3**: block, joint, window — that rung carries no bracket by design) and the S9 pattern against the constant that moved (`RelativePivotTol` back to 1e-11 → **exactly 2 assertions in 1 test**, the two MUST-ANSWER rows, while `PhaseTwoMustNotRefuseABoundedProblem` stays green — the new value extends the 2026-08-13 fix rather than replacing its evidence). **Still owed**: tighten the two certified windows from ±1e-4 to the file's usual ±2e-5 — the shipped solver's readings ARE the `RelativePivotTol` variant's, so the pair is still one diagnostic variant plus one shipped path, and the wider window is honest until a third path exists.
  - **A REFUSAL NOW NAMES ITS ARM, and nothing reaches a phase-2 arm any more.** `EOracleRefusal` + `RefusalText` + `FOracleResult::Refusal` landed with the fix, and the 8-cell wall reported reason 5 (`PhaseTwoNumericalFailure`) before the fix went in — the instrumented diagnosis reproduced by the shipped taxonomy. **The cost, and it is an owed replacement**: with both walls answering, no fixture this project owns reaches `PhaseOneFailure`, any `PhaseTwo*` arm or `VerificationFailure`, so those five enumerators are proven pairwise DISTINCT but are reported by no solve — nothing would catch a site setting the wrong one. `RefusalNamesItsReason`'s retired canary section says so in place. The natural home for the replacement is the specified LU/eta factorisation fuzz below, which is already the only planned exerciser of `ESimplexEnd::NumericalFailure`.
- **Deferred from the 2026-08-15 Slice 0a review, with the mechanisms named rather than one-lined** — the first three are the sharp ones:
  - **(N3) The verification gate clamps away the exact error mode the raised floor makes 100× larger.** `RigidBlockOracle.cpp` (~the `StructValues` extraction, `FMath::Max(0.0, State.XB[Row])`). Worked path: an entering column whose FTRAN image maxes at 3e7 now has `PivotFloor = 1e-9 × 3e7 = 0.03` where it was 3e-4; a ratio-test row with coefficient 0.02 is skipped, the step taken elsewhere drives that basic value to −0.02·θ. The final refactorisation *does* recompute every basic value from the original RHS, so a genuinely infeasible basis shows its negative — and the clamp replaces it with zero before the verification loop sees it. That loop's tolerance is `1e-6·(1 + |rhs| + Σ|terms|)`, ≈0.1 on a wall equilibrium row whose terms sum to ~1e5, so a clamped −0.02 contact force passes silently and λ\* is reported from a basis that is not feasible. **Not demonstrated to occur** — hence deferred — but the entry above that credits "the 1e-6 verification" with judging this is partly wrong, because the clamp prevents it seeing the thing. Closure is measurement, not argument: see the specified test below.
  - **(N4) NaN reads as "the wall collapses" rather than as a refusal**, pre-existing and in the function Slice 0a edited. If any basic value is NaN, `FMath::Max(0.0, NaN)` returns NaN (UE's `Max` is `(B<A)?A:B`), every `bViolated` comparison against NaN is false, so the loop finds no violation, `bAnswered` stays true, λ\* clamps to NaN and `OutcomeOf` reads `NaN >= 1.0` as false → **Falls**. The upstream guards are correctly spelled so reachability is low, but the last line of defence is spelled in the fail-OPEN direction, against house rule. Fix is `if (!(… <= Tolerance))`.
  - **(N1) `PhaseTwoRefusalFor` is new branching logic with no test**, and the canary that exercised its `NumericalFailure` arm was deleted in the same slice. Its `default:` maps `Optimal` — and any future `ESimplexEnd` — to "the basis went singular": fail-closed, but a new enumerator would be mis-reported silently. This is the sharper half of the deleted-canary cost: the five orphaned *enumerators* are proven distinct and their loss is recorded, but the *mapping function* is what nothing watches, and closing it does not need the LU/eta fuzz.
  - **(N2) The cheap half of `RefusalNamesItsReason` is stranded in the opt-in group.** It runs in **1.0 s**, yet lives in the opt-in sweep (`OracleSweepFast` since the 2026-08-16 tier split, which does not close this — a fast tier is still opt-in), so the pairwise-distinctness sweep — the only guard on the taxonomy — never runs in the default suite. Its validation-reason section also re-does what `RigidBlockOracleTest.cpp`'s eight poison rows already sweep in the default suite, where asserting the enumerator is one more line across every validation branch rather than one.
  - **(N5) The control fixture claims a size it does not pin** — `RefusalNamesItsReason` builds case 21's rung and its message says "83 blocks" with no block/joint assertion. Verified 83 by hand in review (a pleasing independent cross-check that the builder reproduces case 21), which is exactly why it deserves the one-line size pin that TRAPS N10/N11/N12 exist to enforce elsewhere.
- **Specified tests from that review, so they are not lost to a scratchpad**: (1) **`PhaseTwoRefusalFor`'s mapping** — expose it (or a thin `RefusalForSimplexEnd`), assert all four mappings including the `default` arm, default suite, microseconds; bite-prover: swap two arms → 2 assertions. (2) **Move taxonomy distinctness to the default suite** and fold the validation-reason assertion into the existing poison-row table. (3) **"The certified optimum's basic values are non-negative"** — carry `double WorstNegativeBasic` on `FOracleResult`, set from `min(0, XB[Row])` at the extraction point, pin it across the slow group. Oracle: the final basis solves `B x_B = b` exactly, so any negative component certifies that the reported λ\* comes from an infeasible basis. Bite-prover: set `RelativePivotTol` to 1e-6 and watch the field grow; reverting to 1e-11 must leave it at today's value. **This is the only thing that turns N3's argued bound into a measured one.** (4) **A fixture that genuinely reaches a phase-2 arm** — the owed replacement for the deleted canary, and the nearest cheap candidate is `IterationCap`, not `NumericalFailure`: case 22 answers at 88,810 of 100,000 pivots, so a slightly wider member of its family hits the cap deterministically. Cost that before committing to the LU/eta fuzz as the only home.
- **The 99- and 107-block phase-2 refusals are FIXED (2026-08-13) by ONE change, and the eight-course-cover family's smaller pair by the SAME constant one notch further (2026-08-15 — the block above).** Driven by `OracleSweepFast.RigidBlock.PhaseTwoMustNotRefuseABoundedProblem`, the fix is a single addition to `RigidBlockOracle.cpp`'s ratio test: a candidate pivot must clear `RelativePivotTol` (1e-11 then, **1e-9 today**) of the ENTERING COLUMN'S OWN largest magnitude as well as the absolute `PivotTol`. The test was measured accepting pivots of 4.7e-9, 6.9e-9, 6.7e-9 and 1.6e-8 out of columns whose FTRAN image ran to 1.7e5–3.0e7 — relative pivots of ~1e-16, i.e. the rounding of the solve that produced the column — and the basis those built went singular (LU pivot 1.3e-13). Both refusers now answer with certified optima (λ\* = 37.932472960794129 and 9.3587305816217992), the j=4 canary was PROMOTED (the abutment ladder's 2× test now exists: 1.698 against 2.00 bearing-proportional and 1.00 flat), and **every other fixture in the slow group came back bit-identical** — no window anywhere was re-pinned. Bite-prover: reverting the floor to the absolute tolerance reproduces the original red exactly (836 and 671 pivots, both refusing).
- **The spurious-unbounded mode is NOT closed in general — it is only no longer reached on these fixtures**, and this is the correction to the first hand-off's claim. A mirrored column (the LP splits shear into p − q whose columns are bitwise negations, so with one basic the other's exact reduced cost is ZERO) can price at ~1e-9 of drift inherited from the dual solve, FTRAN to a single negative entry, and the ratio test then finds nothing: phase 2 returns Unbounded and the oracle refuses **a problem it has effectively solved**. What the pivot floor does is keep the basis well-conditioned enough that ‖y‖ stays small and the drift stays under `CostTol`, so the arm is not reached; it does not make the arm sound. A repair for it (refactorise, re-price, set the column aside, take the next candidate) was written and then **removed after measurement**: with the floor in place, reverting the seam leaves both fixtures answering with bit-identical λ\* and identical pivot counts, so no failing test drove it. ~~**This is a live candidate explanation for the case-22 family's continuing refusal**~~ — **REFUTED 2026-08-15**: instrumented across the whole eight-course-cover sweep (fourteen solves, two of them refusals), the `Leaving == INDEX_NONE` seam fired on **none** of them. The arm stays unreached-rather-than-sound and still has no fixture that drives it; the family's refusals are `NumericalFailure` instead (see the correction block above). It remains the first thing to instrument for a *future* refusal, just not this one.
- **Three measurements from the phase-2 diagnosis, recorded so nobody re-instruments for them** (also at the `RelativePivotTol` comment): (i) making the Bland branch's candidacy tolerance RELATIVE is **inert** — scaling `-CostTol` by the largest term in the reduced cost's own dot product left every pivot count on all six ladder rungs bit-identical, because those terms are ~1.2e-9 at the failure; (ii) the ~1e-9 of drift is **inherited from the dual solve**, not from cancellation in the column — ‖y‖∞ = 6.7e6 at the refusal and ε·6.7e6 ≈ 6.7e-10; (iii) a ‖y‖-scaled OPTIMALITY tolerance would sit at ≈6.7e-3 and was **rejected**: a simplex that stops while 1e-3 improvements remain reports a feasible point with λ\* too LOW, and the post-solve verification checks admissibility rather than optimality, so it would certify that happily. (iii) is the reason any future repair of the seam above must be conservative in shape.
- **One bounded cost the pivot floor buys** (belongs with the "λ could read LOW rather than refuse" seam below): a coefficient in (`PivotTol`, `PivotFloor`] is now skipped by the ratio test, so the chosen step can overshoot that row and drive its basic value slightly negative, which `ApplyPivot` clamps to zero. The magnitude is bounded by θ·`PivotFloor`, the final refactorisation recomputes every basic value from the original right-hand side, and the 1e-6 verification judges the result — in the direction verification can see, which is admissibility. **The 2026-08-15 move to 1e-9 multiplies that bound by a hundred**, which is why the whole slow group is re-run against the 1e-6 gate whenever this constant moves; it was, and every row held (nothing re-pinned, and every fixture outside the two refusers came back bit-identical).
- **Deferred from the 2026-08-13 phase-2 review, each its own small slice**: (NB-3) the ratio test's new dense max scan over `EnteringW` is foldable into `FtranColumn`, which already walks the vector — worth it only at wall-01 scale (58,605 pivots × ~13k reads); (NB-5) the abutment ladder's j=5 rung is already built by the phase-2 test and reads λ\* = 11.115044681607193, so j=5/j=2 = 2.0169 would extend the relation series 1.339 / 1.268 / 1.188 the next time that ladder is open; (NB-7) the artificial pivot-out pass still accepts an entering column on the ABSOLUTE `PivotTol` alone (`RigidBlockOracle.cpp`, the `EnteringAbs = PivotTol` scan) — the same class of defect as the one just fixed, on the pass that picks the basis every phase-2 solve stands on; belongs beside the existing FTRAN-vs-BTRAN pivot-out entry and needs its own red step. **Measured NOT to be what refuses the eight-course-cover family (2026-08-15)**: instrumented to print any pivot-out pivot under 1e-6 absolute or 1e-7 of its own column's largest, it printed nothing on either refusing wall — so this stays a latent hole wanting a fixture, not a live cause; (NB-8) the promoted j=4 window is ±2e-5 where the two certified readings differ by 5.3e-7, so it is ~40× the demonstrated cross-path spread — a future re-pin inside it is the window doing its job, not a regression.
- ~~**The oracle's refusal string collapses three outcomes into one**~~ — **CLOSED 2026-08-15**, driven by `OracleSweepFast.RigidBlock.RefusalNamesItsReason`. `EOracleRefusal` (seven enumerators, `None` at zero), `FString RefusalText(EOracleRefusal)` and `FOracleResult::Refusal` are live, and every refusal site sets the reason and the sentence in ONE statement (a `Refuse` lambda) so the two cannot drift apart. An enumerator rather than a richer string because §5.6 and §11 R4 need production to COUNT refusals by reason. **The three phase-2 phrases keep "phase-2 simplex failed" as their PREFIX and the verification phrase keeps "failed verification"** — deliberately, because `RigidBlockOracleSweepTest.cpp`'s two dead relation branches (~lines 545 and 576) recognise a refusal by those literals, so the split names the arm without renaming the event. Phase 1's three non-optimal ends stay one enumerator: its objective is bounded below by zero, so it cannot be unbounded, and no fixture reaches any of them. **What the measurement contradicted**: the unbreakable λ-cap fixture does not refuse at all — the cap row bounds its LP, so it terminates Optimal at λ\* = LambdaCap and `ValidationCatalogue` pins it as an *answer*; it reaches an unbounded ray only under mutation M5, and a mutation is not a fixture. Only ONE arm is reachable from a fixture today (validation), which is why distinctness is asserted over the enumerators — see the owed replacement in the correction block above.
- **The Bland fallback IS driven now, and by ordinary fixtures** — the note above that "no fixture reaches such a streak" was retired by measurement on 2026-08-13: the opening-ladder rungs enter it in bulk (`BlandDegenerateEntries` 170–358 on healthy answering rungs, 296 on the repaired 99-block one). What is still unwritten is a fixture chosen to exercise the branch's own behaviour rather than one that happens to reach it, and the `PricingCost` rows' "asserted zero" claim is now known to be a statement about those two fixtures only.
- ~~**The LU/eta property fuzz is specified and unwritten**~~ — **WRITTEN 2026-08-27, `DestructionGame.Oracle.RigidBlock.FactorisationFuzz` (default suite, `Tests/FactorisationFuzzTest.cpp`).** Landed with Slice 1: the factorisation cluster was lifted verbatim out of `RigidBlockOracle.cpp` into a production seam `Core/RigidBlock/RigidBlockFactor.h` (`FStandardForm`, `FFactorColumn`, `FBasisFactor`, `SolveBasicValues`, `FEta`, `FRevisedState`) so a separate TU can drive the real objects; the move is answer-preserving (OracleSweepFull 5/5 bit-identical). The fuzz: independent dense Gaussian elimination (a genuinely different algorithm, not a mirror), random **orthonormal** bases (cond≈1 so the honest residual sits at rounding), FTRAN and BTRAN residuals ≤ 1e-10·(1+‖·‖∞) pinned pre-eta AND after each of k∈[1,12] eta replacements, duplicated-column bases refused, and ONE `FRevisedState` reused across 300 cases (stronger than one `FBasisFactor` — exposes workspace leakage). Green on arrival, proven to bite: the three mutations fire by ~10 orders (BTRAN etas forward, FTRAN drops row perm, BTRAN drops output perm) — magnitudes 1.79/1.51/1.42 rather than the 2026-08-12 review's 7.2e4/17/2.0 because the orthonormal generator works at O(1) scale, and review reproduced the BTRAN-order mutation to the exact residual+seed. **Still deferred (this fuzz does NOT close it):** the fuzz drives `FBasisFactor`/`FRevisedState` directly, so it exercises `Factorise` returning false (the singular duplicate-column path) but NOT `ESimplexEnd::NumericalFailure`, which is a simplex end-state needing a full `SolveRigidBlock` run — that remains the owed replacement for the deleted canary (connects to the "no fixture reaches a phase-2 arm" residue), its own slice, and the nearest cheap driver is still `IterationCap` on a wider case-22 member.
- **The artificial pivot-out's ratio uses the FTRAN value where its tolerance checked the BTRAN dot** (~`RigidBlockOracle.cpp:1467`) — in exact arithmetic they agree; a cancellation could put a near-zero pivot into the eta file. Cheap fail-closed re-check on `|EnteringW[Row]| > PivotTol`, own red step (a branch nothing drives yet — the review's finding 4).
- **One seam where λ could read LOW rather than refuse**: `ApplyPivot` clamps any negative XB to zero, sound only when a leaving artificial's basic value is exactly zero; phase 1's infeasibility gate is loose at 13k-row scale (~13 absolute with the λ-cap RHS). Reachable only via dead-load problems (two fixtures today) on a knife-edge-infeasible problem; every constructed path ends in a verification refusal, but under-report is the one direction verification can't catch. Sentence owed at the site; watch it if dead-load fixtures grow.
- **`Result.SimplexIterations` excludes the artificial pivot-out pass's pivots** — deterministic and harmless, but header pivot tables are not total counts. Note when next touched.
- **Slice 6d — the per-block dead/live split. ASSEMBLY SPLIT DONE (2026-08-29); the wall-15/16 bridge posing is the remaining follow-on.** wall-15/16 read identical λ* (868.62 to one ulp) under uniform gravity because gravity scales BOTH the surcharge's demand and the pre-compression that steadies it. `FOracleProblem::bGravityIsLive` (global) + `FOracleAppliedForce::bLive` (per applied force) already existed; 6d adds **`FOracleBlock::bLiveGravity`** so a SUBSET of self-weight can be live. The assembly (`RigidBlockOracle.cpp` ~:1548) now routes a block's weight to the λ (live) column iff `Problem.bGravityIsLive || Block.bLiveGravity` — the per-block flag can only ADD liveness (no tri-state; that is exactly what the surcharge split, the shed's roof-as-surcharge, and step-7 impulses need). INERT for every untagged fixture (the branch reduces to the current arithmetic in both live paths: `true||false==true`, `false||false==false`), so every existing λ* pin is bit-identical (`OracleSweepFull` 5/5). Driven by `Oracle.RigidBlock.DeadLiveSplit.LiveSurchargeHingesTheHeader` — a hand-built lever (header carrying k surcharge blocks) whose SPLIT pose (surcharge live, rest dead) reads the finite tipping margin λ*=6/k (k=1→6, k=6→1 stand, k=12→0.5 falls) while the UNIFORM pose reads the LambdaCap — the wall-15/16 "identical today, must separate" phenomenon in miniature. Suite 197 = 193 green + 4 standing reds. **Reviewed, no blocking findings** (OR branch byte-identical to a globally-live block — same sign/λ-column/row/magnitude/zero-moment; bit-identical by construction since nothing but the test sets the flag; λ*=6/k independently re-derived as genuine no-tension tipping; the min-violation readout assembly has no gravity-liveness branch so `bLiveGravity` is correctly inert there). **FOLLOW-ON DEFERRED to step-7/shed (the OUTCOME red + the production posing path):** the per-block capability is done and proven, but HOW a structure designates its live loads (a per-piece live-load designation on `FStructure` + the bridge translating it to `FOracleBlock::bLiveGravity`) is a production-API design decision better made WITH its real consumer — step-7 impulse posing and the shed's roof-surcharge posing — than built speculatively for one sweep fixture. When taken: a posing path so the BRIDGE tags wall-15's six projecting-header courses (course 3, `DestructionScenarios.cpp:482`) as live surcharge, and a sweep test asserting wall-15/16 SEPARATE in λ* under the live posing (both moving off 868.62; wall-15 the lower margin) — belongs in the opt-in sweep, must NOT break the existing uniform-gravity identity/ratio pins (`RigidBlockOracleSweepTest.cpp:2733-2760`/`:2949-2971`, which pin the uniform identity that stays true). The toy lever (`DeadLiveSplit.LiveSurchargeHingesTheHeader`) stands as the capability's proof until then; the deferral is a scope call (decide-don't-ask), not a gap in the split itself.
- **wall-09's 0.985 knife-edge is now pinned absolutely** (`SpanIsReadInTheJointNotInTheOutcome`: 1e-9 pin + explicit `< 1.0` flap watch, 2026-08-12) — what remains unwatched is only the sweep-side relation flip if it ever crosses; the catalogue-side pin fires first.
- **Specified test — the free-end reading decomposition** (from the classification review): in `FreeEndHeightLadder`, assert production's worst reading decomposes into brick-weights on an *unrelieved* half seat — `WorstUtilisation / 0.058203838191552663` = 15.867 at 10 courses, 33.635 at 20, in tight windows, with the 20-course count exceeding the 10-course by the pinned 2.1198 ratio. Pins the absolute readings (currently a 2.1× drift passes silently), names the worst joint arithmetically (its identity is currently unestablished — it is provably NOT the composite-relieved corbel-root seat), and fails if the composite-depth walk ever starts relieving it. Bite-prover: scale the half-seat anchor 1% in the test.
- **Fixture preconditions don't pin the profile shear ceilings** (dry stone 6.0, mortar 1.3 `MaxShearStrengthMPa`) — both feed real LP rows, neither binds in any validation row (verified in review), and a retune lowering either could make a ceiling govern a hand answer mysteriously. Two one-line `TestEqual`s.
- **Cheap missing validation row**: a *point contact* under an *eccentric* load must read λ* = 0 (no moment capacity) — the catalogue covers point-contact-centred and finite-contact-eccentric, not the combination.

### Layout producer

- **`ContactsOf`, the RunningBond oracle in `Tests/LayoutTest.cpp` (~line 360), still computes its *area* as `Reach − Distance`** while building its rectangle from bounds — the formula the 2026-08-08 MakeInterface fix retired from production. Latent-only: `RunningBond` emits no containment topology, and the divergence direction now fails *loudly* against correct production rather than agreeing with a wrong one. Same one-line change, whenever that sweep gains mixed sizes.
- **`Core.Layout.InterfaceFuzz` is specified and unwritten** (from the MakeInterface review): random touching pairs, oracle `HalfExtent[a] == 0.5·min(2eA, 2eB, eA+eB−|Δc|)` per in-plane axis — a different expression for the same set, so it cannot agree with a wrong producer; generator snaps ~30% of offsets to exactly `0` or `±|eA−eB|` to strike the switchover; measured at 400k pairs (old formula over-reports 301,493 of them, worst 12,210×); mutation that proves it bites: restore `Reach − Distance`. Two cheap example rows also wanted: a containment where the separation axis is X (all three current rows are Z-separated), and strengthening the inside-both-boxes property with the min-form oracle (today it transcribes the fixed code's own expression, so it can only catch the rectangle decoupling from the bounds again).
- **`DBL_MAX` extent → infinite area accepted** (finite, so it passes `IsFinite`; the *computed* area overflows). Needs a finiteness check on `AreaSqCm` after the multiply, own red row. Not reachable into a structure (`AddConnection` rejects non-finite areas).
- **`RunningBond` neither clears nor refuses a non-empty out-layout; `AdoptLayout` has the identical question** — answer the two together, same policy.
- **`RunningBond` ignores `AddPiece`/`AddConnection` returns**, and an infinite `BrickSizeCm` passes the `!(x > 0.0)` guard (`+inf > 0` is true) — desyncs boxes from handles, then `JoinIfTouching` indexes `Boxes[INDEX_NONE]` and aborts the run. Fix: `IsFinite` on the spec guard + honour `LayBrick`'s handle; red rejection row first.

### World binding and world

- **`FStructureBinding::AddPiece` ignores the structure's refusal** — appends unconditionally, arrays off by one forever. The next change here; one red row (NaN mass → refused, nothing appended, `INDEX_NONE`).
- **`FStructure::HasSupportAnswer` has no direct test** — the uncovered row worth writing: a removed piece inside the extent answers TRUE (documented staleness), currently unobservable only because `ApplyResults` skips removed pieces first.
- **`ABrickActor::SetPieceRef` accepts a re-point** — a second call is always a bug (click one brick, delete another). Prefer a one-way setter with the test asserting the ref unchanged, over ensure-matching.
- **A structure can be built and never destroyed** — no `Destroy(StructureId)`, spawned actors leak on rebuild. The scenario switcher hits this immediately.
- **`TracePiece`'s null-`Find` and removed-piece steps are written but undriven**; two cheap red rows.
- **`BuildLayout` crashes on fewer boxes than pieces** (reads past the end before `AdoptLayout` can refuse), **and a refused build leaves already-spawned bricks standing** — one guard at the top, refusing before the spawn, closes both.
- **`BuildRunningBond`'s three refusal paths are written, unverified.**
- **Brick scale has no degenerate-mesh guard** — a zero-extent mesh gives infinite scale; if written, fail closed, spelled `!(Size > 0.0)`.
- **The scenario cut's batching is unfalsifiable** until a catalogue row cuts ≥ 2 bricks (must arrive as a test change, not a data edit).
- **A mistyped `?Scenario=` is never surfaced to the player** — `OptionNamedNoScenario` is computed and read by nobody but a log line.
- **Degenerate `HoldSeconds` means "never runs"** (timer refuses rate ≤ 0/NaN) — a condemned structure stands forever. Guarded by the catalogue sweep (`> 1.0`, finite); left.
- **The label banner covers the top courses of the tallest structures** — geometrically certain (height-governed rows get 10% clear above; the banner is taller). First user-visible defect in the levels; own slice; candidate fixes: bottom-anchor the banner (cheap) or have `ViewpointFor` reserve headroom (moves every swept inequality).
- **The label says nothing about the hold on the ten no-cut rows** — the one-invariant-line claim in `World.Scenarios.Label` must be restated first; no quiet edit.
- **The scenario banner's draw has no test** (content swept 162 ways; the blit unseen) — same standing gap as the piece-menu panel.
- **The framing aspect is a hard 16:9, no viewport ever measured** — ultrawide players get a wrongly framed structure; the two framing tests pin 16:9 as the unmeasurable-case answer, which any fix must keep.
- **Corbel case F has no absolute anchor** — 2.68132 lives only in comments; the only family row far above 1.0, so the only one that would catch a scaling solver change. Cheap root-utilisation anchor, own red step.
- **`World.Scenario.GameModeBuildsTheWallOnBeginPlay` asserts the model, not the outcome** — the fourth integration test (tick, then assert bricks unmoved and kinematic), grouped into the existing scenario world for cost. Mutation (r) exists.
- **`Visual.ScenarioLevelScreenshots`**: row floor still 9 while `Content.ScenarioMapsExist`'s is 29, and its "eighteen files" comment is stale (58 now). Cost note: 29 PIE worlds.

### Piece menu presenter

- **Rank-0 product decision** (selection meaning on cascade-release) — see the top of this file.
- **The compact toggle is an undiscoverable double-click** — needs a model-side caption string (red test), then an `SButton` in the title strip.
- **A short selection leaves dead panel** (158 px compact / 262 full) — the list's 190 px cap is reserved, not taken; making panel height a function of selection count is legal (no feedback path) but a new behaviour, held against `PanelDoesNotGrowWithTheSelection`.
- **Compact keeps the corner full was opened at** (192 px short of the right edge) — re-homing needs a real "has the player moved it" signal; the obvious flag is set by the toggle gesture itself (Slate sends a press before a double-click and synthesises cursor moves).
- **Compact still lays out the empty headroom-scale rows** (~27 px) — suppressing needs a model-side "is there a bar" field (the no-logic-in-Slate rule forbids the branch).
- **A viewport resize is only corrected on the next grab**; a resize that puts the title strip off screen leaves nothing to grab. Cheap fix if it bites: hold the last known viewport size.
- **`Delete` still shifts sideways when the readout collapses** (bounded, safe direction — dead click at worst); **a fourth pick pushes Delete down 22 px** (follows the player's own click; watch if selection ever grows from anything else).
- **"broken (went with a removed piece)" wording is a live lie** (`Core/PieceMenu.cpp:1196`) — the cascade is on the wire (ranked item 1 above), so a joint broken *under load* gets the removed-piece wording today. Needs its failing test (a cascade-broken joint presented, wording asserted wrong); zero-cost interim: drop to bare "broken".
- **A live brick with no joints has no model-side sentence** (the floater case) — a widget would have to branch; `CountText`'s empty-case precedent says give it one.
- **`ForceN` discards the sign** — `Role` recovers bed-above, not head-joint push-vs-pull; a destruction game whose readout can't show tension is worth settling before the shape sets.
- **The entry list carries no liveness** (`FInspectorPieceEntry` presents removed/foreign/live identically) — decide with the widget; it is the surface where the rank-0 decision becomes legible.
- **`PieceMenuInspectorForSelection()` is private** — no test can assert the controller feeds the widget the right inspector; making it public is the highest-value small test left here.
- **`OnHoverPiece` line-traces every moved-mouse frame, unmeasured** — do not pre-optimise (throttling reintroduces the staleness `Triggered` was chosen against); measure in the same session that judges the overlays.
- **`ChoosePieceMenuRow` dereferences a null `Action` unguarded** (crash is the loud direction; hand-built rows only); one red `World.Choose` row when closed. Same family: **`ShowPieceMenu` aliasing its own `ShownPieceMenuRows`** — copy-to-local first is the worked pattern already in `ChoosePieceMenuRow`.
- **Costs recorded, unpaid**: `bIsLivePiece` costs a full `InspectPiece` per entry per hover; the inspector is built twice per build; `SetInspectedPiece` unguarded for idempotence.
- **Unobserved on a screen** (the press-Play list, item 4 of the ranked list): shader compilation for all three overlay materials, hover routing on the opening frame, entry-click swallowing, panel legibility at scale, magenta-vs-cyan legibility. The recommended real-RHI test is the engine's own `ShouldCreateNaniteProxy == false` on a registered in-world brick (boolean, no baseline) — not a screenshot; if a screenshot is ever written it must be a four-state difference test, never absolute RGB.

### Core systems designed, not built

- **Profiles as editor data assets** (`FConnectionStrength` is a plain struct; adding a row needs a recompile) — the remaining gap to "materials are data".
- **Wood, added exactly once** to prove data-drivenness (deliberately not added with the profile library — adding it beside the calibration data would spend the proof).
- **Connection-to-material pairing** — weakest-link: joint fails when connection gives OR bond peels (`connection × material BondFactor`) OR material fails. Build when the second material exists.
- **Damage/force manager; piece-size floor modes 1/3 then 2; piece timestamps** (DESIGN §2).
- **Shear direction is discarded** (`FConnectionLoad::Shear` is a magnitude) — matters when anisotropic wood arrives, not before.
- **In-game builder choosing connection types**; **a real brick mesh** (one-line asset-path change; keep the Nanite rule in TRAPS in mind).

## Housekeeping

- **`BeamAcceptanceTest` no longer needs to sidestep containment** (the MakeInterface fix landed): moving its piers inboard would be the suite's only contained bearing exercised through a real solve. Its own slice — the readings are anchored to the current geometry and must be re-derived, not nudged.

- **Source comments cite the deleted design docs by name** (`ARCHING_DESIGN.md` ×80, `MOMENTS_DESIGN.md` and `COMPOSITE_DEPTH_DESIGN.md` ×28 each, `PROJECT_REVIEW.md` ×7, `REAL_WORLD_CHECK.md` ×2). Those now point at git history and at the matching DESIGN.md section. Not urgent — the reasoning they cite is preserved verbatim in git — but when a cited comment is next touched, repoint it. Find them: `grep -rE "ARCHING_DESIGN|MOMENTS_DESIGN|COMPOSITE_DEPTH_DESIGN|REAL_WORLD_CHECK|PROJECT_REVIEW" Source/`
- **`Scripts/Convert-CommentBlocks.ps1 -Check` exits 2** on `StructureCascadeTest.cpp:707` (backslash-continued comment diagram — the script is behaving correctly; see TRAPS). Either rewrite the diagram line or teach the script to pass such files through — exit 2 on every run trains people to ignore the check.
- **The anonymous-namespace sweep has no check behind it** — the discipline is prose in header comments; a `-Check` script over `Tests/` ("no anonymous namespace, no file-scope declaration") would turn an engine-source build error into a named refusal. `Convert-CommentBlocks.ps1` could plausibly host it.
- **`Tests/CorbelScreenshotTest.cpp` has not been through review-expert** — a second `NonNullRHI` visual harness written inside a reporting task. Review targets: the duplicated brick-spawn recipe, the hand-written release rule (reproduces the `HasSupportAnswer` guard), the 15 m Y-offset applied to spawn only.
- **`PresenterHeadroomScale`'s doc comment is stranded** at `Core/PieceMenu.cpp:604`, eight helpers above its function. Cosmetic.
- **CLAUDE.md's testing section owes the oracle sweep's two tiers, and the edit is the USER's to make** (same ownership as the two open calls below — an agent may not treat a relayed instruction as consent to edit CLAUDE.md). The 2026-08-16 split documented itself in TRAPS, in this file and in the two test-file headers, which is everything except the place someone reads first. Wanted, tightly: the two commands (`Automation RunTests OracleSweepFast` / `…OracleSweepFull`, and `…OracleSweep` for both), their costs (80 s / ~19 min), and the rule that **`OracleSweepFull` is mandatory before any commit touching the LP oracle and before any commit at all if the solver changed**. Without it the fast tier is the one people will run and the full tier is the one that rots.
- **CLAUDE.md needs two edits the moment the first functional test lands** (the `+`-joined filter; "a new test is just a new file" scoped to arithmetic tests). Not yet due — no functional test exists.
- **Promote the integration-test entry rule into CLAUDE.md?** Open call, **owner: the user** (it earned its place by catching a real bug; the case against is CLAUDE.md staying short). Likewise **the visual-run command as Run B** in CLAUDE.md's testing section, owner: the user.
- **Prune `.claude/settings.local.json`** — a dozen stale single-use permission entries.
- **Case 12's standing-weight bracket (550–1,050 N) has a generous floor** — the reviewer's most conservative recount reads ≈ 363 N. Verdict-insensitive (still ≥ 4× on honest thrust; bond alone covers the kern demand); tighten the row comment's bracket if that row is next touched.
- **`SolveKeyOf` in `WallAcceptanceTest.cpp` prints `-0.0` and `0.0` as distinct cache keys** for identical geometry — one redundant solve at worst, never a wrong answer. Normalise the zero if the key is next touched.
- **The load fuzz's inline unbreakable strength literal** (`Tests/StructureFuzzTest.cpp`, grep `1.0e9`) — one-line swap to `DestructionProfiles::Unbreakable`; input change, do it deliberately.
- **Unqualifying the 11 redundant `LayoutTestSupport::`/`StructureFuzzSupport::` qualifications** left from the collision workaround — harmless; belongs to whoever next owns those files.
- **Watch for the transient** `joint #4 must be carrying a bend … it carries 0.000000` (fired once 2026-08-06 during overlapping builds, never reproduced) — a moment going to exactly zero in a corbel fixture is what a mis-firing arching cap looks like; if it recurs, treat as signal.
- **`FStructurePiece` should take a material rather than a bare mass** (every caller re-derives from density) — small change, do it with the brick-actor work it serves.

# Current State

**Living document.** This is the running TODO list for the project, not a historical record.

- **Finishing something? Remove it from this file** as part of that work. A completed item left here is worse than no list.
- **Deferring something? Add it here** before moving on, with enough context to act on it later.
- Check this file at the start of a work session and again before calling anything done.
- Items are deliberately unnumbered where possible — they churn constantly.

**This file assumes you already know the design.** [INDEX.md](INDEX.md) says which document owns what and where to write a new fact; [DESIGN.md](DESIGN.md) is the authority on the model, the constants, the anchors, the evolution path and the dated rulings; [TRAPS.md](TRAPS.md) holds the live footguns and the mutation registry; [PROMOTION_DESIGN.md](PROMOTION_DESIGN.md) owns evolution step 4; [SHED_PATH.md](SHED_PATH.md) sequences the north-star brick-shed /goal (the layer above step 4 — the phased route through wood, tension, member failure and 3D to a playtest-verified shed, with the standing rulings); [LEVELS.md](LEVELS.md) indexes the thirty-five playable levels. Settled reasoning lives there and in git -- not here.

Last updated: **2026-09-16** — the interactive build/destroy SESSION is DELIVERED and growing (UI-6 joint choice and corner joints CR-1/CR-2a/CR-2b landed 2026-09-16 — the corner-return snap and the Rotate chip; corners are PLAYABLE, every player build is 3D-flagged and the LP bridge poses a planar build in 2D under that flag — DESIGN §8 2026-09-16 — so a straight wall no longer pays the 3D cost; the cursor-driven ghost — no click needed — is the newest slice); it was in progress (the toolbar model, its component wiring, the playable `Lvl_Build` plot, the Slate strip and the controller's mode dispatch have landed; the details window's hover peek (S8) and the strip's remaining polish (S1/S2, the C-list) are the open items (the S6 cursor + RMB look, the S7 identity line, the one-based course convention, the chip styling and the S12 load overlay landed); the session proof harness `Visual.SessionScreenshots` is committed under "Build-mode" below; the screen's design is [SESSION_UI_DESIGN.md](SESSION_UI_DESIGN.md)). The 2026-09-01 review's ranked items 1–13 are all complete, committed and reviewed, and item 12's regional collapse prover landed through slice 5 (see [REGIONAL_PROVER_PLAN.md](REGIONAL_PROVER_PLAN.md) and the DESIGN §8 ruling dated 2026-09-03). What landed lives in its own home, not here — rulings in DESIGN §8, step-4 measurements in PROMOTION_DESIGN, footguns and mutation signatures in TRAPS, and the numbers themselves in the test headers that pin them. This file is the list of what is NOT done; if you want the history, read git.

## Current state

- **SOLVER SPEED at building scale — measured, not yet acted on (2026-09-18).** The warehouse
  course-37 playtest ([Experiments/WarehouseCourse37/REPORT.md](../Experiments/WarehouseCourse37/REPORT.md))
  timed one `FStructure::SolveAndBreak` on the 5,581-live-piece building at ~201 ms. Where it goes,
  and the three ways to cut it, are in the report's "How it could be made faster". Ranked: (1) the
  148 ms is the load solve run 3× over the WHOLE structure from scratch — a region-seeded re-solve
  keyed to the removed pieces would replace three whole-structure passes with one local one; (2) the
  51 ms regional-prover LP certified nothing because reachability had already released its whole
  seed neighbourhood — skip the pose when every seed piece is already Falling; (3) `SolveLoads`
  rebuilds the per-piece joint adjacency (~3,600 small allocs) every call, replaceable by
  count-then-fill (the code comment already names this). None change the verdict. A break decision
  over more than a frame now logs at `LogDestructionSolve` Log level. NEW production observability:
  `FStructure::GetLastSolveAndBreakReport()` / `GetLastSolveLoadsProfile()` — per-pass and per-
  fixpoint-iteration timing and counts, pinned by `Core.Structure.SolveAndBreakReportMatchesTheCascade`.
- **Suite:** 361 tests / 357 green / 4 red (measured 2026-09-16, after the click's re-preview (xvii-f)) (`DestructionGame.Core.BuildMode.*` + `DestructionGame.World.BuildMode.*` + `DestructionGame.Core.SessionToolbar.*` — joint inference, the snap solver's full slice-1 vocabulary (brick next-course/same-course, timber centered-on/lintel-bearing/edge-flush, free) with occupancy filtering, the `PlacePiece` placement API, `BuildDemoBuilding` (a scripted wall+plate that stands), three screenshot renders, the UI-1 incremental world place (`BeginBuild`/`PlaceBuildPiece`), the UI-2 `PreviewBuildPiece` ghost query, the UI-4a `UBuildModeComponent` (ghost tracks the preview, a click commits), the UI-4b `UpdatePreviewFromRay` ray→build-plane seam, and the session slice-2 wiring (piece kind/course → material, extent and plane; Snap-vs-Free placement; pose-derived grounded; `CancelBuild`) — added 2026-09-03/04/15). The four reds are the standing design-limit anchors (`Acceptance.Beam.MidspanCarriesTheMembersBendingMoment`, `Acceptance.Wall.Catalogue` row 21, `Acceptance.Wall.StackBondColumnShearIsHeightIndependent`, `Core.Structure.CorbelStepsBeforeTensionWins`). `Oracle…IsPermutationDeterministicAtScale` now runs in `OracleSweepFull`, not the default filter. Never "fix" a standing red by weakening its assertion — each goes green only at its named evolution step.
- **Authority boundary:** the rigid-block LP is the break authority ≤ 200 blocks (`EquilibriumGateBlockCap`); above it the per-joint router (`BreakByCapacitySweep`) decides. Every realistic structure — the 442-block realistic shed, the ~1,200-block default wall — is on the router. The router is dimension-agnostic (routes load down bed joints, reads normals directly); the 3D LP bridge is only consulted below the cap.
- **The WAREHOUSE level and the LAYOUT FILE (2026-09-18) — landed OUTSIDE the three-agent cycle, by owner direction.** `Lvl_Warehouse` / row `warehouse` loads `Content/Layouts/Warehouse.json` (5,612 pieces, [WAREHOUSE_DESIGN.md](WAREHOUSE_DESIGN.md)), written by `Scripts/New-WarehouseLayout.ps1`. The owner waived test → dev → review for it ("You can skip the test dev review on this"), ruled the solver off-limits ("if it falls, that is ok"), then ruled **buildings are DATA, not C++ ("Pivot to data driven")** — so `Core/LayoutFile.{h,cpp}` (JSON boxes + materials, fail-closed parse, one-piece-per-line serialize) and `Core/ContactSweep.{h,cpp}` (the general bucketed pair sweep through `JointForContact`) exist, and the hour-old C++ warehouse builder was deleted. Tests written alongside, not through the agents: `Core.LayoutFile.{RoundTrip, RefusesBadInputAndLeavesNothing}` and `Content.LayoutFile.WarehouseLoadsAndIsSound` (count, materials, envelope, 3D, no overlaps, nothing floating). OWED: (a) a review-expert pass over LayoutFile/ContactSweep when the owner wants the cycle back; (b) the SAVER from the build session — a Save chip / console command writing the current build through `DestructionLayoutFile::SaveFile` to `Saved/Layouts/<name>.json`, and a way to load one into `Lvl_Build` — this is the half the owner's "players will build without C++" needs; (c) MEASURED 2026-09-18 by `Visual.ScenarioLevelScreenshots`: the warehouse STANDS AS LAID — 5,612 pieces, 15,042 joints, 0 breaking passes, 0 pieces lost, worst movement 0 cm 180 frames after the hold; LoadMap 1.5 s; the file parses and sweeps in ~106 ms — so the owner's "if it falls" case did not arise. The Destroy-mode playtest was DONE 2026-09-18 (owner-requested): [Experiments/WarehouseCourse37/REPORT.md](../Experiments/WarehouseCourse37/REPORT.md) — deleting course 37 across the back long wall (31 pieces, the upper sill course) through the player's own click-select + menu-Delete drops the whole upper back wall, 718 pieces released, all by LOAD-PATH LOSS (0 joints over capacity, 0 felled by the prover): removing a whole course severs the wall horizontally and everything above is a disconnected island. The break decision took ~201 ms (148 ms in the load solve run 3×, 51 ms in one regional-prover LP that certified nothing); (d) explicit joint overrides in the file (`"joints": [{a,b,profile}]`) so a screwed cleat like the realistic shed's porch can be data too — today only swept passive joints are expressible, which is why the sheds keep their builders; (e) `+DirectoriesToAlwaysStageAsUFS=(Path="Layouts")` is in DefaultGame.ini but no packaged build has verified the file ships.
- **The 2026-09-01 review's items 1–13 are COMPLETE** (committed; CODE rulings in DESIGN §8, the regional prover in REGIONAL_PROVER_PLAN.md). Do not re-narrate them here — the follow-ups they left open are below.
- **The realistic-brick shed /goal is MET** (built, stands, collapses correctly, coloured, rendered and shown). The remaining north-star polish is brick-resolution walls, load-bearing corner joints and a per-scenario camera azimuth — captured under the open items below.
- **NEXT FEATURE (agreed 2026-09-03): interactive BUILD MODE** — place/snap brick and timber with automatic logical joint formation, then destroy. Full spec + approach (brain-first, TDD) in [BUILD_MODE_PLAN.md](BUILD_MODE_PLAN.md). Slice 1 = the snap-candidate + joint-formation brain.

### The four standing reds — what each anchors

| Red | What it anchors |
|---|---|
| `Acceptance.Wall.Catalogue` — **row 21** | The one INVERTED red: a ruled COLLAPSE that the model (and the LP, λ\* 17.24) now STANDS — its composite re-sectioning reads the cover ~3.1× under the hand σ. `DropsToday = 0`, `StrandsToday = 0` pinned; the caption admits the disagreement. Goes green only at the composite/f_x2 rework or when the ruling is accepted. |
| `Acceptance.Wall.StackBondColumnShearIsHeightIndependent` | The model routes a hanging column's whole load to its foot. Needs the "load sheds as it rises" design pass before code. Asserts a ≥ 1.15 head-over-bed margin at 12 and 20 courses so it cannot go vacuous. |
| `Core.Structure.CorbelStepsBeforeTensionWins` | A finding: the counterweight buys a corbel nothing — C and D cross at 124 steps in CRUSHING (masonry behind a joint never reaches it). **RE-EXAMINED 2026-08-29: a ROUTER-READOUT gap, NOT LP tension.** It drives `SolveLoads`/`GetConnectionUtilisation`, whose downward-only routing never carries a counterweight's tension to the root joint. The LP break authority already credits tension (step-5 done), so this will NOT green from LP tension work — only if the router readout is taught to credit the counterweight, or it is re-based to the LP break behaviour (which already works below the cap). |
| `Acceptance.Beam.MidspanCarriesTheMembersBendingMoment` | The MECHANISM red: midspan reads \|M\| = 0 under 2.32× mean C24 bending capacity, because the true member-bending moment needs member failure (evolution step 6). Deliberately kept as step 6's driving red — first-crack delivered the outcomes by the glue-line route, but the member itself parting is step 6. |

## Open follow-ups from the completed review (items 8, 12) and model fidelity

- **Item 12 — regional collapse prover follow-ups** (also logged in REGIONAL_PROVER_PLAN.md). The prover is a grounded-boundary region LP that upgrades an above-cap router *stand* to a proven *fall* in the disturbance neighbourhood (one-directional: proves collapse, never standing — a sound no-op on today's scenarios). `SetRegionBlockCap` is settable; **default is 200** (the owner's "match 200" reach — briefly lowered to 48 as a latency stopgap, then raised back once grow-from-modest made cap 200 cheap; see the grow-on-contact item below). Follow-ups:
  - **Grow-on-contact (slice 3): LANDED 2026-09-03 as grow-FROM-MODEST.** `ProveRegionalCollapse` no longer poses the greedy cap-ball; it floods a MODEST initial region (~16 blocks) from the seed and grows only as the proof demands, so every pose is mechanism-sized even with a large cap as the ceiling. After each solve: an INTERIOR fall (mechanism bounded by genuine `bIsGrounded` foundations) is stitched; a fall that touches a cut-artifact grounded boundary (pinned by the flood ring, not real earth) RE-FLOODS from the moved set at a doubled budget — dropping any disconnected standing component the modest flood happened to admit (a grounded island) and re-spending the whole budget on the collapse; a pose that does NOT fall grows a bounded SPECULATIVE search from the boundary, capped at `min(cap,48)` — a blind flood must never balloon toward the cap (a large STANDING pose is the single most expensive LP; that is what a wandering search on a big structure hits). Monotone; terminates at interior / cap-bound / fixpoint / iteration bound. `GetLastRegionalProblemBlockCount` = final pose |R∪B|. **Latency goal met:** the cap-200 corner-hang (`Acceptance.Shed.ThreeD.RealisticBrickShedCornerHangFallsWithWeakPerpends`) went from >25 min (cap-ball) to **0.53 s** — its per-pass mechanisms are 1-3 blocks; the old cost was the cap-ball first pose, not the mechanism. Driven by `RegionalProver.GrowOnContactReachesPastAStandingBranch` (leaning stack + disconnected 20-brick grounded island, both seeded, cap 44: modest greedy would fell {7..29}, grow-from-modest fells the whole {1..29}). All six RegionalProver invariants stay green (the chain fits inside the speculative ceiling / reaches the cap via contact re-flood, so posed counts still match the greedy reference). Oracle/bridge untouched. Shipped default cap RAISED BACK TO 200 (the owner's original "match 200" reach) — grow-from-modest makes the cap-200 corner-hang 0.53 s, so 200 ships cheap; the speculative ceiling keeps a no-fall search bounded at min(cap,48) regardless.
  - **3D min-violation READOUT parity (item-8 residue):** the 3D *gate* got the shear-cap + first-crack rows but the *readout* `SolveMinViolationReadoutThreeD` did not — so a below-cap 3D bonded bending joint reports Utilisation against the plastic envelope the gate no longer allows (~4× too comfortable at e=3h), and a high-compression sheared 3D joint reads against unbounded c+μσ not the truncated f_v,max. OVERLAY-ONLY (observability estimator, never the verdict — collapse unaffected); folds into the already-deferred 3D-readout stub (`MomentUuCm` hardcoded 0, minimax deferred to E2). PICKUP: mirror both rows into `SolveMinViolationReadoutThreeD` (its layout has NO leading lambda column — `Base = 6·ContactIndex`, so first-crack `Base = 6·(4J+corner)`); drive with a 3D analogue of `FirstCrackReadoutTest` (a `SetThreeDimensional` eccentric-self-weight corbel, one bonded bed joint NormalZ=1, asserting reported Utilisation == first-crack demand `(−mean+3·bend)/(f_t·Conv·A)`, not plastic) plus a high-compression sheared companion asserting shear Utilisation against the truncated f_v,max. (Non-blocking nit: the 3D gate's first-crack row carries a `< Uncapped` guard the 2D gate/readout deliberately omit, DESIGN §8 residue-c — benign, they coincide for any real profile; recorded so the four assembler blocks don't drift silently.)
  - **Shed-corner-hang oscillation:** dev's observation needs INSTRUMENTED reproduction on the actual shed (the unit-scale re-hold case is provably impossible).
  - **Block coarsening** (E4) — only if a finer shed exceeds the cap.
- **Corner-hang model fidelity:** the router over-collapses the weak-perpend corner-hang (~93 pieces) because it routes the deep-beam arch over a full-width opening through perpend cohesion/tension (the re-seat hangs the panel on perpend shear), so debonding the perpends guts it — where a TRUE compression thrust-line arch would stand the corner-undermined panel independent of perpend bond. A separate model enrichment; the regional prover does NOT address it (the prover only adds collapse). Render DELIVERED 2026-09-03 (the collapse frame — 17 cut → 93 lose the earth — via a temporary `shedrealistic` cut repointed to the courses-6/7 back-wall band, reverted after; not a catalogue level, so it exists as a one-off frame, not a standing scenario).
- **Pre-existing oracle characteristic (low priority):** at ~30 blocks the whole-structure LP's own VERDICT is not fully permutation-robust (Falls vs a fail-closed decline under different construction orders; documented in `OracleMechanismMultiModeDeterminismTest`). Sound because a decline fails closed — its *correctness* is permutation-robust, only its *availability* is not.

### Non-blocking review hardening follow-ups (fold in later)

- **Item-1 readout:** (a) `ReadoutSolvedOnceTest`'s absence-assertion does NOT discriminate a transposed `if (bSeveredThisPass)` guard — the per-pass `ConnectionReadoutCache.Reset()` at `Structure.cpp:2450` launders it, so both assertions pass on the two-load-path fixture; a "pass-1 Falls-sever → pass-2 Stands" fixture WOULD pin the terminal-pass choice, and the test header's rationale overstates this — correct it when that fixture is added. (b) a genuine below-cap collapse reads the min-violation readout ABSENT on every survivor joint (a floated released body poisons the whole-structure readout LP) — decide as its own slice whether a collapse should surface per-joint strain (overlaps "unsupported island reads zero" below).
- **Item-3 overturning gate** (full detail in DESIGN §8's 2026-09-02 ruling): (a) the only theoretical anchor-safety hole — the gate tests the piece's OWN CoM, not the resultant including the load/moment it receives from above, so a compression-only N≥2 body held down by an eccentric load on its stabilising side (a counterweighted cantilever) would be spuriously over-felled. Dormant (no fixture; below the cap the LP handles it). Honest fix: test the received resultant, or promote such a structure below the cap. (b) the tension clause spares on `f_t > 0` capability, not magnitude/position — a token Nail spares a body the real load might overturn (conservative; self-corrects when the capacity sweep latches the inadequate tie). (c) the AABB-vs-convex-hull approximation under-fells a diagonal compression-only arrangement no fixture exhibits.
- **Item-4 arch-thrust axis:** (a) in-plane arches are bit-identical ONLY while their abutments are coplanar in Z — a multi-course opening whose two ends spring from different course heights would now read a horizontal (not vertically-tilted) thrust. Intended (more correct) but UNTESTED; a fixture would pin it. (b) `Arch.SpanCm` still measures the full 3D `.Size()` while the axis is horizontal-only — harmless while coplanar, silently diagonal otherwise; reconcile if a non-coplanar arch is ever built.
- **Build-mode joint inference (`BuildMode::JointForContact`, landed 2026-09-03):** (a) `DestructionShed3D::BuildRealistic`'s own hand-authored per-joint profile choices are NOT yet routed through the function — its internal sweep still keys on profile/material IDENTITY (`&Timber`) while `JointForContact` keys on `bCompressionDominant`. They agree on every axis-aligned contact today; the first tension-capable non-masonry material (steel, per the `bCompressionDominant` doc) would make the two disagree (sweep says Mortar, inference says DryStone) for the same contact. Wire the builder onto the function when a test drives it, and delete the duplicate branch. (b) `JointForContact` fails OPEN on a degenerate normal — a zero vector returns `GeneralPurposeMortar` (the strongest bond), a non-finite one returns Perpend; the snap solver's four sites are now on the BOXED overload (which fails CLOSED to Head on a degenerate normal), so the 3-arg fail-open is UNREACHABLE from the solver — delete the 3-arg overload or keep it documented — adopt the house `!(x > 0)` guard style (return DryStone/Perpend, the weak default) when the solver first emits a COMPUTED normal. (c) the 3-ARG discriminator is pinned only on axis-aligned normals (the BOXED overload's is pinned tilted both ways since CR-1); add a tilted-normal pin (`(0.8,0,0.6) → Perpend`, `(0.6,0,0.8) → Mortar`) when the solver emits non-axis normals.
- **Build-mode snap solver (`BuildMode::SolveSnapCandidates`, `Core/BuildMode/SnapSolver.*`, full slice-1 vocabulary landed 2026-09-03/04: brick next-course + same-course, timber centered-on + lintel-bearing + edge-flush, free):** hardening owed, none yet driven by a failing test. Edge-flush is X-FACES ONLY (near +X/-X, sign-nearest) — no Y-edge, no multi-edge (a later slice). A brick-sized timber block now yields FIVE candidates from one neighbour (three brick-labelled misnomers + centered + edge-flush) — see (ii). (i) the snap-radius guard `if (Offset > SnapRadiusCm)` is NOT NaN-safe — a non-finite neighbour centre makes `Offset` NaN, which passes as in-range and yields a candidate at a NaN pose; adopt `!(Offset <= Radius)` + an `IsFinite` guard on the input centres (cf. `Layout.cpp`). (ii) `IsBrickSized` (0.5 cm tol on full dims) rejects HALF BATS (the `EWallEnd::Flush` end brick) — header-oriented (rotated) bricks are ACCEPTED since CR-2a — and ADMITS brick-sized timber — which now satisfies BOTH `bPlacedIsBrick` (via `IsBrickSized`) and `bPlacedIsTimber` (via `!bCompressionDominant`), so a brick-sized Timber block emits brick-kind candidates (DryStone joints, a `BrickNextCourse`/`BrickSameCourse` misnomer) AND a `TimberCentered` candidate, all at distinct poses (they don't merge). Coherent but the labels lie; widen/relabel the gate when brick-sized timber matters. The `IsBrickSized` support gate ALSO now drops *contacts*, not just snap origins: a timber lintel's contact-based bearings only latch onto brick-sized supports, so a lintel over an opening whose jambs finish with HALF BATS (`EWallEnd::Flush`, the normal jamb) forms NO bearing on the jamb — the one place a lintel matters most. Widen the support gate (accept half bats / header bricks as bearing supports) when openings are built. (iii) `NearbyMaterials.Num()` vs `NearbyBoxes.Num()` mismatch is unchecked (a `check()`/OOB read) — guard on entry. (iv) grid provenance nit: `HalfStaggerCm` (was `HalfStaggerX`; the grid now steps along the neighbour's long axis) is derived from `Settings.BrickSizeCm` while `CoursePitchZ` uses the actual box extents — consistent only within the brick-sized gate; pick one source when it matters. (v) **OCCUPANCY — RESOLVED for snaps 2026-09-04 (owner-delegated ruling, DESIGN §8):** `SolveSnapCandidates` now DROPS any SNAP candidate whose box interpenetrates a nearby piece (strict `<` overlap on all three axes, distinct from a one-joint-gapped contact). FREE placement is honoured verbatim — the deliberate "place anywhere, even overlapping" escape — which also keeps `Candidates[0]` non-empty so `PlacePiece` stays safe. Still owed: (v-a) a `bRequestedPoseOccupied` (or equivalent) signal on the Free candidate / `FPlacementResult`, so the interactive UI can WARN when Free won only because every snap in radius was occupied (a scripted harness asserting `Kind` per step doesn't need it; the UI will). (v-b) a same-course occupancy pin (the driver only exercises next-course). (v-c) occupancy currently blocks a TOMBSTONED piece's cell too — resolve with deferred item (a) (exclude removed pieces from the nearby view) once the UI can remove. (vi) **RANKING POLICY is raw Euclidean distance** (and a TIE falls to emission order — the session proof frames show a plate asked for at x=22.5, equidistant from two course-1 bricks, landing at x=11.25 and overhanging the wall end; physically correct, cosmetically lopsided), so a cursor one course up but end-aligned can be pulled DOWN a course (a same-course pose can out-rank a next-course pose by pure distance); the "which snap wins" policy is unspecified and wants a rule (e.g. prefer the kind matching the cursor's dominant offset axis) once corner/timber add more kinds. (vii) **`Kind` on a MERGED candidate is order-dependent** — when a next-course pose and a same-course pose coincide (every brick in a wall past course 1, the correct bed+head joint set), the merged candidate keeps whichever neighbour `NearbyBoxes` listed first; the *joints* are correct and complete, only the single `Kind` label is ambiguous. Give a merged multi-kind candidate a truthful label (a `Wall`/`Mixed` kind, or make `Kind` a bitset) before any UI keys highlight colour off it. (viii) the timber-bearing Z-gap test uses `KINDA_SMALL_NUMBER` (1e-4 cm) while `IsBrickSized` uses 0.5 cm for the same "is this the support" question — a support 0.01 cm low is silently dropped as a bearing though it visibly carries the lintel; reconcile the two tolerances (a bearing-plane snap tolerance) when hand/imperfect placement is possible. (ix) the timber contact loop is O(neighbours) per timber candidate, O(n²) overall — irrelevant in the radius-bounded set (a few dozen bricks), noted so nobody rediscovers it as a surprise. (x) the timber-bearing contact test assumes LEVEL support tops (a brick one course up reads as intersecting, not bearing — the occupancy item (v)); revisit when stepped/gable bearings arrive.
- **Build-mode placement API (`BuildMode::PlacePiece`, `Core/BuildMode/Placement.*`, landed 2026-09-04):** turns a snap into a live `FStructure` piece with its joints formed via `MakeInterface`. Deferred, none yet driven by a failing test: (a) **TOMBSTONE exclusion** — `PlacePiece` builds the nearby view from ALL `Boxes` (so `OtherPieceIndex == handle`, which survives `RemovePiece` since it tombstones, never compacts), but a removed piece's box STAYS in `Boxes` and the solver will snap onto the ghost, and `AddConnection` has no `bIsInTheStructure` check — so once the UI/undo can remove pieces, `PlacePiece` MUST exclude `IsPieceRemoved` pieces from the nearby view (an index map, or a sentinel box the size gate rejects). (b) **unknown-material fail-OPEN** — a piece with no stored material substitutes a default `FMaterialProfile()` whose `bCompressionDominant` defaults TRUE → the solver infers `GeneralPurposeMortar` (strongest bond) for an undescribed piece; `RunningBond` never sets materials, so "lay a RunningBond wall then place onto it" gets mortar by accident. Prefer a not-compression-dominant placeholder (→ DryStone, fail closed) or require the material. Moot while the harness builds from empty via `PlacePiece` (which always `SetPieceMaterial`s). (c) the caller can't see how many joints were OFFERED vs formed — consider `JointsOffered` on `FPlacementResult` or an `ensure` (a solver pose `MakeInterface` refuses is a solver bug).
- **Build-mode render (`DestructionGame.Visual.BuildDemoScreenshot`, `Tests/BuildDemoScreenshotTest.cpp`, LANDED 2026-09-04):** the /goal's "small render" — a self-contained NonNullRHI screenshot harness (mirroring `CorbelScreenshotTest`) that builds `BuildMode::BuildDemoBuilding` on an empty layout, spawns one `ABrickActor` per piece, frames head-on and writes `Saved/Screenshots/WindowsEditor/BuildDemo.png`. Filtered out of the default `-nullrhi` suite; run it with the exact invocation in the test header (NOT `-nullrhi`). Follow-ups (none blocking, none yet driven by a test): (i) **the grounded course renders HALF-BURIED** — `BuildDemoBuilding` centres course 0 at Z=0 so it straddles the ground plane (−3.25..+3.25); the structure is correct (the solver reads only relative positions) but the picture shows a sunken bottom row. Fix by resting course 0 on the ground (centre Z=3.25, shifting every pinned Z in `DemoBuildingTest` by +3.25 — a small builder+test slice). The convention is now a RULING (DESIGN §8, 2026-09-15) that the session toolbar's `CoursePlaneZCm` already implements; `Core.SessionToolbar.DemoBuildingHeights` pins the demo as exactly one brick half-height BELOW the ruling and now READS the demo's placed centres out of `BuildDemoBuilding`'s own layout (re-pointed 2026-09-15, the review finding), so it goes red AT the migration and not before — expect to discharge its claim then rather than to fix it. (ii) the frame carries the host `Lvl_Sandbox` scenario HUD banner (the `showui` convention shared by every sibling shot); regenerate with the HUD hidden for a clean plate. (iii) harden the harness with `NumConnections()==14` so it can't go green on eight loose Free boxes (today it leans on `BuildDemoBuildingStands` in the default suite for the joint graph).
- **Build-mode INTERACTIVE UI — STARTED 2026-09-04 (design in BUILD_MODE_PLAN.md "Interactive UI" section).** UI-1 landed: `BeginBuild()` + `PlaceBuildPiece(...)` (the incremental world place a click drives). UI-2 first step landed: `PreviewBuildPiece(...) const -> FBuildPreview{bValid,Kind,CentreCm,JointCount}` — the non-mutating ghost query, and the world layer's gather+decision is now consolidated into ONE file-local `ComputeBuildPlacement(const FStructureBinding&)` serving both `PreviewBuildPiece` and `PlaceBuildPiece`. Deferred, none yet driven by a test: (a) **the decision→add middle is still TWO-SITE** — `ComputeBuildPlacement` (world) and `Core/BuildMode/Placement.cpp` `PlacePiece` (the `FBrickLayout` path `BuildDemoBuilding` uses) each carry their own gather+fallback+decision; they can't share directly (binding owns its `FStructure` privately, `PlacePiece` takes an `FBrickLayout`). A shared Core "decide from (boxes, materials) → candidate" function both gathers call would make the material fallback single-site; the tombstone exclusion is inherently in the container-specific gather and stays per-site. So the deferrals (e) below are TWO-site until that extraction. (b) **the confirm API is opaque** — `PlaceBuildPiece` returns only `FPieceRef` (the preview now carries `Kind`); consider returning `Kind`/joints from the commit too so a functional test can assert "the click did what the ghost showed". (b2) **`FBuildPreview.bValid` means "id known", not "would place"** — a degenerate extent previews `bValid==true` with a `Kind`, then the commit returns a default ref; add a `bWouldPlace` (finite mass) so the ghost can show refusal before the click. (b3) **re-preview after any mutation** — preview predicts the commit only while the binding is unchanged; the UI must treat the commit as authoritative and re-preview after each placement/removal (noted in the `FBuildPreview` header). (b4) `FSnapSettings` is default-constructed twice (helper + the wrapper's joint loop); put `JointThicknessCm` on `FBuildPlacement` so a configurable joint can't drift — now also the GROUNDED tolerance's source, so a drifting joint moves which pieces read grounded. (c) any caller of `BeginBuild` OTHER than `UBuildModeComponent` (which now `CancelBuild`s) still owns its own cancel — the subsystem's "the caller owns it" contract is unchanged. (d) `PlaceBuildPiece`/`PreviewBuildPiece` NEVER SOLVE (live-feedback-off default) — the destroy/run path solves. (e) tombstone exclusion + unknown-material fail-open (the `PlacePiece` deferrals) apply to both sites — see (a).
- **Build-mode UI-4a (`UBuildModeComponent`, `World/BuildModeComponent.*`, landed 2026-09-04):** the interactive build loop's testable core — `BeginBuild()` opens a structure, `UpdatePreviewAt(worldCursorCm)` runs `PreviewBuildPiece` and drives a standalone unbound ghost `ABrickActor` (positioned by the now-shared public `UDestructionStructureSubsystem::BrickSpawnTransform`, collision disabled so it can't eat the build raycast or depenetrate; NOT adopted), `ConfirmPlace()` commits the last valid preview (one preview → one commit: the held preview is consumed on commit and reset on `BeginBuild`). Deferred, none yet driven by a test: (a) **three test-side copies of the spawn formula** (`BuildDemoScreenshotTest.cpp`, `CorbelScreenshotTest.cpp`, `BrickWorldTestSupport.h`) now that production's is the public `BrickSpawnTransform` — fold them onto it so a real brick mesh with a different pivot can't drift test vs production. (b) **"translucent" is a claim, not a property** — the ghost is an opaque `ABrickActor` wearing the `Hovered` overlay; the owed ghost-vs-placed render (below) settles whether it reads as a ghost, and if not a dedicated translucent overlay material (data) is the follow-up. (c) `EnsureGhost` uses `Cast<ABrickActor>` not `IsValid` (a pending-kill ghost could be reused); a null-guard on `*CurrentMaterial` belongs before Blueprint/UI can set it (still owed — `SetPieceKind` now writes it from the palette, which never answers null, but the field stays public). (d) `EndPlay` does NOT `CancelBuild` — a component removed mid-play with a build open leaves that binding + bricks alive with nothing naming them (the same leak `BeginBuild`-cancels-first closed, by a different route); one line + a red test. (e) `SetHighlighted` on the snap neighbour(s) tinted by `Kind` (the plan's UI-2 highlight) is not done. (f) the ghost-vs-placed render (`DestructionGame.Visual.BuildGhostScreenshot`, verified by eye 2026-09-04: the `Hovered` overlay renders the ghost gold, clearly distinct from clay-red, so a translucent material (b) is optional polish) still SEEDS AT Z=0 — the half-buried seed the 2026-09-15 ruling exists to remove; re-shoot when the demo/harness migration lands.
- **Build-mode UI-4b (ray→build-plane seam, `UBuildModeComponent::UpdatePreviewFromRay` + `BuildPlaneZCm`, landed 2026-09-04):** turns a WORLD RAY into a preview — intersect the ray with the horizontal plane `Z == BuildPlaneZCm`, then `UpdatePreviewAt` the hit; fails closed (hide ghost, no held preview) on a ray parallel to the plane, behind the origin, or NON-FINITE (`ContainsNaN`/`!IsFinite` guard + the house `!(t >= 0)` form — reachable once the mouse wiring lands, since `DeprojectMousePositionToWorld` can return false without setting its out-params). Pinned by `Component{ObliqueRayHitsIntersection, RayDrivesPreviewAndGhost, RayMissingPlaneHidesGhost (+ confirm-after-miss), NonFiniteRayFailsClosed}`. Deferred, none yet driven by a test: (g) the 1 km `MaxPickDistanceCm` default wants tuning and the parallel-guard comment softening once the mouse wiring lands. (h) the plane is now derived from the toolbar COURSE (`SetCourse` → `CoursePlaneZCm`), but there is no "auto-plane from the piece under the cursor" (the plan's ray-vs-brick-face variant) — stacking still means stepping the course by hand; the existing ray tests set `BuildPlaneZCm` explicitly with LEGACY Z=0 seeds, so if their seeds are ever migrated to 3.25 their planes must go to 10.75. (i) `UpdatePreviewAt` does not guard a NON-FINITE CURSOR: in Free mode a NaN cursor yields a Free candidate at a NaN centre with a finite mass, which `AddPiece` accepts (pre-existing via the Free fallback; Free mode makes it a one-click path) — an `IsFinite` guard at the door, house form, with its row. (j) nits: a SEEDED `MakeComponent` variant is owed (every fixture still hand-lays its seed brick); `RayDirectionCm` is unitless (drop the `Cm`).
- **Build-mode SESSION SLICE 2 (toolbar model → component + subsystem, landed 2026-09-15):** `FBuildPreview` gained `bGrounded`; `PreviewBuildPiece`/`PlaceBuildPiece` take a `DestructionSession::EPlacementMode` (defaulted to `Snap`) and `PlaceBuildPiece`'s `bGrounded` ARGUMENT IS GONE — the shared file-local `ComputeBuildPlacement` derives it from the chosen pose (`CentreCm.Z - ExtentCm.Z <= FSnapSettings::JointThicknessCm`). Deferred, none yet driven by a test: (a) **the grounded comparison is deliberately NOT in house `!(x > y)` form** — `!(Bottom > Joint)` reads a NaN pose as GROUNDED, which is fail-OPEN (a piece that can never fall), so it is written `Bottom <= Joint` with the reasoning at the site; if the house style is ever mechanically enforced, this line must be exempted rather than "fixed". (b) **the ground plane is the hardcoded Z = 0** implied by that comparison — a scenario whose earth is anywhere else would ground everything or nothing; give the derivation a ground height when one exists. (c) the rule is PER-PIECE GEOMETRY ONLY — it never asks whether anything is under the piece, so a Free brick bottomed at 0.5 cm over a pit still reads grounded (correct for a flat world, a lie the moment terrain arrives). (d) `ComputeBuildPlacement` returns `bDecided`; it is `false` on a NON-FINITE chosen centre (pinned by `BuildMode.NonFinitePoseNeverCommitsGrounded`, which found the commit spawning an actor at a NaN transform before `AddPiece` refused it — both doors now fail closed before any spawn; this also means the `<=` in (a) is no longer reachable by a NaN and is defence in depth), and on the solver failing to emit a Free candidate for a Free request — that second route is UNREACHABLE today (`SolveSnapCandidates` always appends one) and therefore UNTESTED; it exists so a solver change fails closed rather than silently placing at the nearest snap. (e) the component's `CurrentKind`/`CurrentCourse`/`PlacementMode` are a copy of three `FSessionToolbarState` fields — DECIDED (S4, 2026-09-15): the controller's state owns them and drives the component through `OnToolbarButton` only; `PlacementMode` stays a bare public field on the component (tests set it directly), so nothing OUTSIDE the controller may write it in production.
- **Session toolbar MODEL (`DestructionSession`, `Core/SessionToolbar.*`, landed 2026-09-15; the Cities-Skylines-style build/destroy toolbar's presenter, mirroring `Core/PieceMenu.h`):** `FSessionToolbarState{Mode, Piece, Placement, Course, bHasStructure}` → `SessionToolbarButtons` (the strip as data: Build mode = mode pair + Brick/Timber plate/Timber lintel + Snap/Free + Course down/up + Clear build; Destroy mode = mode pair + Run structure) and the pure `ApplyToolbarButton` (a refused/greyed button is a bitwise no-op — it consults `SessionToolbarButtons` as the single source of "can this happen", so the controller MUST route every input, keyboard shortcuts included, through `ApplyToolbarButton` and never set fields directly); the piece palette (`BuildPieceHalfExtentCm`/`BuildPieceMaterial`, by address); the rests-on-the-ground `CoursePlaneZCm` (DESIGN §8 ruling above). The COMPONENT WIRING LANDED 2026-09-15 (slice 2): `SetPieceKind`/`GetPieceKind` derive `CurrentMaterial` (by address off the palette) + `CurrentExtentCm` + `BuildPlaneZCm`, `SetCourse`/`GetCourse` store the CLAMPED course and re-derive the plane, a public `PlacementMode` field rides through to both subsystem doors (`EPlacementMode::Free` selects the solver's Free candidate BY KIND, honouring the cursor verbatim and forming no joints), grounded is pose-derived, and `CancelBuild` destroys the binding + its bricks. S4 LANDED 2026-09-15: `ADestructionGamePlayerController` holds the `FSessionToolbarState` and a `UBuildModeComponent` subobject; `OnToolbarButton` is the ONLY mutator (refuses undrawn/greyed buttons with no side effects, pushes the component, Run = `SolveAndPush(GetSessionStructureId())`, Clear = cancel + fresh build); `PointerAlongRay`/`PrimaryAlongRay` dispatch by mode (the mouse handlers route through them) — build-then-destroy in one session is pinned by `World.Session.PrimaryRayPlacesInBuildAndInspectsInDestroy`; the Slate strip `BuildSessionToolbarPanel()` (public, bottom-anchored 48 px, IsFocusable(false) everywhere) is shown by the game mode on every row, and the build plot opens in Build mode. S6 LANDED 2026-09-15: the cursor is the SESSION's (`SetSessionControls` from BeginPlay: shown always, hidden only while RMB is held for the look, `FInputModeGameAndUI`), camera look is CHORDED on `IA_LookModifier` (RMB) via a trigger on `IMC_MouseLook`'s one mapping, `IMC_Session` (Tab / 1 2 3 / G / [ ] / Enter, authored by `Scripts/Author-SessionInput.py`) is applied alongside IMC_Default, the eight shortcuts bind on `Started` and go through `OnToolbarButton` (Tab/G via `ToggleSessionMode`/`ToggleSessionPlacement`), `SetPieceMenuControls` is DELETED (nothing is taken away under a menu any more), and Delete refreshes `bHasStructure` cold. Owed from S6 (none driven by a test): (S6-a) the human check — cursor visible over the strip, the camera still while RMB is up and turning while held, `W` still flying after a chip click — is a PLAYTEST item, unreachable headlessly; (S6-c) `MouseLookMappingContext` is read only by the constructor (drop it or keep as the named handle); (S6-e) `F1` hide-UI (design §d 6) and the mouse-wheel course stepper (§b) are unbuilt; (S6-f) the chord's ORDERING is pinned (`World.Input.LookModifierEvaluatesBeforeTheChord`: `IMC_Session` applied at `SessionMappingContextPriority`, one above the other two, so its RMB→`IA_LookModifier` mapping evaluates before the chorded `Mouse2D`; the review measured 0/14 look frames at equal priorities) — but `SessionMappingContext` is an `EditAnywhere` UPROPERTY like its siblings, so a Blueprint could re-point it and defeat the special case in the apply loop; the more robust shape (the RMB mapping living in `IMC_MouseLook` itself, ordered by `ReorderMappings` regardless of priorities) is the fallback if that ever bites; also, during an RMB drag `IA_HoverPiece` is blocked by the engine's injected chord blocker on the later Mouse2D mapping — desirable, order-dependent, unpinned. CHIP STYLING LANDED 2026-09-15 (the model owns the look — `EToolbarGroup`/`EToolbarSwatch` on the row, `ChipLookFor`/`ModeAccent`/`SwatchColour`; the strip draws `FSlateRoundedBoxBrush` chips from a FIXED per-id `FButtonStyle` array (SButton keeps a raw style pointer — never a TMap value), amber/red accent fills on the lit chip and the Run "go" chip, brick/plank swatches on the piece chips, 1 px hairlines between groups). Chip polish still owed, none driven by a test (dev's render notes): (C1) the hairlines at white@0.16 are effectively INVISIBLE on the dark bar — raise the alpha or widen the group gap; (C3) the timber swatch nearly vanishes on a lit amber chip and the brick swatch reads as a smudge on a dark chip — a thin dark outline on the swatch; (C4) the "drop edge" is a full ring (`FSlateRoundedBoxBrush` outlines all sides), not §e's edge BELOW; (C5) the two swatch colours are TRANSCRIBED into `Core/SessionToolbar.cpp` from `M_Shed_Brick`/`M_Shed_Timber` (Core cannot include `RequiredContent.h`) — a `Content.*` agreement test in the shape of `NeighbourSwatchesMatchTheirMaterials` is owed; (C6) §e's hover lift / pressed push (1 px geometry), the `Clear build` arming, S2 shortcut glyphs and S1 sub-captions remain unbuilt; (C8) the Build strip is pinned to end by 1270 px (`BuildStripFitsTheReferenceWidth`; 17 chips end at 1232 after the captions became `-`/`+`/Plate/Lintel and `Clear build` → `Clear`) — an eighteenth chip will need the S15 narrow-width layout; (C7) `SessionChipStyleFor`'s fail-closed extra slot (unknown id → greyed look) is unreachable through the model and untested. STILL OWED: S1/S2 (sub-captions, shortcut glyphs on the model), S5's remaining half (an open piece menu SURVIVES a switch to Build — `OnToolbarButton(ModeBuild)` dismisses nothing). The committed PROOF is `DestructionGame.Visual.SessionScreenshots` (`Tests/SessionScreenshotTest.cpp`, NonNullRHI, drives the real controller on `Lvl_Build`: Build strip + bonded wall + plate + ghost, Destroy strip + details window on a laid brick, after Delete, after Run) — re-shoot when (v-a)/(v-b) land so the frame-1 ghost is a SNAPPED one rather than the Free fallback. Two findings the proof frames surfaced (neither driven by a test yet): (P1-residue) **the two surfaces agree only when the player builds from the earth**: the toolbar numbers courses from the ground (`CourseLabel(index)` = index + 1) while the piece menu numbers them RELATIVE TO THE BINDING'S LOWEST PIECE (`Core/PieceMenu.cpp` ~419-433: the lowest placed piece is always "course 1") — fresh plot, Course up ×3 (strip "Course 4"), lay one brick → the readout says "course 1 · #1"; `Presenter.CourseNumberingAgrees` cannot see it (it lays on index 0, where both read 1). Owner call: menu goes absolute (from the earth, matching the toolbar and the build plane) or the toolbar goes relative; RED-TEST SHAPE: lay a single brick at `CoursePlaneZCm(3, h)` and assert `CourseNumberIn(Pieces[0].Label) == CourseNumberIn(CourseLabel(3))` (and a second brick at index 1 vs `CourseLabel(1)` to pin the step, not just the origin). (S7 leftovers) the identity line prints the library's own names (`ClayBrick`, `Timber`) — a display-friendly name ("Clay brick") is a presenter follow-up; a non-finite dimension or mass prints as `nan`/`inf` inside the line rather than refusing (the visible-word direction, undocumented guard avoided). (P2) **an UNSOLVED readout draws full green headroom bars** beside "not solved yet" / 0.0 N — a full bar reads as 1000× margin, not "no data"; the bar should be empty or hatched until a solve has run (belongs with the never-solves note (d)). Also logged from the S4 review: `Clear build` reads `bHasStructure` off the LEVEL's wall in Build mode on a scenario level (a visible no-op over somebody else's wall — in Build mode both commands should read the player's build only); toolbar ZOrder is the default (add-order decides; give it an explicit ZOrder when the details window's stacking matters); `RefreshSessionToolbar` rebuilds the whole strip per click/placement (cheap at ≤17 chips; a persistent `SBox` swap if laying feels sticky); `ScenarioLabel`'s invalid-row arm still returns the no-cut sentence; the banner's empty third slot keeps its 6 px top padding; S5's "mode switch is total" test was delivered inside `PrimaryRayPlacesInBuildAndInspectsInDestroy` rather than as its own `ModeSwitchIsTotal`; `SessionControllerTest.cpp` section SEVEN's message prints `NumPieces()` under the label "released" (cosmetic). Deferred, none yet driven by a test: (a) `CoursePlaneZCm` does not guard `PieceHalfHeightCm` (NaN/negative passes through; downstream `!(HitT >= 0)` fails closed today) — house `!(x > 0)` guard when a COMPUTED extent is fed in; (b) an already-negative `Course` is preserved, not repaired (`CourseDown` greyed, `CourseUp` from −3 → −2 while the label reads "Course 0"; unreachable through the model's own transitions until save/load can feed a state in — repair with its row then, and soften the `Transitions` test header's "no click may EVER produce a negative course", which is swept over {0,1,5} only); (c) no `CourseUp` ceiling (int32 overflow in principle); (d) unknown `ESessionMode` draws the DESTROY strip with Run enabled (`bBuilding = Mode == Build`) — fail-open toward the costly mode; flip when a red row drives it; (e) the plate/lintel extents are literals duplicated from `Core/BuildMode/DemoBuilding.cpp` (the brick's is derived from `FSnapSettings`) and the palette is three switches touching ~8 sites per new kind — fold to a `{Kind, ButtonId, Caption, HalfExtent, Material*}` row table in a Core/BuildMode piece catalogue both the demo and the toolbar read (dependency presenter → BuildMode, never the reverse) when a fourth kind arrives; (f) unknown kind → zero extent → zero mass → `AddPiece` refuses (fail closed, fine); (g) a cheap sweep extension: for every ENABLED button over all 72 states assert at most one field differs and it is the named one (the 792-pair cross-check is one-directional by design). **The screen's design is [SESSION_UI_DESIGN.md](SESSION_UI_DESIGN.md) (2026-09-15, S0–S15 slice order); its deferrals, logged here so they are not lost:** (h) an "auto-run after delete / hold" destroy setting is NOT honest today — `RunPieceActions` deliberately has no public run-without-settling; building it means a settle POLICY threaded through `RunPieceActions` such that the no-settle path also refuses to push (a Core-invariant change, its own red + review); (i) `IA_HoverPiece` exists only because `IMC_MouseLook` was removed under the piece menu; once the session keeps the cursor permanently (design §d) it can fold back into `IA_MouseLook` — a content change, take it separately; (j) the piece menu's double-click detail toggle on the grab strip wants a captioned control (needs a model field + red test, already noted in the panel header); (k) the Build-mode ghost card (design S9) is gated on the logged `bWouldPlace` (b2), `bRequestedPoseOccupied` (v-a) and merged-`Kind` (vii) debts.
- **Session S12 — the LOAD OVERLAY (landed 2026-09-15; SESSION_UI_DESIGN §b "Destroy settings group" / §g S12):** the Destroy strip's `Load overlay` chip (`EToolbarButtonId::ToggleLoadOverlay`, a LATCHING setting in the Settings group between the mode pair and `Run structure`, greyed without a structure, and preserved across mode switches because the Build strip does not draw it); `WorstJointBandForPiece(const FStructure&, int32)` in `Core/PieceMenu.cpp` — the worst band over the piece's INTACT joints, bucketed one joint at a time through the inspector's own `PresenterMarginBand` so the overlay and the details window cannot grow separate thresholds, with a GIVEN joint skipped (the one place the overlay's rule differs from a row's: counting it would paint every neighbour of every deleted brick red) and Critical for an out-of-range/removed/jointless piece and for a structure nothing has solved (detected through `HasSupportAnswer`, because `GetConnectionUtilisation` reads 0 before a solve and would paint an untouched wall green end to end); `BrickHighlightForLoadBand` + the three `EBrickHighlight::Load*` states + three translucent-unlit overlay materials authored by `Scripts/Author-LoadOverlayMaterials.py` (the M_BrickHover shape at the hover's 0.35 opacity rather than the selection's 0.95 — the overlay covers every piece at once and 0.95 hides the bond); and `ADestructionGamePlayerController::RefreshLoadOverlay` — one non-destructive `SolveLoads` per toggle / Run / committed Delete / Build placement, feeding `HighlightForPiece` as its WEAKEST input rather than painting bricks (a painter would fight the cursor and win). The three band colours MOVED out of the controller into `DestructionContent::BrickLoadSwatchColours` beside the three material paths, indexed by `EJointMarginBand` (Critical first), so the bar, the dot-free panel fill and the material are one home. Owed, none driven by a test: (S12-a) the `L` SHORTCUT is not built (design §b lists it; the chip is the only way in today). (S12-b) **a `Content.*` agreement test holding `BrickLoadSwatchColours` against the three materials' emissive constants** is owed, in the exact shape of `Content.NeighbourSwatchesMatchTheirMaterials` — today the Python script transcribes the three triples and nothing holds the two together, which is precisely how the neighbour palette drifted before it was moved beside its paths. (S12-c) `ClearBuild` and `ModeBuild` do NOT refresh the overlay, so a cleared plot leaves `LoadOverlayStructureId` naming a destroyed binding with the chip still lit — benign today (no actor answers those refs and ids are not reused) but the tint does not come back until the next placement or toggle; one line each plus a row. (S12-d) the three SUPPORT-dot colours in `DestructionGamePlayerController.cpp` (`PieceMenuSupportFallingColour`/`Stranded`/`Supported`) still duplicate the same three triples that moved — a second home for the same numbers, left alone deliberately because nothing drove it. (S12-e) **the overlay reads the ROUTER's `GetConnectionUtilisation` while the inspector ROWS prefer a cached min-violation readout when a below-cap settle left one** (`Core/PieceInspection.cpp`), so after a Run or a Delete on a below-cap structure the panel and the tint can disagree for exactly the joints the LP re-priced; `World.Session.LoadOverlayTintsByWorstJoint`'s cross-check runs before any settle and cannot see it. Decide which source the overlay should read, then pin it. (S12-f) cost while on: the overlay now READS a settled solve (it solves only when a live piece has no support answer — the first toggle on a fresh build, or after a Build-mode placement), so the per-mutation cost is a full repaint plus a solve only in those two cases; still a synchronous whole-structure solve on the ~1,200-brick default wall at first toggle. (S12-i) banding is SUPPORT-FIRST through the composite `IsPieceSupported`, so a STRANDED piece (an unroutable knot — a solver limitation, not physics) reads Critical alongside a genuinely Falling one; fail-closed, but if the overlay should ever say "the solver gave up here" distinctly, that is its own row. (S12-j) with the overlay ON the Destroy strip is THREE red chips in a row — the lit `Destroy` tab, the lit `Load overlay` setting and the ringed `Run structure` — the lit tab and the lit setting are pixel-identical by the model's own rule (C2 extended to the Settings group); the §b/§e "mode pair one size up with an icon" treatment is the eventual separation. (S12-g) the chip carries no sub-caption and no shortcut glyph (S1/S2, already owed). (S12-h) **`Content.HighlightMaterialsPaintSomething` (`Tests/HighlightMaterialPaintTest.cpp`) still lists only the nine older overlays** — the three `M_BrickLoad*` assets are in the required-content sweep (they resolve) and in `World.Brick.HighlightWearsAMaterial` (the right state asks for the right asset), but NOTHING asserts they paint anything, so a material authored black, opaque or with an unconnected emissive would pass the whole suite and draw an invisible overlay on every brick. Three rows in that table, driven red first by breaking one of them.
- **Session S0 — the PLAYABLE BUILD LEVEL (`build` / `Lvl_Build`, landed 2026-09-15):** `FScenario::bBuildSandbox` (the row lays NOTHING — the player lays it), the catalogue row next to `sandbox` (no `LayStructure`, default `Wall`, no cut, `HoldSeconds` left at 4.0, `Framing = ThreeQuarter`), and `ADestructionGameGameMode::BeginPlay`'s branch on it BEFORE `DestructionScenarios::Build` (which refuses the row — `RunningBond` refuses zero courses): nothing built, `BuiltStructureId` stays INDEX_NONE, no hold armed, the row still recorded so the label works, and the player framed over a DEFAULT plot box (origin-centred, half-size 150/150/50 cm → standoff 484 cm, camera (269.61, 321.31, 242.16), pitch −30°). Driven by `World.Scenarios.{BuildSandboxRowExists, EveryOtherRowIsNotABuildSandbox}`, the amended `World.Scenarios.Build` sweep and `World.Scenario.GameModeOpensAnEmptyBuildSandbox`. Deferred, none yet driven by a failing test: (a) **the plot box is a game-mode constant nothing pins** — `GameModeBuildPlotHalfSizeCm` is asserted only as a property (finite, within 20 m, pitched down, aimed at the plot), deliberately, so retuning it is free; when the build plane/course grid decides how big a plot actually is, give the two one source. (b) **the screenshot sweep's skip is asserted by nothing** — `Visual.ScenarioLevelScreenshots` skips `bBuildSandbox` rows in both its queue loop and its file check, and a regression that photographed the row would fail loudly (no file) while one that skipped a REAL row would pass in silence; the sweep has no "every non-sandbox row was photographed" count. (c) **`Scripts/New-ScenarioMap.ps1` resaves the WHOLE `/Game/Maps/Scenarios` folder**, so making one map leaves every other scenario `.umap` modified in the working tree (33 files on the Lvl_Build run, content-unchanged re-serialisation — REVERTED with `git checkout` before the commit; only the new map was kept). Narrow the commandlet to the one package (`-Package=` instead of `-PackageFolder=`), or say in the script header that the churn is expected and must be reverted. (e) "no hold is armed" is asserted by CONSEQUENCE (nothing runs over 1.5× the hold + `INDEX_NONE`), sound only while `SetTimer` stays behind `BuiltStructureId != INDEX_NONE` — if a later slice ever arms a timer on another id (the player's build binding), add an `IsScenarioHoldArmed() const` accessor and assert it directly; also `PastEveryDelaySeconds` in that test reads `free-end-40`'s hold rather than the build row's own (`Row->HoldSeconds * 1.5` is the honest figure; both are 4.0 today). (f) section FOUR of `BuildSandboxRowExists` duplicates the inverted arm of the `World.Scenarios.Build` sweep — keep the sweep's, drop the copy.

- **Session UI-6 — the JOINT CHOICE (landed 2026-09-16; owner ask 2026-09-15):** six `Joint*` chips (Auto / Mortar / Dry / Nail / Screw / Bolt) in Build mode; `JointOverrideFor(choice)` is the library extern by address (Auto/unknown → nullptr = the inference); both subsystem doors take `const FConnectionStrength* JointOverride` and substitute it into EVERY joint the placed piece forms (existing joints never re-priced); `FBuildPreview::JointProfile` names the profile the ghost would fasten with; `FNamedConnectionProfile::Strength` is now a REFERENCE to the extern (the S7 identity fix repeated) and `FindConnectionProfileRow` matches a stored by-value strength back to its library row field-for-field; the inspector's intact joint rows append the library row's lower-cased name, and `PresenterFullPanelWidthPx` grew 640 → 800 to fit it. Deferred, none driven by a test: (J1) **the row word is the raw extern name** (`generalpurposemortarperpend`, 27 characters in a player-facing line, and what cost the 160 px of panel) — the data-shaped fix is a DISPLAY-NAME column on `FNamedConnectionProfile` ("mortar", "perpend", "dry stone", "nail", "screw", "bolt"); the test pins `Contains` on the profile word so a display name can replace it without a red; (J2) the BROKEN-joint row does not name its profile (only the intact branch does); (J3) RESOLVED — a jointless placement previews `JointProfile == nullptr` even with an override (pinned); (J4) RESOLVED — `Core.Profiles.ConnectionRowsRoundTripByValue` pins every library row finds ITSELF by pointer (distinctness + all five fields) and the "custom" arm is pinned by a second one-neighbour fixture; (J5) the choice is ONE PER PLACEMENT — a plate laid with Screw screws BOTH bearings; per-joint choice (click a joint in the ghost card) is a later refinement; (J6) the choice is not yet recorded for save/load (UI-5) and the ghost card (S9) does not show `JointProfile` yet; (J7) the joint chips have no swatch/glyph and no shortcut.
- **Build-mode CORNER-RETURN snap — IN PROGRESS (owner-ratified 2026-09-15, DESIGN §8):** CR-1 LANDED: `BuildMode::ClassifyMasonryContact` (Bed / Head / Corner from the two boxes + the normal; fail-closed Head on a degenerate normal or a no-long-axis box; an exact 45° tie goes HORIZONTAL) and the boxed `JointForContact` overload (Corner → full `GeneralPurposeMortar`, Head → the perpend). CR-2a LANDED (2026-09-16, solver): every solver joint is inferred through the BOXED overload (the solver is the single inference site — `Placement.cpp` and `ComputeBuildPlacement` consume the candidate's profile); `IsBrickSized` accepts the rotated footprint; bed/head candidates only for SAME-orientation pairs, stepping along the NEIGHBOUR's long axis (a Y-wall grows as an X-wall does — `SnapSolverYWallMirrorsTheXWall` reflects the X answer through x=y); `ESnapKind::BrickCornerReturn` — four poses per crossed brick-sized neighbour (each end × each flush choice), one 66.625 cm² quoin joint each → mortar; the L-FOOTPRINT rule closes the KNOWN LIMIT: after crossed long axes, Corner iff one piece's span PROPERLY crosses (strict) EXACTLY ONE of the other's width-face planes (A-vs-B OR B-vs-A) — a closer crossing both or a buried header crossing neither is Head; `CornerWallStands` lays a 9-piece L (6 beds + 1 quoin = mortar, 5 perpend heads, all Supported). CR-2b LANDED (2026-09-16): the `Rotate` chip (Build, between Lintel and Snap, latches; `Clear build` shortened to `Clear` so 17 chips end at 1232 px), `UBuildModeComponent::SetRotated` (one `ApplyPalette()` site derives material + extent with X/Y swapped + plane), and `Visual.SessionScreenshots` frame 6 `Session_Corner.png` — an 11-piece L laid through the controller, bonded across courses. Owed from CR-2b: (xiii) RESOLVED (2026-09-16): `BeginBuild` flags every player build 3D UNCONDITIONALLY (stated at the door, never inferred — the E3 ruling), pinned by `World.Session.{CornerBuildIsJudgedByTheLP, StraightBuildIsJudgedByTheLP}` (a posed Y-normal head joint reads `bPresent` after Run; a straight wall is 3D too). **WHAT THAT COST — an OWNER-VISIBLE playability finding (measured 2026-09-16 on a 100-brick / 261-joint straight session wall, nothing released):** a COLD `Run structure` went from **2.5 s (2D pose) to 94 s (3D pose)**, ~37×; per-click solves 705 ms → 12.3 s mean, worst 4.9 s → 92 s; negligible below ~10 bricks, superlinear after. Nothing in the session changes the verdict AUTHORITY any more — but a big straight wall is now slow to run. FIX LANDED (planar-pose slice, 2026-09-16, `OracleSweepFull` 16/16 both tiers): the LP bridge (equilibrium AND regional, in lock-step) poses 2D under the 3D flag iff BOTH (a) every POSED joint normal is in-plane AND (b) every Y that enters an equilibrium row — each UNGROUNDED block's centroid and each posed patch centre — is one common Y; else 3D; the flag stays the STATED permission (a 2D-flagged out-of-plane joint still refuses). (a) alone was UNSOUND — measured: the coarse closed-box shed's blocks have coplanar normals at DIFFERENT Y, the lever arms make an out-of-plane moment demand, and a post-removed overhang read λ* = 137 STANDING under the projection — hence (b). `FStructure::GetLastEquilibriumProblemDim()` (2/3/INDEX_NONE) is the observable, pinned by `Core.Oracle.{PlanarProblemUnderThe3DFlagPosesIn2D, PlanarPoseChangesNoVerdict}` and `World.Session.StraightBuildRunsInThePlanarPose`. REVIEWED 2026-09-16 (fable): the slice's original "same feasible set, only the cost differs" claim was WRONG — the 3D friction pyramid and shear ceiling are an INSCRIBED k=8 octagon (`ThreeDPyramidInscribeFactor` = 0.924), so the 2D pose is the exact Coulomb cone and up to 7.6% MORE PERMISSIVE on pure in-plane shear; recorded as a ruling with a cost in DESIGN §8 (2026-09-16), every "same feasible set" sentence in the bridge/Structure docs corrected. Review gaps closed by `Core.Oracle.RegionalBridgePosesThePlanarRegionIn2D` (the regional lock-step had NO test — mutating it back to always-3D left the suite green) and `Core.Oracle.CoplanarNormalsAtDifferentYPoseIn3D` (condition (b) pinned directly and mutation-proven: the Y-cantilever brick on DryStone — Dim3D AND Falling — pins BOTH halves of (b); the plank across two walls at different Y pins the patch-centre half only, because its two bearing patches disagree with each other; registry in the test header). STILL OWED: (xiii-h) a 2D-FLAGGED plank on two walls at different Y is silently PROJECTED (no Y-normal joint, so the 2D refusal never fires) — inherent to the 2D wythe-projection model, not new; if it should fail closed that is its own red; (xiii-d) the 3D MECHANISM READER severed eight joints inside a free-falling body where the 2D reader dropped it whole — with different feasible sets that is not "dual degeneracy", it is an open question about the 3D extraction (THREED_DESIGN E2b) that genuinely 3D builds still run; (xiii-f) condition (b) is GLOBAL (one plane per problem) where the moment rows only need it PER UNGROUNDED BLOCK (centroid Y = every posed patch centre it touches) — two independent straight walls at different Y in one flagged session pose 3D and pay 37× though each is planar; refine only with a red test; (xiii-g) no fixture in the suite is shear-critical inside the 0.924 band, so the one verdict class this ruling can move (3D FALLS → 2D STANDS) has no witness — worth one when a shear-governed planar fixture exists. Also now PLAYER-VISIBLE: the inspector rows on every session build read `SolveMinViolationReadoutThreeD`, whose parity gap (item-8 residue: no shear-cap/first-crack rows) reads the SAME joint 6.7× more comfortable than the 2D readout (0.006 vs 0.041 utilisation on the 100-brick wall) — promote that residue to a ranked item; (xv) the two-wythe header residual (iv) is now REACHABLE (Rotate + Free); (xvi) stale prose: "the ten-chip configuration" (SessionToolbarPanelTest.cpp ~786, ~1016), "sixteen-chip" (~1798), and "Clear build" as the command's name in SessionToolbar.h/.cpp/tests (the caption is `Clear`); (xvii) **OWNER PLAYTEST 2026-09-16: "it should show where the brick is going to go without clicking anything"** — the ghost was driven only by the `IA_HoverPiece` Mouse2D action, which fires on movement, so in the owner's session it appeared only after a click. **LANDED 2026-09-16 (cursor-driven ghost):** `UBuildModeComponent::RefreshPreview()` re-drives the held preview at `(LastCursorCm.X, LastCursorCm.Y, BuildPlaneZCm)` — the held cursor put back on the CURRENT build plane, which is what lets a course change lift the ghost — and REFUSES (returns false, touches nothing) when no preview is held, including after `ConfirmPlace` spent one; `ApplyPalette()` ends by calling it, so all five settings doors converge there — `SetPieceKind`/`SetCourse`/`SetRotated` plus the new `SetPlacementMode`/`SetJointChoice` setters over the (still public, fixture-writable) fields, which `OnToolbarButton` now calls instead of assigning; `ADestructionGamePlayerController::RefreshBuildPreviewFromRay(Origin, Direction)` drives the component's ray seam in Build mode and is a no-op returning false in Destroy; and `PlayerTick` → the private `RefreshBuildPreviewFromCursor()` deprojects the mouse into it. Pinned by `World.BuildMode.{SettingsChangeRefreshesTheHeldPreview, RefreshWithoutAHeldPreviewDoesNothing}`, `World.Session.{ToolbarChangesMoveTheGhostAtOnce, CursorRefreshDrivesTheGhostFromARay}` and `Visual.SessionScreenshots` frame 7 `Session_Ghost.png` (one seed brick, a turned gold ghost beside it, Rotate lit, no click). `PrimaryAlongRay` previews along the ray, commits, and previews along that SAME ray again — the commit spends the held preview and the tick throttle skips a still mouse, so without the second call the ghost stood inside the brick it had just laid with nothing held behind it (pinned by `World.Session.PlacingReDrivesTheGhostAlongTheSameRay`: the click takes the next-course pose, which is then occupied and dropped, so the re-preview lands on the same-course pose (22.5, 0, 3.25) — visible, not at the placed centre, equal to a fresh ray preview taken afterwards, and HELD). Owed from it, none driven by a test: (xvii-a) **the tick glue is UNTESTED BY CONSTRUCTION and needs the owner's playtest** — `RefreshBuildPreviewFromCursor` skips while `IsInputKeyDown(EKeys::RightMouseButton)` (the look chord), returns on `GetMousePosition` false (no viewport/local player), skips when the pixel position equals the previous frame's (`bHasBuildCursorPx` makes a first frame at exactly (0,0) a refresh rather than a skip), then deprojects and calls `RefreshBuildPreviewFromRay`. The check the playtest owes: a ghost is up before the first click, it follows the cursor, it does NOT wander during an RMB camera drag, and a chip click moves it at once. (xvii-b) **the chord is read as the raw RIGHT MOUSE BUTTON, not as `IA_LookModifier`** — the action has no C++ handler to reach and the two are the same press today, but re-pointing the chord in `IMC_MouseLook` to another button would leave the skip and the look disagreeing; the honest fix is reading the action's value off the Enhanced Input subsystem, which no fixture in this suite can drive. (xvii-c) **the refresh PROJECTS the held cursor onto the new plane rather than re-intersecting the last RAY with it**, because the component has no ray to remember — so under the session's oblique camera a `Course up` lifts the ghost straight up and it is no longer exactly under the pointer until the next mouse move. Remembering the last ray on the component (and re-intersecting in `RefreshPreview`) satisfies every assertion the current tests make and is the better answer; take it with a red that pins an oblique ray across a course change. (xvii-d) `SetPlacementMode`/`SetJointChoice` re-derive material, extent and plane through `ApplyPalette` though neither can change them — deliberate (one convergence site beats five setters remembering a rule), idempotent, noted so it is not read as a bug. (xvii-e) the per-frame throttle compares `FVector2D` EXACTLY, so a sub-pixel jitter re-previews; cheap and in the safe direction, but a small dead-band is the tidier rule if a noisy pointer ever shows up. REVIEW FINDINGS ON THE GHOST SLICE (2026-09-16, fable): (xvii-g) STILL OWED — Destroy → Build with a still mouse shows no ghost until the pointer moves (`ModeDestroy` hides via `HidePreview`, `ModeBuild` does nothing, the remembered pixel makes the next frames a skip); fix = resetting `bHasBuildCursorPx` on the `ModeBuild` path. NOT TAKEN 2026-09-16 with (xvii-f): both the flag and `RefreshBuildPreviewFromCursor` are private with no accessor and the throttle is unreachable headlessly (`GetMousePosition` answers false with no viewport), so NOTHING in the suite can go red for it — the TDD gate refused it rather than landing an untested line. To land it, first give the throttle an observable seam (a pure `ShouldRefreshBuildCursor(Current, Last, bHas)` the model layer can test, or a dev-only accessor), then drive the reset red through that; PLAYTEST CHECK meanwhile: press Tab into Build without moving — is the ghost up?; (xvii-h) TWO previews per moved frame — `Super::PlayerTick` runs `IA_HoverPiece` → `OnHoverPiece` → `PointerAlongRay` → `UpdatePreviewFromRay`, then `RefreshBuildPreviewFromCursor` previews the same ray again; same answer, double solver cost per moved frame (harmless at session sizes, wasteful at the 100-brick wall) — fold `IA_HoverPiece`'s Build half away (SESSION_UI_DESIGN §d item 4); (xvii-i) `SetJointChoice`'s refresh is UNPINNED — section SEVEN of `SettingsChangeRefreshesTheHeldPreview` asserts only that the ghost did not move, and mutating the setter to skip `ApplyPalette()` leaves the suite green; nothing observable changes today (the joint lives only on `FBuildPreview::JointProfile`, which the component does not expose) — pin it when S9's ghost card reads the held preview's profile; (xvii-j) nits: section FIVE's Free pose (11.25, 3, 3.25) interpenetrates the seed (Free is not occupancy-filtered — the assertion is exact, the fixture physically odd); `RefreshPreview`'s valid-preview guard also refuses after a ray MISS hid the ghost, so a chip click then does nothing until the mouse moves (benign — a miss means no pose exists for that cursor). REVIEW FINDINGS ON CR-2a (2026-09-16): (B1) the quoin's VERTICAL joint carries no load in a vertical-load `SolveLoads` (loads route down beds; it bonds laterally) — `CornerWallStands`' stands-claim pins the beds, its mechanism sections pin the quoin; the return becomes load-bearing from above only through the course-1 lap (B2); (B2) RESOLVED — a brick NextCourse pose now takes its joints from the contact sweep (`RestingJointsAtPose`, the one timber bearings use): it beds on EVERY brick-sized neighbour its underside rests on whatever that neighbour's orientation, so the course-1 corner brick laps the return (`SnapSolverNextCourseBedsOnACrossedNeighbour`; `CornerWallStands` is now 11 pieces / 19 connections / 12 mortar + 7 perpend with the return load-bearing from above). Orientation decides the POSE, contact decides the JOINTS. Still owed from that fix: (ix) ORDER-DEPENDENCE — joints come only from candidates the PLACED piece emits, so a brick laid AFTER a neighbour that rests on it (or beside it via a one-sided corner pose) forms no joint to it (`CornerWallStands`' brick 8 vs the corner brick, depending on build order); the fix is a symmetric "what now rests on / abuts me" sweep at placement time, not in the solver; (x) the lapping corner brick's horizontal face against the next leg's course-1 brick (9-8) classifies CORNER under the crossing rule (its span properly crosses exactly one of that brick's width planes) — pinned as mortar; if a lapping stretcher against the adjacent leg brick should be a perpend, that is a ruling question; (xi) the resting sweep ignores the snap radius (only the POSE is radius-filtered) — harmless while callers pass a radius-bounded view; (xii) snap-solver item (viii)'s 1e-4 cm resting tolerance now governs brick beds as well as timber bearings. NOTES: (i) `DestructionShed3D::BuildRealistic`'s per-pair sweep DELIBERATELY downgrades every horizontal brick-brick contact — quoins included — to the perpend (its comment names the wall corner; `Acceptance.Shed.ThreeD.RealisticBrickShedCornerHangFallsWithWeakPerpends` depends on it), while the COARSE sheds author quoins as full mortar; the ruling is scoped to the interactive build, and routing `BuildRealistic` through `JointForContact` (deferral (a) above) would move the corner-hang fixture — an owner-visible call when taken; (ii) an INFINITE half-extent still reads as a long axis (`Abs(inf − 5) > 0.5`), so `+inf` × Y-long classifies Corner — NaN is caught by the guard shape, +inf is not; one `IsFinite` on the extents with its row; (iii) the 3-arg `JointForContact` keeps its fail-OPEN zero-normal answer (deferral (b)) until every caller is on the boxed overload; (iv) **"crossed long axes" is necessary, not sufficient, for a quoin — NARROWED by CR-2a, not closed** (DESIGN §8): the crossing rule refuses a header stuck through a half-brick wall and a header buried in a thicker wall, but a header in a ONE-BRICK wall of two wythes (stretchers at (0, ±5.625), header at (16.875, 0) — English/Flemish bond) is PAIRWISE identical to the quoin and classifies Corner with two mortar joints; reachable once the rotate control lands (one Free placement makes the second wythe); fix = a third-piece check or a wall-thickness input; a T-junction reads Head (the weak perpend) — a consequence, not a defect; (v) RESOLVED — the tolerance pair and `QuoinReturnAcrossY` (which was a T-JUNCTION, not an L) were re-posed as genuine L's in CR-2a; (vi) `LongAxisUnit` (solver) and `ReadLongAxis` (inference) answer the same "which way does this run" question with different tolerances — fold to one when a third caller appears; (vii-corner) the merged-candidate `Kind` ambiguity (snap-solver item (vii)) gains a third case: a corner pose can coincide with a bed/head pose from ANOTHER neighbour and keeps whichever kind was emitted first (`CornerWallStands` step 7 is a live instance) — joints complete, label ambiguous; (viii) the 3-arg `JointForContact` fail-open zero-normal (deferral (b)) is now UNREACHABLE from the solver (all four sites are boxed); delete the 3-arg overload or keep it documented; +inf extents now fail closed INCIDENTALLY (an infinite span crosses both planes → Head) — the `IsFinite` guard is still the honest fix.

### Deferred from the step-4 slices (still owed)

- **R7 Shipping-FP confirmation (pre-RELEASE, de-risked by argument):** the oracle compiles under UE's default `/fp:precise` in every config (no fast-math override in the Build/Target files), and the Farkas verification runs in ALL configs including Shipping, so Shipping can never name the WRONG bricks — at worst a different-but-valid set or a fail-closed decline. So the residual stake is *test-validity consistency*, not correctness. A bespoke flag-gated Shipping entry point (the Test configuration cannot be built with this binary engine distribution, and Shipping drops the automation tests) that solves the mechanism fixtures and logs a hash of the named set, built Shipping and compared to Development. Owed before shipping; run it or have the user waive it.
- **D8 latency debt:** the block cap default was raised 84 → 200 to bring the medium wall-catalogue fixtures (125–174 blocks) into LP scope. This EXCEEDS the measured ~84–104-block per-action latency band, so a mid-size (84–200 block) production structure, if ever built, would solve the LP synchronously. No production structure sits there today (the default wall declines to the router), so the cost is test-time — but the 200 value wants an owner's ratification and a per-action wall-clock guard (§5.6). The step-4 slices ran one feasibility LP per cascade pass plus a remainder solve; no test guards per-action wall-clock.
- **Block-velocity canonicalization has no lexicographic minimal-support tie-break** — every owned fixture has UNIQUE block velocities (permutation drift ≤ 1.1e-15), so the moving-block set is stable without one and the D7-named tie-break was left unbuilt (no failing test). If a fixture whose collapse is certifiable by two different moving-block sets is ever found, add the index-ordered minimal-support tie-break; the joint set is already robust (derived from kinematics).

## What to do next — ranked

### Decide before building

- **What a selection MEANS when a cascade releases a picked brick** — a product decision with both readings written out (see "piece menu presenter" below). Reachable today: the cascade is already on the world wire (`FStructureBinding::SolveAndBreak` at `Core/StructureBinding.cpp:177`, called by both commit doors in `Core/PieceActions.cpp` and the spawn settle in `World/DestructionStructureSubsystem.cpp`). Decide now.
- **The open user call on §8's cover-pair note** — whether it moves on the LP direction finding (from the 2026-08-12/13 rulings round).

### Then build, in this order

1. **Close the cascade-wire residue.** The wire itself is in-tree (`FStructureBinding::SolveAndBreak`; both commit doors and the spawn settle call it; `ApplyResults` releases Stranded and Falling alike). What its spec still owes: (a) the presenter's "broken (went with a removed piece)" wording at `Core/PieceMenu.cpp:1196` is now a LIVE LIE — a joint the cascade breaks under load gets the removed-piece wording; its failing test was supposed to land with the wire and never did (see piece menu presenter below); (b) confirm whether the strength-driven *world* integration test exists — the leaning stack is now a fixture that can genuinely overload a joint, which was the recorded blocker.
2. **The 2026-08-08 review queue's remaining pieces** — the lost-discrimination replacements, the new acceptance cases, and the standing doubts, all below.
3. **The per-brick joint-force breakout: a human presses Play.** Built end to end and green, but the suite proves the model and nothing about the panel (every run is `-nullrhi`). The check list when someone does: panel legibility (engine-default text, no scroll — a large selection runs off screen), magenta-vs-cyan strength at playing distance, hover routing on the opening frame, whether an entry click is swallowed, the odd reading order (Delete above the readout — accepted for the no-moving-buttons safety property, wants human eyes).
4. **Scenario system — the switching half.** Restart-current, a tiny lightweight default so the game opens fast (today's default is the 30 × 40 wall, ~190 ms of begin-play), and in-game scenario change (the in-world menu of DESIGN §9). Rows are data; a scenario needing a class is the drift to stop.

### Lost discrimination — replacements OWED (from the mean-strength re-anchor; may be built any time, never silently dropped)

- **Too-wide-to-arch (mortared)**: `StructureThrustTest`'s 20-cell fall arm and `StructureCoverTest`'s one-course fall arm both STAND at mean strengths (springing capacity ~4.5×). Replacement: a 20-cell cut under ONE course of cover in the 30×40 wall — hand estimate demand ~1.26 MPa vs capacity ~0.92, ~1.4× over — laid and measured, never re-tuned.
- **Free-end height ladder**: capacity crossing moved ~59 → ~412 courses; the ladder keeps trend + agreement anchor only. Replacement: a deeper multi-course free-end cut (re-design, measure).
- **Corbel crossover pair E35/E36**: the 36-step tension crossover is dead; the worst-axis crossover is now ~124 steps in COMPRESSION (closed form `F(k)·w/(105.0625e4·10)`; bisection cap raised to 150). A replacement pair of scenario levels either side of the measured crossover is owed; `ScenarioHoldTest`'s condemned floor is SUSPENDED (`StandingCases >= 2` interim) until the condemned exemplar lands — restore `CondemnedCases >= 1` then.
- **One-cell anti-over-withhold**: lime's wholesale-withhold outcome signature is dead. `AOneCellArchMustEarnItsThrust` is currently blind to over-withholding (the withhold-everywhere mutation fires 5 other tests but not it). Replacement: pin the earned seats' readings to the arched identity (`ArchedUtilisation`, the 1e-12 kern-edge form `StructureArchingTest` uses) inside `AOneCellArchMustEarnItsThrust`; prove by the same mutation and update its TRAPS row.
- **Composite depth's mortared falling arm**: `ACorbelResistsWithItsWholeDepth`'s two 45-step MustComeDown rows STAND at the mean basis (0.1786 and 0.1948 vs 0.70; the tension ladder can't reach 1.0 below the ~124-step crushing crossover), so both are now `Unasserted`. The DRY row still falls and `AHundredStepCorbelMustComeDown` (150 steps, crushing) carries the giant-corbel guarantee. A mortared depth-bound falling row, if wanted, must be a re-design at the crushing crossover. Deciding the 45-step rows' honest verdict (MustStand is a ruling nobody has made) goes with that re-design.
- **Case 21's post-flip axis is UNVERIFIED**: everywhere records the 0.93542327561664174 as "the squeezed-edge COMPRESSION", but that exact attribution proved WRONG on decomposition for case 9's jamb and case 22's worst joint (both read Mohr-Coulomb SHEAR with compression an order of magnitude under); case 21's worst joint is the same jamb-bed shape. The verdict and inverted-red pins don't depend on it; all eight sites now hedge it rather than assert it. To settle: `FWallResult` already carries the pre-cascade reading — add the worst joint's `GetConnectionForce` decomposition beside it (three doubles, no extra solve) and assert the axis as a one-formula identity as case 9's now is. Bite-prover: write the identity against the WRONG axis, confirm it fails by an order of magnitude (as case-9 mutation W3 measured). Case 22 wants the same seam (its axis is measured Mohr-Coulomb shear, margin 2.91×; asserting it needs the pre-cascade force carried on `FWallResult`).
- **The span pair's span-law floor is RETIRED** (it passed for a reason unrelated to span). What replaced it: the wide opening puts 270,024 uu of horizontal THRUST through its jamb where the narrow one puts exactly none, plus a strict-increase row on the reaction and the 1.9046 ratio pinned as a passing characterisation. **Owed**: the reaction's span term is SUBLINEAR (1.9046× for 2.6928× of span) — a finding, not a red; if step-4's equilibrium promotion gives the reaction an honest span term, restore the ≥ linear arm as a real assertion.

### New acceptance cases wanted (priority order, from the reviews)

| P | Case | Why |
|---|---|---|
| 2 | Compression-vs-shear capacity ratio (one joint, two directions, ~50×) | DESIGN §4 calls it key validation; nothing does it. Belongs beside `ConnectionStrengthTest` |
| 2 | **Connected-wall locality** (cut ONE brick from the middle of a single large bonded running-bond wall; assert the far end keeps its exact support state) | The stronger form of C1. `Acceptance.Locality.AChangeFarFromTheCutStaysPut` covers the DISCONNECTED-regions case but not over-propagation ALONG real joints inside one bonded structure |
| 3 | L-corner / return (brick out at a corner vs a free end; assert the *relation*) | the solver has never seen two orthogonal joint families |
| 4 | Lintel over case 7's opening (with/without) | the pair case 7 needs; the model's first spanning member |
| 5 | Dry-stone acceptance rows (intact; one out) | the only configuration where Mohr-Coulomb coupling is first-order; the set has none |
| 6 | Two-wythe wall (same cut, one vs two wythes) | the composite `t` term has only ever been 10.25 cm |
| 7 | Eccentric surcharge over an opening | no test applies a discrete external load |
| 8 | Progressive removal to a predicted count (12×12 wall; stands at N−1, falls at N) | DESIGN §4's headline integration test exists only on a 6-piece toy |
| 9 | Removal order independence ({A,B} == {B,A} settled state, bit for bit) | cheap, Core-level, uncovered |

### Standing doubts on existing cases (physical evidence would settle, not more derivation)

Case 17 has zero discriminating power on *outcome* intact (narrowed 2026-08-12: λ\* 918.05, 1.41× LP margin vs case 18) — the *wide* stack-bond removal repurpose still stands (utilisation grows with width while running bond arches); case 18's height-independence property belongs on a cheap 3-column Core fixture parameterised over n; case 20's true count is more than 2, fewer than the model's 9-today; case 3's verdict is right but reached via a 2.1 m composite section, not the real mechanism.

## Deliberately left alone — known, unfixed, with pickup context

None of these block the list above. Each needs its own red test first; most are branching logic that nothing drives yet.

### Load solver

- **A cohesionless joint with a real tensile bond survives the withheld one-cell relief, and that entangles gap 5 behind gap 4.** `CohesionlessBond` (dry stone's μ = 0.7 with a real 0.08 MPa tensile bond) is refused the relief correctly at 1.4921 of sliding capacity, then does NOT fail because composite vertical action re-sections the moment to 0.807. The honest verdict needs DESIGN §7 gap 5 (the deep beam's horizontal shear flow applied as a demand on the bed joints) as well. Write the gap-5 demand first, then a row.
- **The one-cell thrust check credits the whole moment vector's magnitude, torsion included.** `(1 − k)·|M|` in `SolveLoads` takes `FVector::Size()`, where only the in-plane part is what a horizontal pair can supply. Fails closed (asks for more thrust), but judges a torsion-carrying joint slightly harshly. Distinguishing fixture needed before either spelling can be preferred.
- **The one-cell thrust gate's degenerate and asymmetric cases are covered only by construction.** The single `!(demand <= capacity)` lands a NaN capacity, a zero/NaN arm and a degenerate area inside the refusal, and per-seat evaluation would let an asymmetric pair resolve one side at a time — both believed right, neither named by a test. One genuine fail-open in the family: the truncation keeps a NaN *capacity* (refuses, correct) but silently drops a NaN `MaxShearStrengthMPa` *ceiling* (compares false → capacity untruncated). Unreachable today (profiles static, fixture-asserted); fold its row in with these.
- **The guard's `Supported` gate and NaN polarity are untested in either direction** (comment at the site in `Structure.cpp` states the cross-pass-vs-within-pass simultaneity honestly). Specified pin: grounded base → over-capacity joint → platform → 12-course stack; pass 1's sweep severs the weak joint; assert whether the stack's bottom bridge carries a pass-1 stamp — either answer is defensible, the value is pinning the convention. NaN polarity (guard fires nothing on garbage arithmetic) likewise has no test either side. *(verify — some of this block predates the Slice-2 deletion of `BreakOverturnedBodies`; re-confirm which gate the comment now refers to.)*
- **Dry stone has no general rocking model.** The specific red (`Acceptance.MustNotFall.APureCompressionDryStackStandsByGeometry`) GREENED at Slice 3b (below the cap the LP mechanism suppresses the spurious kern break), but dry stone's tensile strength is an exact zero, so above the cap a geometrically-stable dry stack outside the kern still reads as falling. A step-5 no-tension rocking model, or keeping such structures below the cap, is the general fix.
- **The leaning-stack fixture family has no playable levels** — the four rows are the first acceptance fixtures without scenario rows. A `FScenario` producer for an offset stack is a small slice.
- **`FConnection` is copyable and the latch is per-copy** — `for (FConnection C : Connections)` latches temporaries and reports a structure that never breaks. Closed at the one site that latches; nothing prevents the next. Revisit if a second site latches.
- **`StructureFuzzSupport::UtilisationUnder` is a deliberate hand transcription** — redirect it at production as its own change (never inside a solver refactor), re-running mutations (g)/(h) before and after (TRAPS registry).
- **`AddConnection` happily joins a removed piece** (a tombstone is a valid index) and such a joint is never severed — fail-open, reachable the day anything adds bracing to a damaged wall. One `GraphValidation` row + a guard reusing `IsPieceRemoved`.
- **A pre-latched `FConnection` can be added through the front door** (`ApplyForce` then `AddConnection`): given, unstamped, invisible to the solver, counted by `NumConnections`. Matters for save/load; reject or accept-and-stamp, red test first.
- **`CheckCascadeCase` has a spec-vs-structure index desync one level up** — a fixture row `AddPiece` rejects would shift every per-piece assertion onto the wrong piece. Needs a decision on what such a row asserts.
- **Cycle-division rule absent** (a true voussoir arch still strands; DESIGN §5.1). Deliberately later; the reason is observable meanwhile — cases 10 and 19 strand 3/6 *live* pieces, pinned by `FWallCase::StrandsToday`. Those pins go to zero when this rule lands.
- **Neither fuzz covers piece removal.** Extend `Structure.CascadeFuzz`: remove 0–2 seeded pieces before cascading, floor the removing-case count, assert (a joint touching a removed piece has given and carries no stamp) and the converse, plus stamps strictly increasing across a cascade–remove–cascade sequence.
- **`CascadeFuzz` doesn't check a joint broke in the *earliest* deserved pass** — sweep every joint in the pass-N graph and require over-capacity ⇒ stamped exactly N. Its own mutation needed.
- **`CascadeFuzz` mostly probes the obliterated regime** (median break 9.9× capacity; only ~20% below 2×). Biasing the mass table toward the marginal band is the cheapest improvement; it moves every reported distribution figure, so do it deliberately.
- **Neither fuzz asserts stranded-set equality both ways** — `GetPieceSupport` now exposes it; promote `STRANDING` from diagnostic to property, mutation to prove it bites.
- **The accumulation ordering contract is named in a comment and asserted nowhere.** Specified test: three bed joints with areas `{1.0, 1e-16, 1e-16}`, exact `==` against the ascending-order sum; second row mixing PieceA/PieceB ends; prove by descending-order mutation.
- **The knot check is O(pieces × (pieces + edges)) per fixpoint pass** — Tarjan SCC + one reusable visited buffer is the known fix. Re-measure the cascade by hand first: the 1.2 s bottom-course delete predates the adjacency index (which took `SolveLoads` 33.6 → 6.4 ms) and nobody has repeated it. Nothing in the suite exercises `SolveAndBreak` at scenario scale.
- **Pass-bound observations unasserted**: inner fixpoint provably ≤ 2 passes (no seam to observe it); outer cascade measured ≤ 4 (pin only as a generous ceiling if ever).
- **`AddConnection` normal top-end hole** (~1e200 components) — lockstep change with the fuzz transcription; see TRAPS.
- **`UtilisationTolerance`'s 1e-12 absolute floor** must stay below the smallest non-zero expectation (`Unbreakable`'s 1.96e-11) or that row can't fail.
- **An unsupported island reads zero on its internal joints** — "carries nothing" is ambiguous between given / grounded-both-sides / falling; a strain readout will someday need more than the force.
- **`StructureCascadeTest.cpp` hard-sets `BrickMassKg = 2.72`** — used only for grounded pads; two-line tidy when the file is next open.

### Rigid-block LP oracle (`Core/RigidBlock/RigidBlockOracle.*`, bridge in `Core/RigidBlock/RigidBlockBridge.*`)

- **THE OPT-IN SWEEP IS TWO TIERS AND THE TIERING IS ITSELF A ROT HAZARD** (TRAPS carries it). `OracleSweepFast` (~10 tests, ~90 s) and `OracleSweepFull` (~5 tests, ~22 min) share the `OracleSweep` stem; neither name contains `DestructionGame`, so the full-suite command never pays for them. `Run-OracleSweep.ps1` ASSERTS the per-tier counts because a new solver-scale test once joined no bucket and ran nowhere. `OracleSweepFull` is mandatory before any commit touching the LP oracle, and before any commit at all if the solver changed; the fast tier verifies nothing about the solver at scale. (Per-test timings live in the automation log; re-measure rather than trust a stale table.)
- **Two sweep optimisations queued and deliberately NOT taken** (warm starts were refuted, so nothing is coming to make these unnecessary — judge on merits): (a) use the feasibility pose on rows where only the verdict matters (~150–250 s, but about half the expensive rows are discriminating pairs where the λ\* *number* is the finding — a per-row judgement); (b) trim the covered-opening test's two bracket neighbours (~141 s, but the bracket proves the 8-cell rung is not secretly its neighbour, TRAPS N11 — the trim costs a bite-prover).
- **Four rows' LIVE pivot pins are pinned NOWHERE** (`FeasibilityReformulationCost` now solves them dead; `WallsAndLadders` pins their λ\* not pivots). Cheap re-homing: pin the pivot counts in `WallsAndLadders`, which already solves those four.
- **The solver performance roadmap** (each lever its own measured slice — never assume, measure): pricing is finished (0.5% of wall-01 runtime); the profile is now BTRAN 44% / refactorisation 33% / FTRAN 18% per-iteration. Still queued: **Markowitz/fill-reducing LU ordering** (refactorisation is a third of runtime), **presolve**, **`RefactoriseEvery = 64` tuning** (untested, 33% of runtime), **the artificial pivot-out pass's scans** (dominant scan cost, its own test first), **Devex/dynamic weights** (cheap scans lengthen some mid-size pivot paths — wall-09 50→125 s while corbel D 80→9 s), **dual-simplex re-solves** after a deletion (a handful of pivots). *Deliberately avoided:* threading the scans (parallel FP sums are order-dependent; bit-reproducibility is a load-bearing contract).
- **Production-side regional decomposition** — still the likeliest answer to 1,220-piece scenarios, but only in a repaired form (omitted weight kept as a dead surcharge, regions ground-anchored by construction). Measured: certifies at 27.5% of a wall for 0.322× a global solve, so the lever is real — but the pessimistic side is NOT a bound (`OracleSweepFast.RigidBlock.SubUnityWallCertificate` certifies falsely at four of six strip widths, wrong by ~40×), so §5.6's fail-closed fallback is mandatory on every region solve. What is owed is bigger than the surcharge: the region must keep the interface joint AND the charged component's internal joints in the problem (a surcharge carries a force and a moment and no STRENGTH), the carried-set rule must stop reading "has a ground path" as "is not carried", and the area-proportional split across multiple interfaces is arbitrary. A cheaper front-runner needs no trustworthiness: D2″(iii)'s probe-once-else-solve-globally. Also: the regional collapse *prover* (item 12) is the sound one-directional cousin already landed.
- **A DELETION-CAUSED sub-1.0 fixture is still wanted** — the chimney wall is infeasible BEFORE its deletion (sound as a refutation, weaker as evidence about a production region lever). The stronger fixture: a wide opening near λ\* = 1 (~40–45 cells) with the deletion in a JAMB (the strip through the jamb is ground-anchored and stands while the cover it no longer supports fails outside the strip). ~500+ blocks, minutes per dead solve; build it only if the region lever is revived.
- **The abandoned warm attempt's refusal reason is discarded at the seam** — `SolveRigidBlock`'s fallback throws away `Warm.WhyNot`/`Warm.Refusal`, so `WarmStartColumnsAccepted == 0` is overloaded across three events. Half is closed (`WarmStartAfterADeletion`/`WarmStartAtWallScale` pin `WarmPivots − ColdPivots == 64`). Owed: carry the discarded attempt's `EOracleRefusal` on `FOracleResult` and assert the wall-01 row's `NumericalFailure` at pivot 64. Its own slice (production code, would leave opt-in tiers red). Same slice: **`PivotsToFirstFeasible` is not carried across the warm fallback retry** (harmless today — no warm-path fixture reads it; three lines apart).
- **The case-21 family's MEAN-BASIS mechanism split is open** (feeds the step-4 brittleness precondition): at mean strengths λ\* moved ×3.128 (17.2389), NOT the ×4.5 a pure jamb-cohesion pricing predicts; abutment sensitivity collapsed; the zeroed-cohesion probe keeps 27.7% of λ\* and the wall now STANDS (4.768). The mean-basis capacity mix (dominant-but-not-whole cohesion plus something abutment-blind) is unattributed. Design the chain-vs-bearing split probe against the MEAN data. The chain-vs-bearing split itself is owed: the probe zeroes all 36 jamb bed joints at once (including the two bearing courses), so it can't say whether the bearing or the run to ground is the tighter link — add one row zeroing only the bearing-course beds, or only the below-bearing run.
- **Two LP cross-fixture identities died at the flip and are open re-derivations**: the free-end 7×10/7×20 λ\* height-identity (256.82 both → 629.199/297.997, ratio 2.1114 — what binds is unestablished) and the wall-13/14 projection-pair cross-validation (0.03% at characteristic data; at mean data the LP reads 1.856 while production keeps 2.7786). Both pinned at measured values; naming the constraints belongs with the step-4 design.
- **The PhaseTwo 99er's certified window is single-reading since the flip** (89.898933555823774 ±2e-5) — re-pair it the next time a pivot-path change gives a second certified reading.
- **Specified test — each rung's clear reveal from the built geometry**: extend `BuildOpeningLadderWall` to compute the top-opening-course clear reveal against the oracle `(OpeningCells + 1)·22.5 − 21.5` (minus 22.5 on an odd course). Per rung: 406.00, 383.50 (equality pin on 18@s=1 AND 17@s=2), 361.00; phase-2 span ladder at s=3: 158.50, 181.00, 203.50. Bite-prover: widen the cut's ±0.25-cell margin to ±0.75, or flip the parity term's sign.
- **Specified test — the cohesion selector's set derived twice**: the Z-below-cover selector and an independent X-outside-the-reveal-band selector must choose the IDENTICAL joint index set (not merely the same count) on every rung. No extra solve. Mutation: shift the X window one cell.
- **Recorded gap — the three probe production-invariance ratios have no bite-prover** (all four probe rows build the identical fixture and `AdjustProblem` can't reach `FStructure` by type). Defensible as a guard against a future structure-side override; the driving mutation is unrecorded. The (17,s=1) rung's pins are covered only by symmetry with the recorded rung-flip.
- **Not reproducible without re-instrumenting**: the deleted active-set probe (which contacts sit at tension/friction/crushing bounds at the optimum — 108/149/0 of 266 on wall-21). Worth building as a real `FOracleResult` field if the thrust-line shape ever needs settling.
- **The case-22 hazard is unwatched**: case 22 answers after 88,810 pivots against a `MaxPivots` of 100,000 (89% of the cap). Bit-identical across re-measurement, but one pivot-path change from a different refusal, and nothing in the suite watches it (a watching row costs 20 minutes a run). The nearest cheap driver for a genuine phase-2 arm is `IterationCap` on a slightly wider case-22 member — the owed replacement for the deleted refusal canary (no fixture this project owns reaches `PhaseOneFailure`, any `PhaseTwo*` arm or `VerificationFailure`, so those five enumerators are proven distinct but reported by no solve). Still owed on the two known refusers: tighten their certified windows from ±1e-4 to the file's usual ±2e-5 once a third pivot path exists.
- **The spurious-unbounded mode is NOT closed in general — only no longer reached on these fixtures.** A mirrored column can price at ~1e-9 of dual drift, FTRAN to a single negative entry, and the ratio test then finds nothing → phase 2 returns Unbounded and the oracle refuses a problem it has effectively solved. The pivot floor keeps the basis conditioned enough that the arm is not reached; it does not make the arm sound. A conservative repair was written and removed (no failing test drove it). First thing to instrument for a *future* refusal.
- **The verification gate deferrals (N3/N4/N1)** — the three sharp ones from the Slice-0a review:
  - **(N3)** the verification gate clamps away the exact error mode the raised `RelativePivotTol` floor makes 100× larger (`FMath::Max(0.0, State.XB[Row])` before the verification loop can see a genuinely-negative basic value). Closure is measurement: carry `double WorstNegativeBasic` on `FOracleResult` set from `min(0, XB[Row])` at extraction, pin it across the slow group. Bite-prover: set `RelativePivotTol` to 1e-6 and watch it grow; reverting to 1e-11 must leave it unchanged. This is the only thing that turns N3's argued bound into a measured one.
  - **(N4)** NaN reads as "the wall collapses" rather than a refusal — `FMath::Max(0.0, NaN)` returns NaN, every `bViolated` compare against NaN is false, λ\* clamps to NaN, `OutcomeOf` reads `NaN >= 1.0` as false → Falls. Fix is `if (!(… <= Tolerance))` (fail-closed, house rule).
  - **(N1)** `PhaseTwoRefusalFor` is new branching logic with no test; its `default:` maps `Optimal` (and any future `ESimplexEnd`) to "singular basis" — fail-closed but silent. Expose it (or a thin `RefusalForSimplexEnd`), assert all four mappings including `default`, microseconds; bite-prover: swap two arms → 2 assertions.
- **(N2/N5) taxonomy hygiene**: move `RefusalNamesItsReason`'s pairwise-distinctness sweep to the default suite (it runs 1.0 s but lives in the opt-in tier, so the only guard on the taxonomy never runs by default) and fold the validation-reason assertion into `RigidBlockOracleTest.cpp`'s existing poison-row table; and pin the control fixture's "83 blocks" with a block/joint assertion (verified 83 by hand in review).
- **The LU/eta fuzz does NOT close `ESimplexEnd::NumericalFailure`** — `FactorisationFuzzTest` drives `FBasisFactor`/`FRevisedState` directly (exercises `Factorise` returning false on a singular duplicate-column path) but not the simplex end-state, which needs a full `SolveRigidBlock` run. That remains the owed replacement for the deleted canary, its own slice, nearest cheap driver `IterationCap` on a wider case-22 member.
- **The artificial pivot-out pass still accepts an entering column on the ABSOLUTE `PivotTol` alone** (`RigidBlockOracle.cpp`, the `EnteringAbs = PivotTol` scan) — the same class of defect as the ratio-test one already fixed, on the pass that picks the basis every phase-2 solve stands on. Measured NOT to be what refuses the eight-course-cover family, so a latent hole wanting a fixture, its own red step. Related: **the pivot-out's ratio uses the FTRAN value where its tolerance checked the BTRAN dot** (~`RigidBlockOracle.cpp:1467`) — cheap fail-closed re-check on `|EnteringW[Row]| > PivotTol`, own red step.
- **One seam where λ could read LOW rather than refuse**: `ApplyPivot` clamps any negative XB to zero, sound only when a leaving artificial's basic value is exactly zero; phase 1's infeasibility gate is loose at 13k-row scale. Reachable only via dead-load problems (two fixtures today) on a knife-edge-infeasible problem; under-report is the one direction verification can't catch. Sentence owed at the site; watch it if dead-load fixtures grow.
- **`Result.SimplexIterations` excludes the artificial pivot-out pass's pivots** — deterministic and harmless, but header pivot tables are not total counts. Note when next touched.
- **The Bland fallback IS driven now, by ordinary fixtures** (opening-ladder rungs enter it 170–358 times). What is unwritten is a fixture chosen to exercise the branch's own behaviour rather than one that happens to reach it; the `PricingCost` rows' "asserted zero" claim is a statement about those two fixtures only.
- **NB residues** (small, each its own slice): the ratio test's dense max scan over `EnteringW` is foldable into `FtranColumn` (worth it only at wall-01 scale); the abutment ladder's j=5 rung (λ\* 11.115) would extend the relation series 1.339 / 1.268 / 1.188 the next time that ladder is open; the promoted j=4 window is ±2e-5 where two certified readings differ by 5.3e-7 (a future re-pin inside it is the window working).
- **Slice 6d — the dead/live split**: the per-block capability is done and proven (`FOracleBlock::bLiveGravity`, `DeadLiveSplit.LiveSurchargeHingesTheHeader`). The FOLLOW-ON is deferred to step-7/shed: HOW a structure designates its live loads (a per-piece live-load designation on `FStructure` + the bridge translating it) is a production-API decision better made with its real consumer. When taken: a posing path so the BRIDGE tags wall-15's six projecting-header courses as live surcharge, and a sweep test asserting wall-15/16 SEPARATE in λ\* under live posing (both moving off 868.62) — must NOT break the existing uniform-gravity identity/ratio pins.
- **Specified test — the free-end reading decomposition**: in `FreeEndHeightLadder`, assert production's worst reading decomposes into brick-weights on an *unrelieved* half seat (`WorstUtilisation / 0.058203838191552663` = 15.867 at 10 courses, 33.635 at 20, 20-course/10-course = 2.1198). Pins the absolute readings (a 2.1× drift passes silently today), names the worst joint arithmetically, fails if the composite-depth walk ever starts relieving it. Bite-prover: scale the half-seat anchor 1%.
- **Fixture preconditions don't pin the profile shear ceilings** (dry stone 6.0, mortar 1.3 `MaxShearStrengthMPa`) — both feed real LP rows, neither binds in any validation row; a retune could make a ceiling govern a hand answer mysteriously. Two one-line `TestEqual`s.
- **Cheap missing validation row**: a *point contact* under an *eccentric* load must read λ\* = 0 (no moment capacity) — the catalogue covers point-contact-centred and finite-contact-eccentric, not the combination.

### Layout producer

- **`ContactsOf`, the RunningBond oracle in `Tests/LayoutTest.cpp` (~line 360), still computes its *area* as `Reach − Distance`** while building its rectangle from bounds — the formula the MakeInterface fix retired from production. Latent-only (`RunningBond` emits no containment topology); same one-line change whenever that sweep gains mixed sizes.
- **`Core.Layout.InterfaceFuzz` is specified and unwritten**: random touching pairs, oracle `HalfExtent[a] == 0.5·min(2eA, 2eB, eA+eB−|Δc|)` per in-plane axis (a different expression for the same set); snap ~30% of offsets to `0`/`±|eA−eB|` to strike the switchover; ~400k pairs (old formula over-reports 301,493, worst 12,210×); mutation: restore `Reach − Distance`. Two cheap example rows wanted: an X-separated containment (all three current rows are Z-separated), and strengthening the inside-both-boxes property with the min-form oracle.
- **`DBL_MAX` extent → infinite area accepted** (finite, so it passes `IsFinite`; the *computed* area overflows). Needs a finiteness check on `AreaSqCm` after the multiply, own red row. Not reachable into a structure (`AddConnection` rejects non-finite areas).
- **`RunningBond` neither clears nor refuses a non-empty out-layout; `AdoptLayout` has the identical question** — answer the two together, same policy.
- **`RunningBond` ignores `AddPiece`/`AddConnection` returns**, and an infinite `BrickSizeCm` passes the `!(x > 0.0)` guard — desyncs boxes from handles, then `JoinIfTouching` indexes `Boxes[INDEX_NONE]`. Fix: `IsFinite` on the spec guard + honour `LayBrick`'s handle; red rejection row first.

### World binding and world

- **`FStructure::HasSupportAnswer` has no direct test** — a removed piece inside the extent answers TRUE (documented staleness), currently unobservable only because `ApplyResults` skips removed pieces first.
- **`ABrickActor::SetPieceRef` accepts a re-point** — a second call is always a bug (click one brick, delete another). Prefer a one-way setter asserting the ref unchanged.
- **`TracePiece`'s null-`Find` and removed-piece steps are written but undriven**; two cheap red rows.
- **`BuildRunningBond`'s three refusal paths are written, unverified.**
- **Brick scale has no degenerate-mesh guard** — a zero-extent mesh gives infinite scale; if written, fail closed, spelled `!(Size > 0.0)`.
- **The scenario cut's batching is unfalsifiable** until a catalogue row cuts ≥ 2 bricks (must arrive as a test change, not a data edit). *(Note: `shedrealistic` now cuts 9 — re-check whether this is still unfalsifiable.)*
- **A mistyped `?Scenario=` is never surfaced to the player** — `OptionNamedNoScenario` is computed and read by nobody but a log line.
- **Degenerate `HoldSeconds` means "never runs"** (timer refuses rate ≤ 0/NaN) — a condemned structure stands forever. Guarded by the catalogue sweep (`> 1.0`, finite); left.
- **The label banner covers the top courses of the tallest structures** — geometrically certain (height-governed rows get 10% clear above; the banner is taller). First user-visible defect in the levels; candidate fixes: bottom-anchor the banner (cheap) or have `ViewpointFor` reserve headroom (moves every swept inequality).
- **The label says nothing about the hold on the ten no-cut rows** — the one-invariant-line claim in `World.Scenarios.Label` must be restated first; no quiet edit.
- **The scenario banner's draw has no test** (content swept many ways; the blit unseen) — same standing gap as the piece-menu panel.
- **The framing aspect is a hard 16:9, no viewport ever measured** — ultrawide players get a wrongly framed structure; the two framing tests pin 16:9 as the unmeasurable-case answer, which any fix must keep.
- **Corbel case F has no absolute anchor** — 2.68132 lives only in comments; the only family row far above 1.0, so the only one that would catch a scaling solver change. Cheap root-utilisation anchor, own red step.
- **`World.Scenario.GameModeBuildsTheWallOnBeginPlay` asserts the model, not the outcome** — the fourth integration test (tick, then assert bricks unmoved and kinematic), grouped into the existing scenario world for cost. Mutation (r) exists.
- **`Visual.ScenarioLevelScreenshots`**: row floor still 9 while `Content.ScenarioMapsExist`'s is 29 (now 35 levels); its "eighteen files" comment is stale. Cost note: many PIE worlds.
- **Per-scenario camera azimuth**: the `shedrealistic` door/porch sit on the −Y face while the shared ThreeQuarter camera orbits from +Y, so that shed is viewed from the back (the collapse is partly hidden through the roof-purlin gaps). Cleanly showing its door/porch/collapse needs a per-scenario azimuth or flipping the builder's front to +Y — offered to the user, not built.
- **The 3D-shed collapse through the AdoptLayout binding hop has no behavioural test** — `Shed3DRow`/`ShedRealisticRow` solve `Layout.Structure` directly; a test that adopts a shed builder's layout into a binding, cuts the post, `SolveAndBreak`, and asserts the overhang reads not-standing while the walls keep the earth (positive control: assembled overhang Supported through the binding) would now be green for the right reason. Also: **`AdoptLayout` drops `EquilibriumGateBlockCap`** (harmless today — no production builder sets a non-default cap — but a future test that sets the cap then adopts would silently reset to 200; not fixed speculatively).
- **The four Y-normal corner joints on the toy/recognizable 3D sheds are structurally INERT** (every wall is independently grounded). Follow-up slice: a fixture arm where a wall segment is grounded ONLY through its two corner mortar joints — corners intact → the segment reads Supported (λ\*≥1); sever both corners → it loses the earth and the mechanism names it moving. The first case where the genuinely-3D Y-normal joint is LOAD-BEARING, catching a corner-load-routing bug the current arms cannot.

### Piece menu presenter

- **Rank-0 product decision** (selection meaning on cascade-release) — see the top of this file.
- **The compact toggle is an undiscoverable double-click** — needs a model-side caption string (red test), then an `SButton` in the title strip.
- **A short selection leaves dead panel** (158 px compact / 262 full) — the list's 190 px cap is reserved, not taken; making panel height a function of selection count is legal but a new behaviour, held against `PanelDoesNotGrowWithTheSelection`.
- **Compact keeps the corner full was opened at** (192 px short of the right edge) — re-homing needs a real "has the player moved it" signal.
- **Compact still lays out the empty headroom-scale rows** (~27 px) — suppressing needs a model-side "is there a bar" field (the no-logic-in-Slate rule forbids the branch).
- **A viewport resize is only corrected on the next grab** — a resize that puts the title strip off screen leaves nothing to grab. Cheap fix: hold the last known viewport size.
- **`Delete` still shifts sideways when the readout collapses** (bounded, safe direction); **a fourth pick pushes Delete down 22 px** (follows the player's own click).
- **"broken (went with a removed piece)" wording is a live lie** (`Core/PieceMenu.cpp:1196`) — the cascade is on the wire, so a joint broken *under load* gets the removed-piece wording today. Needs its failing test (a cascade-broken joint presented, wording asserted wrong); zero-cost interim: drop to bare "broken".
- **A live brick with no joints has no model-side sentence** (the floater case) — a widget would have to branch; `CountText`'s empty-case precedent says give it one.
- **`ForceN` discards the sign** — `Role` recovers bed-above, not head-joint push-vs-pull; a destruction game whose readout can't show tension is worth settling before the shape sets.
- **The entry list carries no liveness** (`FInspectorPieceEntry` presents removed/foreign/live identically) — decide with the widget; the surface where the rank-0 decision becomes legible.
- **`PieceMenuInspectorForSelection()` is private** — no test can assert the controller feeds the widget the right inspector; making it public is the highest-value small test left here.
- **`OnHoverPiece` line-traces every moved-mouse frame, unmeasured** — do not pre-optimise; measure in the same session that judges the overlays.
- **`ChoosePieceMenuRow` dereferences a null `Action` unguarded** (crash is the loud direction); one red `World.Choose` row. Same family: **`ShowPieceMenu` aliasing its own `ShownPieceMenuRows`** — copy-to-local first is the worked pattern in `ChoosePieceMenuRow`.
- **Costs recorded, unpaid**: `bIsLivePiece` costs a full `InspectPiece` per entry per hover; the inspector is built twice per build; `SetInspectedPiece` unguarded for idempotence.
- **Unobserved on a screen** (the press-Play list): shader compilation for all three overlay materials, hover routing on the opening frame, entry-click swallowing, panel legibility at scale, magenta-vs-cyan legibility. The recommended real-RHI test is the engine's own `ShouldCreateNaniteProxy == false` on a registered in-world brick — not a screenshot; if a screenshot is ever written it must be a four-state difference test, never absolute RGB.

### Core systems designed, not built

- **Profiles as editor data assets** (`FConnectionStrength` is a plain struct; adding a row needs a recompile) — the remaining gap to "materials are data".
- **Cross-material pairing above the cap is UNWIRED** — the below-cap LP break authority uses the material pairing (`FStructure::EffectiveJointStrength`, via the bridge), and the router's readout/`GetConnectionUtilisation` do, but `FStructure::BreakByCapacitySweep` (`Core/Structure.cpp:2767`, the router's ABOVE-cap / LP-refusal break authority) still calls `FConnection::ApplyForce` against the BARE connection strength. So a >200-block cross-material structure's above-cap break sweep would not consult the material crush. Wants its own red (a below-then-above-cap cross-material fixture) before wiring — relevant if the shed exceeds the cap. *(Pickup nits: pin `bCompressionDominant == true` on the masonry rows in `ProfileLibraryTest`; revisit an `enum MaterialClass` when a second per-class behaviour lands; whether real Timber's mortar-bond `BondFactor` should be < 1 is a calibration question left until there's evidence.)*
- **Brick-resolution walls** — the shed's toy walls are one block per wall; "take individual bricks out" wants base/head splits / real courses (the realistic shed already does this at 442 blocks on the router). A per-piece ROLE so the roof could be slate-grey and the door frame white (colour is by structural material today).
- **Damage/force manager; piece-size floor modes 1/3 then 2; piece timestamps** (DESIGN §2).
- **Shear direction is discarded** (`FConnectionLoad::Shear` is a magnitude) — matters when anisotropic wood arrives, not before.
- **In-game builder choosing connection types**; **a real brick mesh** (one-line asset-path change; keep the Nanite rule in TRAPS in mind).
- **`FStructurePiece` should take a material rather than a bare mass** (every caller re-derives from density) — small change, do it with the brick-actor work it serves.

## Housekeeping

- **`BeamAcceptanceTest` no longer needs to sidestep containment** (the MakeInterface fix landed): moving its piers inboard would be the suite's only contained bearing exercised through a real solve. Its own slice — readings must be re-derived, not nudged.
- **Source comments cite the deleted design docs by name** (`ARCHING_DESIGN.md` ×80, `MOMENTS_DESIGN.md` and `COMPOSITE_DEPTH_DESIGN.md` ×28 each, `PROJECT_REVIEW.md` ×7, `REAL_WORLD_CHECK.md` ×2). Not urgent (the reasoning is in git); when a cited comment is next touched, repoint it. Find them: `grep -rE "ARCHING_DESIGN|MOMENTS_DESIGN|COMPOSITE_DEPTH_DESIGN|REAL_WORLD_CHECK|PROJECT_REVIEW" Source/`
- **`Scripts/Convert-CommentBlocks.ps1 -Check` exits 2** on `StructureCascadeTest.cpp:707` (backslash-continued comment diagram — the script is behaving correctly). Either rewrite the diagram line or teach the script to pass such files through — exit 2 on every run trains people to ignore the check.
- **The anonymous-namespace sweep has no check behind it** — a `-Check` script over `Tests/` ("no anonymous namespace, no file-scope declaration") would turn an engine-source build error into a named refusal. `Convert-CommentBlocks.ps1` could host it.
- **`Tests/CorbelScreenshotTest.cpp` has not been through review-expert** — a second `NonNullRHI` visual harness written inside a reporting task. Review targets: the duplicated brick-spawn recipe, the hand-written release rule, the 15 m Y-offset applied to spawn only.
- **`PresenterHeadroomScale`'s doc comment is stranded** at `Core/PieceMenu.cpp:604`, eight helpers above its function. Cosmetic.
- **CLAUDE.md's testing section owes the oracle sweep's two tiers, and the edit is the USER's to make** (an agent may not treat a relayed instruction as consent to edit CLAUDE.md). Wanted: the two commands (`Automation RunTests OracleSweepFast` / `…OracleSweepFull`, and `…OracleSweep` for both), their costs (~90 s / ~22 min), and the rule that `OracleSweepFull` is mandatory before any commit touching the LP oracle and before any commit at all if the solver changed.
- **CLAUDE.md needs two edits the moment the first functional test lands** (the `+`-joined filter; "a new test is just a new file" scoped to arithmetic tests). Not yet due.
- **Promote the integration-test entry rule into CLAUDE.md?** Open call, owner: the user (it caught a real bug; the case against is CLAUDE.md staying short). Likewise the visual-run command as Run B in CLAUDE.md's testing section, owner: the user.
- **TestResults PNG untrack still owed** (from review item 13 hygiene; `.gitattributes`, settings prune, script fix and the clean exit-0 comment-checker gate all done).
- **Prune `.claude/settings.local.json`** — a dozen stale single-use permission entries.
- **Case 12's standing-weight bracket (550–1,050 N) has a generous floor** — the most conservative recount reads ≈ 363 N. Verdict-insensitive; tighten the row comment's bracket if that row is next touched.
- **`SolveKeyOf` in `WallAcceptanceTest.cpp` prints `-0.0` and `0.0` as distinct cache keys** for identical geometry — one redundant solve at worst. Normalise the zero if the key is next touched.
- **The load fuzz's inline unbreakable strength literal** (`Tests/StructureFuzzTest.cpp`, grep `1.0e9`) — one-line swap to `DestructionProfiles::Unbreakable`; input change, do it deliberately.
- **Unqualifying the 11 redundant `LayoutTestSupport::`/`StructureFuzzSupport::` qualifications** left from the collision workaround — harmless; belongs to whoever next owns those files.
- **Watch for the transient** `joint #4 must be carrying a bend … it carries 0.000000` (fired once 2026-08-06 during overlapping builds, never reproduced) — a moment going to exactly zero in a corbel fixture is what a mis-firing arching cap looks like; if it recurs, treat as signal.
